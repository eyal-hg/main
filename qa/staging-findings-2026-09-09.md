# ממצאי QA — סטייג׳ינג אחרי העלאת `design/0909` (stg.hak.co.il)

נבדק ב-09.09.2026, אחרי מיזוג `design/0909` ל-`stg` (קומיט `3ccf71d`, ריצת פריסה 34355606561 — הצליחה).
שיטה: מעבר בדפדפן על מסכי הוורקספייס בסטייג׳ עם משתמש מנהל מערכת, בשילוב קריאות GET ישירות ל-API של הסטייג׳
כדי להפריד בין באג בקליינט לבאג בשרת. לא בוצעה אף כתיבה, שליחה או מחיקה.
דגש מיוחד על מסך המדדים, לפי הבקשה. חברות שנבדקו: 105 (גבדור — 13 מדדים), 81 (אומניבייס — 4 מדדים).

## באגים ופערים

פריט 1 נסגר: אימות מול פרודקשיין הראה שזו תופעה של הסטייג׳ בלבד. נותרו תשעה ממצאים פעילים.

| # | ממצא | חומרה | פירוט | המלצה |
|---|------|-------|--------|--------|
| 1 | ~~שגיאת Postgres גולמית מוצגת למשתמש במסך "היום"~~ **בסטייג׳ בלבד — ירד מהרשימה** | — | לוח היומן בכרטיס היום הציג את הטקסט `insert or update on table "Calendar_Events" violates foreign key constraint "Calendar_Events_company_id_fkey"`, ו-`GET /api/calendar/events` החזיר 500. אייל אימת שבפרודקשיין זה לא קורה — נתוני סטייג׳ | אין פעולה |
| 2 | מדד מחושב אף פעם לא מציג ערך | גבוה | השרת מחשב ושומר `current_value` אבל משאיר `actual_received=false`, והקליינט מציג ערך רק כש-`actual_received === true` או שיש `actual_received_at`. תוצאה: "מדד מחושב 3" ו-"test123" מחזיקים 12.0000 לאוגוסט ומוצגים "—". ההוכחה שזה השרת: מדד מקור Google Sheets באותה חברה כן מקבל `actual_received=true` ומציג 436 | שרת: לסמן `actual_received` גם בסנכרון המדדים המחושבים, כמו בסנכרון הגיליון והקטגוריות |
| 3 | יעד שלא הוגדר מוצג כ-0 ולא כ-"—" | גבוה | הקליינט מחפש `target_is_set`, והשרת לא מחזיר את השדה בכלל. הנפילה לאחור היא `target_value != null`, וזה תמיד אמת כי השרת יוצר שורה `0.0000` לכל חודש בשנה מראש. תוצאה: כל חודש עתידי מוצג עם יעד 0, ואי אפשר להבחין בין "היעד הוא 0" לבין "לא נקבע יעד". נובמבר 2026 מוצג 0 בכל 13 השורות | שרת: להחזיר `target_is_set`. עד אז אין דרך נכונה לתקן את זה בקליינט |
| 4 | מספרים במסך המדדים בלי מפריד אלפים | בינוני | תאים הניתנים לעריכה מציגים `String(value)` גולמי: 800000, 500000, 24000, 6.6667. תאים לקריאה בלבד באותה טבלה כן מעוצבים — "1,794" בחברה 81 — וגם הווידג׳ט בדשבורד מציג 6.67 ₪. שתי מוסכמות באותו מסך | להציג ערך מעוצב כשהתא לא בפוקוס, ולעבור לערך גולמי בפוקוס — כמו שעשינו בתאי הסכומים |
| 5 | החיפוש במדדים מחפש רק בשם | בינוני | `MetricsPanel.tsx:3751` מסנן על `definition.name` בלבד. חיפוש "500000" בחברה 105 מחזיר "0 מתוך 13" למרות ש"מלאי" מחזיק יעד 500,000. חיפוש "מלאי" מחזיר 1 מתוך 13 | להוסיף חיפוש על היעד ועל הבפועל, בערך מוחלט ובלי מפריד אלפים — בדיוק ההתנהגות שכבר מוטמעת בתזרים ובפעולות האחרונות |
| 6 | אין דרך לסדר מדדים | בינוני | השדה `order` נקרא בשני מקומות ולא נכתב באף מקום, ואין ממשק גרירה. בכל 13 ההגדרות של חברה 105 הערך 0, ולכן הגיליון תמיד ממוין אלפביתית. מדד עיקרי ומדד בדיקה יושבים זה לצד זה בלי היררכיה | להחליט אם רוצים סדר ידני. אם כן — גרירה ששומרת `order`, כמו בווידג׳טים |
| 7 | נוסחת מדד שמצביעה על הגדרה שלא קיימת מחזירה 0 בשקט | בינוני | הנוסחה של "ממוצע לעסקה" בחברה 105 היא `7 + 8`, אבל הגדרה 8 אינה קיימת בחברה. התוצאה 0, בלי אזהרה ובלי חיווי שהנוסחה שבורה | לאמת קיום הגדרות בשמירת נוסחה, ולסמן במסך מדד שנוסחתו לא ניתנת לחישוב |
| 8 | שתי נקודות קצה חלוקות על אותו מדד | נמוך | `GET /api/companies/105/metrics` מחזיר `source: "hand"` לכל 13 המדדים ו-`unit: "amount"` ל"ממוצע לעסקה", בעוד `GET /api/workspace/monthly-targets` מחזיר `calc`/`cats`/`sheets` ו-`unit: "number"`. המסך משתמש בשנייה ולכן מוצג נכון, אבל כל צרכן אחר יקבל תמונה שגויה | לבטל את הישנה או ליישר אותה מול אותה שכבה |
| 9 | כותרת פגישה מעוותת | נמוך | בזירת הפגישות של חברה 105 מופיעה פגישה בשם `גבדור בע"מ, 2026-05-03--00`. שתי פגישות באותו תאריך, ולמנגנון יצירת השם נוסף סיומת שנשארה חשופה. בנוסף כל 27 הפגישות נושאות את אותו תבנית "שם חברה, תאריך" ואין כותרת אמיתית | לנקות את הסיומת, ולשקול שם ברירת מחדל שכולל שעה במקום מונה |
| 10 | מעטפות שגיאה לא אחידות ב-API | נמוך | `GET /api/widgets/999999` מחזיר `{"error":"company not found","field":"company"}` ואילו קריאה בלי טוקן מחזירה `{"success":false,"message":"No token provided"}`. הקליינט צריך לדעת לקרוא שתי צורות | מעטפה אחת לכל השגיאות |

## עובד לפי המפרט ✓

- **חיפוש סכום בערך מוחלט ובלי מפריד אלפים** — השינוי שלנו עלה ועובד בסטייג׳: בווידג׳ט תחזית התזרים "44,000" מסנן לפריט +44,000, ו-"3894" מסנן לפריט −3,894 ✓
- `/api/widgets` מחזיר שגיאות כ-JSON קצר עם קוד ולא כדף HTML — פריט 151 של עידו מאומת ✓
- מסך המדדים: פתיחה וקיפול של חודש, שלוש עמודות בפועל/יעד/ביצוע, שבבי מקור (הזנה ידנית · לפי קטגוריות · מחושב · Google Sheets · דוח חשבונאי), בורר ימי עבודה 23/22/21/20, בורר טווח חודשים, שורת הוספה מהירה ✓
- סנכרון מדדי קטגוריות רץ ומעדכן: ארבעת מדדי הקטגוריות בחברה 105 קיבלו `actual_received_at` היום ב-13:23 ✓
- סנכרון Google Sheets מזין בפועל ומסמן אותו כהתקבל: "יעד לידים חודשי" ספטמבר = 436 ✓
- חיווי חריגה בתזרים: חברה 81 מציגה "חריגה צפויה · 15.9.2026" באדום מול יתרה של −360,849 ₪ ✓
- זירת הפגישות: "האחרונה 02.06" מדלגת נכון על פגישה שסומנה "לא התקיימה" ב-14.06 ✓
- רצועת המדדים בתפעול עקבית פנימית: 6 מתוך 37 פגישות חודשיות מול "31 בלי פגישות החודש" ✓
- אין שגיאות קונסול לאורך הסבב ✓

## לא כוסה בסבב זה

- כל פעולת כתיבה: שינוי ימי עבודה, יצירה/עריכה/מחיקה של מדד, אישור משימה, שליחת הודעה או סיכום
- זרימת ההקלטה מקצה לקצה — דורשת מיקרופון ושיתוף מסך
- קבוצות וואטסאפ עם נתונים אמיתיים; חברה 81 בלי קבוצות, ולא נבחרה חברה עם היסטוריה
- אימות סדר הווידג׳טים בחברה 514 (המקרה המעורב) — אושרה גרירה, טרם בוצעה
- שאר הפריטים שעידו סגר: 185 ניגון הקלטה, 196 ג׳וב תיאום מחדש, 140–145 עוזר ותמונת תזרים
