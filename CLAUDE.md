# לפני כל משימה בריפו הזה

קרא קודם את `docs/AI-GUIDE.md` — מה אייל רוצה, מה אסור, והכרעות שכבר נפלו. אחר כך את המשימה.
הפרוטוטיפ (`index.html`) הוא האמת. לא נוגעים במה שלא במשימה. לא ברור — Reject בלוח, לא ניחוש.

**המשימות שלך כבר כאן, בריפו.** `git pull`, ואז:
`node board/md.mjs <שם>` — כל מה שעליך (עידו / עומרי / אייל), `node board/md.mjs 86` — פריט אחד לפי המספר.
המקור: `board/data.js` (פריטים עם `dev` = השם, `status` פתוח; `pin` = קודם כל). אין צורך להוריד MD מהלוח.
