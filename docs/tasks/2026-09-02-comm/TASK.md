# משימה: מסך תקשורת — הנחיה מלאה ל-AI של המתכנת

> **איך לקרוא את הקובץ הזה.** אתה ה-AI שבונה את מסך **תקשורת** במערכת HK
> (`/workspace/communications`, ליועץ ולמנהל התזרים — אותו מסך בדיוק לשניהם). הקובץ הזה הוא
> המקור היחיד למשימה. אין תיאור אחר ב-Asana ואין מסמך נוסף. הוא מכיל, בסדר הזה:
> (א) מה המקור המחייב, (ב) מה נכשל בסבב האחרון ומה לתקן, (ג) המודל, ההרשאות וההתנהגות שהקובץ
> הרץ לא יכול להראות, (ד) 28 קריטריוני מבנה, (ה) איך מסיימים.

## א · המקור המחייב — קובץ אחד

**`docs/adv3/meetings.html`** בריפו הזה, נפתח בדפדפן כמו שהוא (`?embed=1` מסתיר את סרגל
הניווט של הפרוטוטיפ). זה העיצוב והמבנה: הרצועה, שורת הסינון, רצועת החיפוש, הרשימה, התור,
פאנל האירוע, הפופאפים. **בנה בדיוק את מה שאתה רואה, אזור אחרי אזור.** הטקסט שבקובץ הוא
הטקסט במסך. הפונקציות בקובץ (`stripQ`, `arenaShow`, `unidRender`, `weekHTML`, `ksegRender`,
`arSendPop`) מראות את ההתנהגות — אתה בונה את ההתנהגות הזאת על הנתונים האמיתיים.

הכללים של המוצר: אין `<select>` נייטיב · אין `alert`/`confirm`/`prompt` · כל פופאפ נסגר ב-✕,
Escape ולחיצה ברקע · קורל = פעולה, אדום עמוק = סכנה כספית בלבד, נייבי = מבנה · אין "HK"
בשום טקסט שבעל העסק יכול לראות.

אם משהו כאן סותר את הקובץ — **הקובץ קובע**, ואתה כותב על הסתירה בתגובה במשימה במקום להכריע לבד.

## ב · הסבב האחרון (03.09) — מה נכשל, לתקן הכל

נבדק בסטייג׳ינג כמנהל מערכת, מול הקריטריונים בסעיף ד. לפי חומרה:

1. **אין פאנל אירוע בכלל** (סעיפים 19, 7, 22–25). לחיצה על שורה לא עושה כלום, "לאישור" לא
   עושה כלום. צריך: לחיצה על שורה, וגם על כפתור הפעולה, פותחת פאנל **בצד הרשימה, באותו מסך**
   (`arenaShow(id)` בקובץ). ראש כהה: חברה · שם האירוע · תאריך · שעה · משך · צ׳יפ ערוץ · צ׳יפ מצב
   · נגן. **חמישה טאבים בסדר הזה: סיכום פגישה · משימות · זיכרון · משוב · תמלול.** בתחתית
   **"אישור ושליחה"** (פותח פופאפ נמענים: אנשי הקשר של החברה, שם + תפקיד, תיבות סימון, אף אחד
   לא מסומן, הכפתור פעיל רק אחרי סימון) ו-**"אישור ללא שליחה"**. לשיחה, קבוצה ועוזר — אין
   כפתורי אישור/שליחה.
2. **הרצועה הכהה היא ראש המסך, לא התראה** (סעיף 1). בסטייג׳ינג יש מונה וכפתור בלבד. צריך,
   מימין לשמאל: הכותרת **"תקשורת"** עם קו קורל קצר · התאריך "יום חמישי · 02.07.2026" · קו
   מפריד · "ממתין לשיוך · **N** שיחות טלפון לא מזוהות · בלי שיוך לחברה התמלול לא מתחיל" ·
   הכפתור **"לשיוך"** · ובקצה השמאלי השעון "10:54 · עכשיו". ב-N=0 הרצועה נשארת: "ממתין לשיוך
   · 0 · כל השיחות שויכו · אין מה לשייך כרגע · לתור".
3. **הסיכום נשפך לשורות** (סעיפים 6, 14). כל שורה מציגה פסקת סיכום עם התווית "הקטע שנמצא"
   גם בלי חיפוש. צריך: `.hit` ריק כשאין חיפוש. רק אחרי "חיפוש" מופיע בשורה הקטע שנמצא, עם
   המילה מודגשת. בלי חיפוש השורה היא: תאריך · ערוץ · לקוח · שם האירוע · מה חסר · פעולה. וזהו.
4. **שיחות טלפון מסומנות "סיכום לא אושר"** (סעיפים 9, 17). "מה חסר" הוא אוצר מילים סגור של
   חמישה ערכים (סעיף ד-17). שלושת הכתומים לפגישה בלבד; שיחה, קבוצה ועוזר מקבלים רק "סיכום
   נוצר" או "לא הוקלטה". להסיר "נקבעה" ואת הצ׳יפ השני "נשלח". מתחת לערך — המדד של הערוץ:
   משך (פגישה ושיחה) · הודעות וכותבים (קבוצה) · שאלות (עוזר). בסטייג׳ינג לפגישות אין משך.
5. **שם האירוע בשורה** (סעיף 16). בסטייג׳ינג: "חברה, 2026-09-03", "xxx", "שיווק", "שיחה עם
   +9725…". צריך: פגישה — שם הפגישה; שיחה — "שיחה עם {איש קשר}" והמספר רק כשאין איש קשר;
   קבוצה — "קבוצת הוואטסאפ"; עוזר — "העוזר · {יוזר}".
6. **התור** (סעיפים 6, 8). חסרים: פס הסטטיסטיקה של השבוע (`weekHTML`: שיחות השבוע · שעות שיחה
   · ממתינות לשיוך שלך · "הכי הרבה השבוע" · מד שני צבעים עם מקרא); נגן וגל בשורה; הסיבה
   ("⚠ המספר לא מוכר לאף לקוח" / "⚠ {שם} מחזיק שתי חברות — לאיזו מהן לשייך?" עם שתי
   החברות); "מחיקה" שמעבירה לארכיון "שיחות שנמחקו" עם "נמחקה {מתי} · {מי}" ו"שחזור לתור".
   מה שכבר טוב: כותרת התור + "חזרה לתקשורת", ממתינות/ארכיון + חיפוש, פופאפ "לאיזו חברה שייכת
   השיחה?" עם "חיפוש חברה", ✕, ביטול, "שיוך והתחלת עיבוד".
7. **קטנות** (סעיפים 12, 21, 20): אייקון וצבע לכל ערוץ בבורר (`KSEG`/`KCHIP` בקובץ) · שורת
   סיכום בתחתית "משוב מהלקוח: N מתוך M · הוקלטו: N" · מצב ריק עם כפתור "ניקוי הסינון".

**עובר ולא נוגעים:** הרצועה כולה לחיצה · בורר לקוח עם חיפוש · רצועת "מתי דיברנו על…" עם
"ניקוי" אחרי חיפוש · תוצאות חיפוש עם קטע מודגש · העמודות · אין `select`.

**איכות, לא קריטריון:** "מסגרת אשראי" מחזיר 24 מתוך 50 אירועים, כולל סיכומי בדיקות תמלול,
והקטע המודגש בהם הוא תחילת הסיכום ולא קטע רלוונטי — לכוון את סף הדמיון, ולהחזיר את
הפסקה הרלוונטית. וואטסאפ ועוזר AI על 0 — לוודא שזה כי הג׳וב הלילי עוד לא רץ.

---

# ג · מה הקובץ לא יכול להראות — מודל, הרשאות, התנהגות

**04.09.2026 10:18 — סבב סגירה בסטייג׳ינג (server bbf86ee · client 04a2c5f):** המסך נטען ב-`/workspace/communications`
בשני התפקידים: פס "42 שיחות טלפון לא מזוהות · ממתין לשיוך · לשייך", מסנני ערוץ עם מונים, חיפוש "מתי דיברנו על…",
הטבלה עם "מה חסר" ו"לאישור". **באג ניווט:** לחיצה על "תקשורת" בסרגל הגלובלי **לא מנווטת** (נשארים במסך
הנוכחי) — גם כמנהל מערכת וגם כיועץ. רק כתובת ישירה עובדת. לתקן לפני Done.

**04.09.2026 10:25 — סיכום הפגישה בזירה, "תובנות מרכזיות" (צילום מאייל):** לא לפי הפרוטוטיפ (`docs/adv3/meetings.html`,
`.ar-ins2`): (1) בסטייג׳ינג התובנות עם חיצים "◄" — בפרוטוטיפ **ממוספרות** ("1. כותרת"), הכותרת מודגשת בשורה משלה
(14px, נייבי), הטקסט מתחתיה (13px, 1.55); (2) "קרא עוד" **מודבק לכותרת בלי רווח** ("אסטרטגי**קרא עוד**", "עב**קרא עוד**")
ושובר מילה — בפרוטוטיפ זה **כפתור נפרד בשורה משלו** מתחת לפסקה, תכלת 12px, שמחליף ל"פחות" ופותח את ההמשך במקום;
(3) "משימות לביצוע" מוצג כקופסה עם "1." בשורה נפרדת ו"אחראי · היועץ" — בפרוטוטיפ "משימות שנפתחו": שורה אחת למשימה,
טקסט + אחראי + צ׳יפ מצב, בלי מספור בקופסה. לתקן בסבב הזה (חל גם על פגישות בכרטיס החברה — אותה זירה).

## 1 · המודל

כל שיחה נכנסת דרך **קו של המערכת** (מספר טלפון, SIM). לקו יש הגדרה במסך "מספרי טלפון":

| שדה | משמעות |
|---|---|
| `number` | המספר |
| `representative_user_id` | **נציג** — היועץ / מנהל התזרים שמחזיק את הקו |
| `business_id` | **חברת ייעוץ** — המשרד שהקו שייך לו (HK היא חברת ייעוץ כמו כל אחת) |

**שיחה יורשת את שניהם מהקו שקלט אותה.** שיחה שלא שויכה לחברה (לקוח) היא "לא מזוהה":
ההקלטה נשמרת, התמלול לא מתחיל, והיא ממתינה בתור.

## 2 · מי רואה אילו שיחות לא מזוהות

הכלל אחד לכל המערכת: **רק בתוך חברת הייעוץ של המשתמש המחובר**, ובתוכה לפי התפקיד:

| תפקיד | רואה שיחות לא מזוהות מ… |
|---|---|
| `SUPER_ADMIN`, `HK_CONSULTANT` | כל הקווים של HK |
| `HK_REPRESENTATIVE` | רק הקווים שהוא הנציג שלהם |
| `ADMIN` (מנהל חברת ייעוץ) | כל הקווים של חברת הייעוץ שלו |
| `REPRESENTATIVE` (יועץ) | רק הקווים שהוא הנציג שלהם |

- **אף אחד לא רואה קווים של חברת ייעוץ אחרת** — גם לא מנהל המערכת. המסנן "כל חברות
  הייעוץ" בפס העליון לא משפיע על זה.
- **המונה ברצועה** סופר לפי אותו כלל: N = השיחות הלא מזוהות שהמשתמש הזה רואה.
- כשהתוצאה ריקה הרצועה נשארת (היא ראש המסך) ומראה "ממתין לשיוך · 0 · כל השיחות שויכו ·
  אין מה לשייך כרגע · לתור" — כמו בפרוטוטיפ (`stripQ()`). *תוקן 03.09: קודם היה כתוב כאן
  "אין רצועה", וזה סתר את הקובץ.*

```sql
-- השיחות הלא מזוהות של המשתמש u
SELECT c.* FROM calls c
JOIN phone_lines l ON l.id = c.line_id
WHERE c.company_id IS NULL AND c.deleted_at IS NULL
  AND l.business_id = u.business_id
  AND (u.role IN ('SUPER_ADMIN','HK_CONSULTANT','ADMIN') OR l.representative_user_id = u.id)
```

- **שיוך** שיחה לחברה (מהתור) מפעיל מיד תמלול → סיכום → זיכרון, לפי אפיון הג׳וב. השיחה
  יוצאת מהתור לכולם.
- **מחיקה** מהתור = ארכיון (`deleted_at`), עם שחזור.

**קו בלי חברת ייעוץ** ("בחירת חברה" ריק): שיחה שנכנסה בו **נשארת מוסתרת** — לא מופיעה לאף
אחד, לא ברצועה ולא במונה — עד שמישהו משייך את הקו לחברת ייעוץ במסך "מספרי טלפון". ברגע
השיוך היא מופיעה למי שהכלל למעלה מתיר. (הכרעת אייל, 02.09.)

## 2א · פגישות — לפי החברה, כמו היום

לפגישה **תמיד** יש חברה — אין פגישה בלי חברה, ולכן אין "פגישה לא מזוהה" ואין תור.
הנראות של פגישה במסך התקשורת = הנראות של החברה שלה: המשתמש רואה את הפגישות של החברות
שיש לו הרשאה אליהן, בדיוק אותה רשימת חברות שהוא רואה בסרגל ובמסך "התיק שלי". שום כלל
נוסף. אותו דבר לשיחות **אחרי** שיוך: משויכה לחברה — נראית למי שרואה את החברה.

**"אישור ושליחה" של סיכום פגישה** (בזירה, וגם "שליחה ללקוח" של סיכום שאושר בלי שליחה) לא
שולח מיד: נפתח פופאפ "למי לשלוח את הסיכום" עם אנשי הקשר של החברה (מטבלת `contact`), שם
ותפקיד, ותיבת סימון לכל אחד. אף אחד לא מסומן מראש; "אישור ושליחה" פעיל רק אחרי סימון. ✕ /
Escape / לחיצה ברקע — ביטול. בשורת הרשימה הכפתור נקרא "לאישור" והוא רק פותח את הזירה.

## 2ב · מה כתוב בשורה — בלי "נושא"

לשיחה, לקבוצה ולעוזר **אין נושא** לפני עיבוד, ולכן השורה ברשימת התקשורת לא מציגה נושא.
היא מציגה רק מה שידוע מהנתונים:

| ערוץ | טקסט השורה | מאיפה |
|---|---|---|
| פגישה | שם הפגישה | היומן |
| שיחת טלפון | "שיחה עם {שם איש הקשר}" | המספר שהתקשר מול אנשי הקשר של החברה (`contact`); אם לא נמצא — המספר |
| קבוצת וואטסאפ | "קבוצת הוואטסאפ" | שם הקבוצה |
| עוזר | "העוזר · {שם היוזר}" | היוזר שהתכתב |

בעמודת המצב: משך השיחה, מספר ההודעות והכותבים, מספר השאלות — כמו היום.

## 2ג · החיפוש — הפיצ׳ר החזק במסך

**מה זה:** "מתי דיברנו עם הלקוח על מסגרת אשראי?" — מקלידים, לוחצים "חיפוש", ומקבלים את
האירועים שבהם זה עלה, עם הקטע עצמו. **חיפוש סמנטי על embeddings**, לא חיפוש מילים:
מוצא גם ניסוח אחר של אותו עניין ("קו אשראי", "הגדלת המסגרת מול הבנק").

- **על מה מחפשים:** התוכן של **50 האירועים האחרונים** בטווח הנראות של המשתמש — פגישות,
  שיחות **וגם סיכומי הקבוצות בוואטסאפ**: סיכומים, משימות, החלטות, תמלולים. כלומר **כל סיכום
  שהג׳וב מייצר עובר embedding**, כולל הסיכום הלילי של הקבוצה (`comm_embeddings` באפיון הג׳וב).
- **כשמסונן לקוח** — רק אצלו (ואז 50 האירועים האחרונים שלו). ההסבר מתחת לשדה אומר את זה
  במפורש: "מסונן לקוח — מחפשים רק אצל {שם}".
- **הפעלה:** Enter או כפתור "חיפוש" (קורל — פעולה). לא סינון חי תוך כדי הקלדה: זו בקשה לשרת.
  ריקון השדה מנקה.
- **התוצאה:** פס "N אירועים שבהם דיברו על ״…״ — אצל {לקוח} · מתוך 50 האירועים האחרונים",
  והרשימה מצטמצמת לאירועים שנמצאו, לפי דמיון. **בכל שורה — הקטע שנמצא** ("סעיף 3 · …")
  עם הדגשת ההתאמה, ולחיצה פותחת את הזירה על הסעיף.
- **מהירות:** התשובה מיידית (vector search), בלי קריאה למודל. חשוב שזה יורגש.

```
POST /communications/search   { q, company_id?, window: 50 }
← [{ comm_id, kind, company, date, passage, score }]   ← ממוין לפי score
```

## 3 · מסך "מספרי טלפון" — תיקון הבוררים

היום שני הבוררים בכל שורה ("נציג", "חברת ייעוץ") הם `<select>` רגיל עם כל המשתמשים במערכת —
רשימה של עשרות שמות, בלי חיפוש. **להחליף בבורר עם חיפוש**, כמו שאר בוררי הלקוח במערכת:

- שדה טקסט; הקלדה מסננת את הרשימה תוך כדי (טייפ-אהד), ניווט עם חצים, Enter בוחר, Esc סוגר.
  לא `<select>` של הדפדפן.
- **"נציג"** מציג רק את המשתמשים של חברת הייעוץ שנבחרה באותה שורה (אם נבחרה); אם לא —
  את כולם, עם שם חברת הייעוץ ליד כל שם.
- **"חברת ייעוץ"** — אותו בורר, על רשימת חברות הייעוץ.
- שינוי חברת ייעוץ מאפס נציג שלא שייך אליה.
- שאר המסך כמו היום: מונה השיחות מתחת למספר, סטטוס "משויך / ללא נציג", הוספת מספר ידנית.

## 4 · API (סקיצה)

```
GET  /calls/unidentified?view=wait|deleted&q          ← לפי הכלל ב-§2, מהמשתמש המחובר
POST /calls/:id/assign      {company_id}              ← שיוך → עיבוד מיידי
POST /calls/:id/archive · /restore
GET  /admin/phone-lines
PUT  /admin/phone-lines/:id {representative_user_id, business_id}
GET  /admin/users?business_id=&q=                      ← לבורר הנציג
GET  /admin/businesses?q=                              ← לבורר חברת הייעוץ
```

## 5 · קריטריוני נתונים והרשאות (7)

1. יועץ רואה ברצועה ובתור רק שיחות מהקווים שהוא הנציג שלהם; מנהל חברת ייעוץ — כל הקווים
   של החברה; אף אחד לא רואה חברה אחרת. המונה תואם.
2. אותו כלל לתפקידי HK: מנהל מערכת ויועץ HK — כל הקווים של HK; מנהל תזרים — רק שלו.
3. שיוך שיחה מוציא אותה מהתור לכולם ומפעיל עיבוד.
4. שיחה מקו בלי חברת ייעוץ לא מופיעה לאף אחד עד שהקו משויך; אחרי השיוך היא מופיעה בלי פעולה נוספת.
5. במסך "מספרי טלפון" שני הבוררים עם חיפוש, בלי `<select>`; "נציג" מסונן לפי חברת הייעוץ.
6. פגישות ושיחות משויכות נראות בדיוק למי שרואה את החברה שלהן — לא פחות ולא יותר.
7. חיפוש "מסגרת אשראי" מחזיר גם אירוע שבו נאמר "קו אשראי", עם הקטע; מסונן לקוח — רק אצלו; תוך פחות משנייה.

# ד · 28 קריטריוני מבנה והתנהגות — אזור אחרי אזור

**הפרוטוטיפ הוא המקור המחייב למבנה:** `docs/adv3/meetings.html` — בריפו, נפתח בדפדפן
כמו שהוא. הרשימה הזאת נכתבה במעבר על הקובץ מלמעלה למטה, אזור אחרי אזור — לא מתוך באגים
שנמצאו. כל סעיף הוא בדיקה שעוברים או נכשלים בה. **מה שלא ברשימה ונמצא בקובץ — גם הוא
מחייב;** הרשימה עוזרת לא לפספס, היא לא מחליפה את הקובץ.

**א · הרצועה הכהה (ראש המסך)**

1. מימין לשמאל: הכותרת **"תקשורת"** עם קו קורל קצר מתחתיה · התאריך "יום חמישי · 02.07.2026"
   · קו מפריד · המשפט "ממתין לשיוך · **N** שיחות טלפון לא מזוהות · בלי שיוך לחברה התמלול לא
   מתחיל" · הכפתור **"לשיוך"** · ובקצה השמאלי השעון "10:54 · עכשיו". רצועה עם מונה וכפתור
   בלבד (מה שחזר בסבב השני) — **נכשל.**
2. כל החלק "ממתין לשיוך" הוא לחיץ ופותח את תור הלא-מזוהות (ב); לא רק הכפתור.
3. N = 0: הרצועה נשארת ומראה "ממתין לשיוך · 0 · כל השיחות שויכו · אין מה לשייך כרגע ·
   לתור". לא נעלמת, לא ריקה.
4. שום אייקון או כפתור ליד הכותרת "תקשורת". (בפרוטוטיפ היה באג CSS שערם את אייקוני הערוצים
   שם — תוקן 03.09.)

**ב · תור הלא-מזוהות (נפתח מהרצועה, באותו מסך, במקום הרשימה)**

5. כותרת "שיחות טלפון לא מזוהות" עם "חזרה לתקשורת".
6. פס סטטיסטיקה של השבוע: שיחות השבוע · שעות שיחה · ממתינות לשיוך שלך · "הכי הרבה השבוע"
   (שלושה שמות עם מונה) · מד שני צבעים "זוהו ושויכו לבד / ממתינות" עם מקרא.
7. שני מצבים: **ממתינות לשיוך (N)** · **שיחות שנמחקו (N)**, ושדה "חיפוש לפי מספר טלפון, שם או
   חברה". במצב ממתינות ההערה: "אפשר להאזין להקלטה כבר עכשיו. התמלול והסיכום מתחילים רק אחרי
   השיוך…".
8. שורת שיחה: אייקון · המספר (LTR) · מי מחזיק אותו או "לא מזוהה" · נגן (Play, גל, משך) · מתי.
   שורה שנייה: הסיבה — "⚠ המספר לא מוכר לאף לקוח" או "⚠ {שם} מחזיק שתי חברות — לאיזו מהן
   לשייך?" עם שתי החברות · כפתור **"מחיקה"** · בורר **"שיוך לחברה"** עם חיפוש (בלי `select`).
   אין נושא ואין סיכום — הם נולדים רק אחרי השיוך.
9. שיוך: השורה עוברת לרשימת התקשורת כשיחה משויכת, יוצאת מהתור לכולם, מפעילה תמלול → סיכום
   → זיכרון. מחיקה: לארכיון "שיחות שנמחקו" עם "נמחקה {מתי} · {מי}" ו-**"שחזור לתור"**.
10. תור ריק: "אין שיחות ממתינות — כל השיחות שנקלטו שויכו לחברה". חיפוש בלי תוצאה: "אין שיחה
    שמכילה ״…״".

**ג · שורת הסינון (מעל הרשימה)**

11. בורר לקוח **עם חיפוש** ("כל הלקוחות" · רשימה עם שדה "חיפוש לקוח"), לא `select`.
12. בורר ערוץ: **הכל (N) · פגישות (N) · טלפון (N) · וואטסאפ (N) · עוזר AI (N)**, כל ערוץ עם
    האייקון והצבע שלו; המונים נגזרים מהשורות. ערוץ שמראה 0 כשיש בו שורות — נכשל.

**ד · רצועת החיפוש "מתי דיברנו על…"**

13. כותרת "מתי דיברנו על…" + ההסבר "חיפוש בתוכן, לא בשמות — מוצא גם ניסוח אחר, ומראה את
    הקטע בפגישה" · שדה עם placeholder "מסגרת אשראי, גבייה, תמחור… — ב-50 האירועים האחרונים"
    · כפתור **"חיפוש"** (קורל) · "ניקוי". Enter = חיפוש.
14. תוצאה: שורת סיכום מעל הטבלה, הרשימה מצטמצמת לאירועים שנמצאו, ובכל שורה **הקטע שנמצא**
    (`.hit`) עם המילה מודגשת; בפאנל האירוע אותו קטע מסומן בסיכום. בלי חיפוש פעיל `.hit` ריק.

**ה · הרשימה**

15. עמודות: **תאריך · ערוץ · לקוח · מה חסר · פעולה** — שורה אחת לאירוע.
16. תא התאריך: "היום" / "02.07" ומתחתיו השעה או המדד (4 שאלות · סיכום לילי). תא הערוץ:
    צ׳יפ עם אייקון — פגישה / שיחה / קבוצה / עוזר AI, בצבע הערוץ. תא הלקוח: שם החברה
    (קישור) · שם האירוע: שם הפגישה / "שיחה עם {איש קשר}" / "קבוצת הוואטסאפ" / "העוזר · {יוזר}".
17. "מה חסר" — אוצר מילים סגור: **סיכום נוצר** (ירוק) · **לא הוקלטה** (אדום) · **סיכום לא
    אושר** · **סיכום לא נשלח** · **הכנה לא נפתחה** (כתום, **פגישה בלבד**). מתחתיו המדד של
    הערוץ: משך / הודעות וכותבים / שאלות. שיחה, קבוצה ועוזר — רק "סיכום נוצר" או "לא הוקלטה".
18. תא הפעולה: **"לאישור"** (סיכום לא אושר) · **"שליחה ללקוח"** (אושר ולא נשלח) · **"תיאום
    מחדש"** (פגישה שלא התקיימה). כולם רק פותחים את הפאנל על אותו אירוע; "שליחה ללקוח" פותח
    אותו על מצב שליחה. אף כפתור לא מאשר ולא שולח לבד.
19. לחיצה על שורה (או Enter) פותחת את הפאנל **באותו מסך**; השורה מסומנת.
20. מסנן בלי תוצאות: "אין אירועים…" עם **"ניקוי הסינון"**.
21. שורת סיכום בתחתית: "משוב מהלקוח: **N מתוך M** · הוקלטו: **N** · הפגישה האחרונה שנשלחה…".

**ו · פאנל האירוע (הזירה, בצד הרשימה)**

22. ראש כהה: שם החברה · שם האירוע · תאריך · שעה · משך · צ׳יפ ערוץ · צ׳יפ מצב ("ממתין
    לאישור" וכד׳) · נגן ההקלטה עם המשך.
23. חמישה טאבים בסדר: **סיכום פגישה · משימות · זיכרון · משוב · תמלול**. בסיכום: "תמונת מצב
    ונתוני יסוד" · "תובנות מרכזיות" (כל תובנה עם **"קרא עוד"**) · "משימות לביצוע" עם אחראי.
24. תחתית: **"אישור ושליחה"** (קורל) ו-**"אישור ללא שליחה"**. באירוע שאושר ולא נשלח — כפתור
    אחד: "שליחת הסיכום ללקוח". לשיחה, קבוצה ועוזר — אין כפתורי אישור/שליחה.
25. "אישור ושליחה" / "שליחת הסיכום" פותחים פופאפ **"למי לשלוח את הסיכום"**: אנשי הקשר של
    החברה (שם + תפקיד) עם תיבות סימון, אף אחד לא מסומן מראש, הכפתור פעיל רק אחרי סימון.
    סגירה: ✕ · Escape · לחיצה ברקע. אין `alert`/`confirm`.

**ז · כללי**

26. אין `<select>` נייטיב בשום מקום במסך. אין "HK" בשום טקסט שבעל העסק יכול לראות (המסך
    הזה של היועץ, אבל הסיכום שנשלח — לא).
27. מנהל התזרים רואה **בדיוק** את אותו מסך כמו היועץ (`docs/TASK_RAILS.md`).



---

# ה · איך מסיימים

1. פותחים את `docs/adv3/meetings.html` ואת המסך שנבנה **זה לצד זה** ועוברים על 28 הסעיפים
   בסעיף ד אחד-אחד. לכל סעיף כותבים עובר / נכשל. סעיף שלא נבדק מול הקובץ — לא נסגר.
2. עוברים גם על 7 קריטריוני הנתונים בסוף סעיף ג.
3. **לא מסמנים Done.** כותבים במשימה תגובה "מוכן לבדיקה" עם הרשימה. אנחנו בודקים בסטייג׳ינג,
   כותבים כאן סעיף "סבב N" עם מה שנשאר, ורק כשהכל עובר — Done.
