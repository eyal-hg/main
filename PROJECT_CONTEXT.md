# PROJECT_CONTEXT — HK mainScreen

*עודכן: 21.08.2026 · הקובץ הזה מתוחזק אוטומטית — כל שינוי משמעותי מעדכן אותו.*

---

## 1. מטרת הפרויקט

פרוטוטייפ חי (דמו אינטראקטיבי) של מערכת HK (חזות קריספין) — פלטפורמת ניהול תזרים וייעוץ עסקי. משמש לאפיון מול מפתחים ולהדגמות. **לא פרודקשן** — כל הנתונים מזויפים, הכל רץ מ-`file://` בלי שרת.

**עובדה תפעולית (קריטי):** **כל החברות מתופעלות כל יום** — זה המוצר. אין "חברה שלא תופעלה היום", ולכן מונים יומיים (ימי המתנה, תזכורות) הם נתון אמין ולא ארטיפקט.

**המודל העסקי (קריטי להבנה):** היועצים העסקיים הם *הלקוחות* של HK, לא עובדים. הם מקבלים: (1) את הממשק (יומן, הקלטות, זיכרון, דוחות), (2) את "העבודה השחורה" — מנהלי התזרים של HK מתפעלים את לקוחותיהם, (3) חלקם מקיימים פגישות חודשיות עבור לקוחות HK ("לקוח HK · הכל הוכן עבורך"). כל הפגישות מתקיימות תמיד ע"י היועץ — אין "יועץ HK שמקיים פגישות".

**הליבה המוצרית:** כל אינטראקציה מוקלטת (פגישה / שיחת SIM / קבוצת וואטסאפ) → תמלול → סיכום + משימות + עדכוני זיכרון אוטומטיים → הזיכרון מייצר את ההכנה לפגישה הבאה ואת ההתאמה של הצ'אט. פגישה לא מוקלטת = חור בזיכרון.

## 2. ארכיטקטורה

Vanilla JS/CSS, סקריפטים קלאסיים בסקופ גלובלי, בלי build. סדר טעינה: `data → core → board → screens → ops → queue → alerts → topbar → meetings → client → memory → cosettings → admin → flow → how → advhome → recbar → main` (main.js אחרון).

| קובץ | תפקיד |
|---|---|
| `index.html` | כל ה-views, המודלים (`memOv`, `meetNewOv`, `recSrcOv`, `mtkOv`, `evOv`…), הברים (`preMeetBar`, `mrecBar`, `mrecTrPop`), `#adminShell` |
| `js/data.js` | `CLIENTS` (price, mgr, coopDays, spark, hl…) |
| `js/screens.js` | ניווט: `setRole`, `selectPortfolio/Client`, `showTab`, `gnavGo`, rail גלובלי + rail חברה, `renderClientsView` (כרטיסי יועץ) |
| `js/alerts.js` | דשבורד היועץ: `renderAdvisorHome`, `advTopCards` (4 קוביות + `ADV_RADAR`), הפיד המאוחד `FEED`, `advPop` (status/risk/tasks/mem/radar), יומן `ADV_AGENDA`+`ADV_TODO`+`ADV_DOFF`, `ADV_PREPS` |
| `js/queue.js` | מסך מנהל תזרים: יומן `renderMgrCal` + `MGR_TODO`, מודל משימה `mtkOv` (משותף — `window._mtkTarget='adv'`), מודל אירוע `evQuick`, רדאר נטישה `cRad`, הודעות `msgCol` |
| `js/ops.js` | תור התפעול: שלבים `OPS_STAGES`, קיטלוג AI, מוטבים, נגררות/לא-צפויות (`dir` inc/exp, גבייה מלקוחות, בדיקות התאמה), הודעות לקוח (`msgStage` — אירוע = הודעה אחת; מצב "הלקוח ענה לשאלה שלנו" `reply/orig/ours`; בלי כפתור "הבא"; "לא רלוונטי" סוגר בלי פופאפ תגים). **קבוצת הוואטסאפ משמשת כרגע רק לזיכרון לקוח** — לא מזינה את התפעול; הצ׳יפ `grp` נשאר בקוד אך כבוי בדמו |
| `js/meetings.js` | הכל על פגישות: `MEETINGS`, `renderMeetings` (2 עמודות), `openMeeting` (עמוד + 5 טאבים), הקלטה (`startMeetRec` בר גלובלי, `showPreMeetBar`, `recSrcOv`), `renderMeetsArena` (זירה גלובלית), `renderPrepView` (הכנה), `meetNew`, `whoPicker` |
| `js/memory.js` | זיכרון: `MEM_CATS` (קטגוריות+פרומפטים), `MEM_USERS/MEM_UDATA/MEM_NUMS`, `MEM_UPDATES` (דלתאות), `openMemCard`/`renderMemCard` (+`MEM_HOST` לרנדור בטאב), `renderMemAdminScreen` (ניהול באדמין) |
| `js/cosettings.js` | הגדרות חברה — 5 טאבים (פרטי לקוח/אנשי קשר/תפעולי/פיננסים/זיכרון) |
| `js/admin.js` | איזור אדמין (`openAdmin`, rail, iframes של `adminScreens/*?embed=1`, panel זיכרון) |
| `css/theme-hk.css` | **append-only override** — נטען אחרון, כל תיקון עיצוב מתווסף בסופו |
| `past-cashflow.html`, `budget-flow.html`, `monthly-goal-index.html` | דוחות תזרים עצמאיים, מוטמעים ב-iframe עם `?embed=1` |
| `meetings-screen.html` | **מסך הפגישות המעוצב** (עוצב מאפס, מוטמע ב-iframe בטאב "פגישות"): 2/3 הכנה + 1/3 רשימה עם טאבים (קרובות/לטיפול/הסתיימו/הכל), פס עליון שמתריע כשאין פגישה קרובה (`?empty=1`), ומצב סיכום פגישה שהסתיימה (`?done=1`). `meetings-screen-v1.html` = גרסה קודמת שנשמרה |
| `flow-changes.html` | **לוג שינויים בתזרים** ("מה השתנה") — עונה על "למה יש חריגה שלא הייתה אתמול": דלתא מוסברת + גרף אתמול-מול-היום + סיבות מדורגות לפי השפעה. מוטמע ב-iframe, פריט סרגל "מה השתנה", וכפתור "למה?" בהתראת החריגה |
| `meetings-brief.md` | אפיון מחזור חיי הפגישה למפתחים |
| `docs/ADVISORS_FEATURES.md` + `docs/screenshots/` | מסמך הפיצ'רים ליועצים (לדף נחיתה) |
| `docs/FLOW_CHANGE_LOG_SPEC.md` | **אפיון לוג השינויים בתזרים** — הצנרת, האלגוריתם, מבנה הטבלאות, סדר הבנייה, שאלות פתוחות |


### מסכים עצמאיים המוטמעים ב-iframe

חמישה מסכים חיים כקבצי HTML עצמאיים ונטענים לתוך `index.html` כ-iframe עם `?embed=1`.
הקובץ מוסיף `body.is-embedded` שמסתיר את המעטפת שלו (רייל, טופבר) ומדווח גובה להורה
ב-`postMessage`. ההורה מאזין ב-`js/board.js` ומרחיב את ה-iframe.

| קובץ | איפה באפליקציה | iframe | הודעה |
|---|---|---|---|
| `accounting-plan.html` | טאב "תכנון חשבונאי" | `#acctFrame` | `hkEmbed` |
| `meeting-room.html` | טאב "פגישות" בכרטיס החברה | `#meetFrame` | `hkMeet` |
| `meetings-arena.html` | "פגישות ושיחות" במבט-על | `#arenaFrame` | `hkArena` |
| `clients.html` | "לקוחות" במבט-על של היועץ | `#clFrame` | `hkCl` · `hkClOpen` |
| `ai-assistant.html` | טאב "עוזר AI" | `#aiFrame` | — (גובה קבוע) |
| `client-home.html` | הבית של בעל העסק | `#homeFrame` | `hkHome` |

`ai-assistant.html` הוא היחיד עם **גובה קבוע וגלילה פנימית** (`height:calc(100vh - 190px)`),
כי הריל הימני ותיבת הקלט שלו דביקים — באוטו-גובה הדביקות מתה.
`clients.html` שולח `hkClOpen` בלחיצה על "פתיחת החברה", וההורה מתרגם לשם החברה ומנווט
ל-`selectClient(ix)` + `showTab('dash')`.

### קיר האש של בעל העסק

`ROLE` הוא `client1` (חברה אחת) או `clientN` (רב-חברות). לבעל העסק אסור לראות
דבר מהעולם הפנימי, וזה נאכף בשלוש רמות:

1. **ניתוב** — `setScope('portfolio')` מוחזר ל-`selectClient` כש-`ROLE==='client1'`.
   טאב "פגישות" מרנדר `js/climeet.js` במקום לטעון את `meeting-room.html`.
   הסרגל בלי `acct` ו-`entries`. `showPreMeetBar` ו-`recStart` יוצאים מיד.
   `setRole` סוגר פס טרום-פגישה ופס הקלטה שנשארו פתוחים מהתפקיד הקודם.
2. **פרמטר ב-iframe** — `budget-flow.html` נטען עם `&r=cli` ומכבה סיווג פערים,
   "+ שורה תקציבית", "הגדרות" ואת בלוק ההמלצה שנשען על זיכרון הלקוח.
   `client-home.html` נטען עם `&m=one` או `&m=group`.
3. **שפה** — מול בעל העסק זה "HK", לא "המתפעל"; אין "הלקוח" בגוף שלישי;
   אין תגיות ראוטינג ("← זיכרון") על ההודעות שלו; במסך ההודעות הוא רואה
   את הקבוצה בלבד (השיחות הפרטיות הן של מנהל התזרים מול כל עובד).

**מה שנשאר פתוח:** `clientN` בתוך חברה ספציפית עדיין מקבל את לוח הווידג'טים
הישן (הנתונים שלו, נוקו מדליפות) ולא את `client-home.html`, כי הקובץ נושא
דאטה של אנרגי אינטרנשיונל בלבד.

**חשוב:** מסך שחי ב-iframe — הרנדרר הישן שלו ב-JS חייב להיות מנוטרל בענף של `setScope`,
אחרת הוא דורס את ה-iframe בכל רנדר. שני מקרים כאלה כבר קרו (`renderMeetsArena`,
`renderClientsView`) — הענף היום ריק עם הערה.

## 3. החלטות מרכזיות + נימוקים

**זיכרון:**
- מסמך חי פר קטגוריה — **מתעדכן, לא נערם**; כל עדכון = גרסה בהיסטוריה.
- **אין עריכה ידנית ואין אישור ידני** לעדכוני זיכרון — הכל נכנס אוטומטית מה-flow. רגישות = בערוץ אחר: קטגוריות `internal:true` (שביעות רצון, מצב אישי) לא נשלחות לצ'אט — רק "הנחיות מזוקקות" + guard (הפרדה ארכיטקטונית, לא אמון בפרומפט).
- מספרים ≠ זיכרון: snapshot נשלף בזמן שליחה לצ'אט, לא נשמר (מקור אמת אחד). בכרטיס היועץ מוצגים; בטאב ההגדרות — רק שורת שקיפות "מה נשלח לצ'אט".
- תפקידים על קטגוריות user בלבד (company = תמיד כולם): Super Admin / יועץ HK / נציג HK / אדמין יועץ / נציג יועץ / לקוח. מציגים רק חריגים (לא "כל היוזרים" על כל שורה).
- ניהול קטגוריות = מסך מלא באדמין (לא פופאפ) — **ליועץ אין גישה אליו**.

**פגישות:**
- הכנה **נגזרת מהזיכרון בזמן אמת, לא נשמרת** (בוטל מנגנון "הכנה שנבנית בסוף פגישה מפגישות קודמות" — מקור חלקי, תזמון שגוי, אחסון כפול).
- משתתפים מהחברה: עד 2, **חובה לשייך לפני אישור סיכום** (הזיכרון פר-יוזר). בורר מערכתי עם "+ הוספת איש קשר" (לא select של דפדפן!).
- אישור סיכום → נשלח ללקוח **בוואטסאפ** + נוצרות המשימות שאושרו; אחרי אישור: ייצוא PDF / שליחה חוזרת.
- הלקוח רואה רק פגישות שאושרו, ורק סיכום+משימות (קריאה); תמלול+משוב+עדכוני זיכרון — רק למקיים ולאדמין.
- הקלטה = **בר צף גלובלי**, לא מסך (ממשיכים לעבוד בממשק): טיימר, השהיה, ⚑ רגע חשוב, פאנל תמלול חי לפי דרישה, הגנת beforeunload, סיום עם hkConfirm. הברים דוחפים את המסך (body.bar-on padding, topbar top:48px) — לא מכסים.
- בר טרום-פגישה 5 דק' לפני: זום/הקלטה/"הפגישה לא תתקיים".
- רשימת פגישות בחברה: שתי עמודות — פתוחות (ימין) / הסתיימו ואושרו (שמאל); בלי פילטרים.
- מקצב פגישות לפי מוצר — רק אצל מנהל (Money+ וכו' לא מעניין את היועץ).

**דשבורד יועץ:**
- עיקרון "רק חריגים, בלי כפילויות": פאנל אחד מאוחד "דורש את תשומת הלב שלך" (מספרים+זיכרון), בלי פס סטטיסטיקה בזירה, בלי טבלת מקצב אצל יועץ.
- **מלחמה בעומס כפתורים**: שורות לוחיצות במקום כפתורים (פיד, משימות); כפתורי פעולה נחשפים ב-hover בלבד (CSS גורף ב-theme-hk); בלי "התחל פגישה" גנרי.
- משימות לא יושבות בציר היומן — פאנל "המשימות שלי" נפרד.

**נגררות/גבייה (מנהל):** פיצול לפי כיוון — הוצאה (סף/ימים/הודעה חוזרת + בדיקת אשראי 30 יום אחורה, מוצגת אוטומטית בפתיחה) מול הכנסה = "גבייה מלקוחות" (סף/איחור/תדירות תזכורת — **בלי** אסקלציה ליועץ). כפתור מאוחד "היסטוריה ובדיקת התאמה" — ותוצאת הבדיקה **מופרדת לפי מקור, שורה למקור**: בנגררת מחפשים אם הפעולה כן קרתה — **בנק** / **אשראי** (30 יום אחורה); בלא-צפויה מחפשים אם היא כן נצפתה — **הצפי בתזרים** (30 יום קדימה). מקור עם התאמה = שורה ירוקה עם הפעולה וכפתור הפעולה שלה ("סימון כשולם באשראי"); בלי התאמה — "לא נמצאה תנועה תואמת". "אירועים" ולא "הודעות"; הערת סף-מחיקה רק בלא-צפויות.

**נגררות ולא צפויות — מסך מידע, לא מסך פעולה:** **ההתאמות נעשות רק בביזיבוקס.** HK לא מתאימה, לא סוגרת ולא משנה תנועות — היא **מראה מה מצאה**: המלצת התאמה כהערה על השורה ("↔ נראה כמו…"), תוצאת בדיקת האשראי (30 יום אחורה), ותנועות הבנק של אותו מוטב. כפתור **"פתיחת ביזיבוקס בטאב חדש"** בראש השלב. שורה נושאת **צ'יפ חשבון**, ושם וסכום במקום טקסט גנרי (הסכום בולט כי הוא מפתח הזיהוי; ירוק=הכנסה).

- **הפעולות היחידות:** "היסטוריה ובדיקת התאמה" (מידע) · **מחיקה בלא-צפויות** (תנועה שקיימת ואפשר להוציא מהתזרים) · **התעלם בנגררות** (אין מה למחוק בצפי — רק להוריד מהרשימה) · ובהכנסה גם "תזכורת גבייה". שניהם עוברים באותו פופאפ **תג סיבה חובה, בלי טקסט חופשי** (סיבה חופשית לא ניתנת לניתוח); התגים מנוהלים ונשמרים.
- **הודעות ללקוח = תבניות WhatsApp Business** (`WA_TPL`): טקסט לא ניתן לעריכה, שני כפתורי תשובה מהירה, תצוגה מקדימה כבועת וואטסאפ. תשובת הלקוח חוזרת דרך הבוט לתור.
- **מה נפסל בדרך (לא לחזור):** התאמה ידנית בצ'קבוקסים · גייט/פילטר חשבונות · עמעום שורות מחשבון אחר · כפתורי "אישור ההתאמה"/"סימון כשולם באשראי" · דחייה (מחר/תאריך/התעלם) · קישור טקסט מהקבוצה לפעולה (`promise`) · שורת "בנק" בבדיקת ההתאמה (כפלה את תנועות הבנק שמתחתיה) · "שליחת הודעה" ידנית · תיוג "מהותיות" ידני. **כרטיסי אשראי אינם חשבונות כאן** — הכרטיס הוא מקור בדיקה, לא מקור שורות.

**הודעות ללקוח = תבניות WhatsApp Business (`WA_TPL`, `smOpenTpl`):** **הטקסט אינו ניתן לעריכה** — תבנית מאושרת מראש מול Meta עם משתנים שממולאים אוטומטית, **ושני כפתורי תשובה מהירה**. הפופאפ מציג מזהה תבנית, תג "תבנית מאושרת", ותצוגה מקדימה בסגנון בועת וואטסאפ (קריאה בלבד) כולל הכפתורים כפי שהלקוח יראה אותם. תשובת הלקוח על כפתור חוזרת דרך הבוט אל תור התפעול. תבניות: `collection_reminder_he` (שולם—אשלח אסמכתא / יש עיכוב), `budget_over_he`, `material_request_he`. **"לא רלוונטי" הפך ל"מחיקה"** בשני הצדדים, עם פופאפ **תג סיבה חובה ובלי טקסט חופשי** (סיבה חופשית לא ניתנת לניתוח). התגים **מנוהלים** — הוספה/הסרה מהרשימה, נשמרים ב-localStorage — כדי שאפשר יהיה לשאול בהמשך "למה נמחקות פעולות אצל הלקוח הזה". `NR_TAGS_DEF`/`openNR`/`nrTagAdd` ב-ops.js. *(מנגנון ה-snooze של נגררת — "מוסתרת להיום, חוזרת מחר" — בוטל יחד עם השינוי.)* **כרטיסי אשראי אינם חשבונות כאן** — עסקה בכרטיס אינה תנועה בבנק; הכרטיס משמש כמקור בדיקה בהתאמה (30 יום אחורה). **אין קישור בין טקסט מהקבוצה לפעולה** — מנגנון ה-`promise` ("הלקוח כתב שההוראה יורדת מחר" צמוד לנגררת) הוסר; לא נבנה חיבור כזה. `ACCTS`/`acctChip`/`cuAcctSet`/`muSel`/`muApply` ב-ops.js.

**קבוצת הוואטסאפ (לקוח+יועץ+מנהל+בוט):** הקבוצה = ה-API אל הלקוח. הבוט הוא **ראוטר**: תפעולי (קובץ פיננסי / תשובה לבקשה / התחייבות תזרימית / שאלה תפעולית) → משימות בתור המנהל עם תג "מהקבוצה" + "הקשר מהקבוצה"; ייעוצי/רך → זיכרון בשקט; רעש → כלום. בספק → זיכרון (לא מציפים את המנהל). דפי בנק נשאבים אוטומטית — בקשות הן נקודתיות, נוצרות אוטומטית כשחסר מסמך (`OPS_REQS`), הבוט מפרסם ועוקב. **אין מסך צ'אט בממשק.**

**מסך הודעות (פר חברה, פריט "הודעות" בסרגל):** רשימת שיחות מימין + שיחה משמאל. ערוצים: **הקבוצה** + **פרטי מנהל↔משתמש** (דרך הסים של HK — עובר את אותו ראוטינג). **בלי צ'אטי AI** — לא מעניינים את המנהל. בשיחות: תגי ראוטינג על הודעות ("→ משימה/זיכרון/נגררת/טבלת הזנה", "בקשה פתוחה"), חיפוש, כתיבה (בקבוצה — בשם עצמו). הרשאות: הפרטי גלוי למנהל ולאדמין בלבד; היועץ רואה רק את הקבוצה. כלל: שיחה בממשק רק כפריט עבודה או כהיסטוריה יזומה — המונה סופר תפעוליות בלבד. `MSGS_THREADS`/`renderMsgsView` ב-ops.js, view `#viewMsgs`. (רעיון "ארכיון קבוצה במודל" נבלע במסך הזה.) **הדראואר הישן (openChat) הוצא משימוש** — כל הכניסות (chatFrom/openChatFor/opsQueueMsg) מפנות למסך. הסרגל צומצם: הכנה לפגישה ירדה ממנו (נגישה מהפגישות), פגישות בקבוצה הראשית, בלי כותרות ליווי/החברה.

**ניהול חומר (הגלגול של טבלאות ההזנה — מודל של אייל, אוג׳ 2026):** טאבים **הכנסות / הוצאות**. **הכנסות = חלון על קטגוריות התזרים** (בד"כ "הכנסה ממכירות") עם **סנכרון דו-כיווני**: מה שצבוע בקטגוריות נשלף למסך, מה שמוזן נצבע לתזרים; **שינוי תאריך כאן משנה את התנועה בתזרים באותו רגע** ונרשם בלוג כ"תאריך זז". **כל פריט = תנועה בביזיבוקס (trans_id)** — עותק אחד של האמת, המזהה בטולטיפ. שני מקורות: **אוטומציה מתוכנת ניהול** (חשבוניות שהוצאו — נכנסות ישר לתזרים) / **הזנה ידנית של הלקוח** (ממתין לאישור). פאנל צד "השפעה על התזרים": בתזרים · ללא מועד—לא בתזרים · באיחור—בגבייה · ממתין לאישור. **שורות ללא מועד** — קומפקטיות בתחתית הטבלה (סקשן נפוח נפסל: "זה ניהול ספקים ולקוחות, יהיו הרבה") + **פילטר ייעודי**; המלצת מועד ✦ **מתנאי התשלום של המוטב** (שוטף+30 וכו׳), אימוץ בלחיצה או "מועד מהלקוח" (בוט). **תנאי תשלום מנוהלים לפי מוטב, לא לפי תיאור** — ספר ספקים/לקוחות פר חברה (שם+ח.פ., כינויים בבנק, תנאי תשלום, קטגוריית ברירת מחדל) שמזין המלצות/התאמה/קיטלוג/גבייה — טרם נבנה. הטבלה **אינה חודשית** — התאריך קובע, כותרות-חודש ויזואליות, שיקים דחויים לחודשים קדימה. עריכת שורה: ✎ בהאבר, טופס במקום, עריכת מנהל סמכותית (נצבעת מיד, "עודכן ישן←חדש"). **סטטוס=פעולה בעמודה אחת** (אישור/צ'יפ איחור לחיץ/צ'יפ שקט). תג מקור על שיקים: ידני/אוטומציה. פילטר "לאישור" ראשון. באנר הגבייה הוסר. שלב 4 בתפעול: פס "ניהול קטגוריות ←" שקופץ לפופאפ במעקב ופערים.

**סרגל הניווט + מצב החברה (אוג׳ 2026):** בסרגל ישבו שני סוגי דברים באותה רמה — יעדי ניווט למעלה ורשימת חברות מתחת, כשהמפריד ביניהם היה `height:0`. **הרשימה היא לא "לקוחות" אלא תור התפעול של היום**, ולכן קיבלה תיבה משלה: כותרת + מונה חי, גלילה פנימית, ומי שהושלם נאסף ל"✓ הושלמו (N)" מקופל — הסרגל מתקצר ככל שהיום מתקדם. **מודל מצב אחד — `coState(c)`** (`js/data.js`): `פעיל · בהקמה · ארכיון`, נגזר מ-`advStatus`. השדות `status` ו-`stat` היו **שדות מתים** (הכל 'active') ואינם בשימוש. **בהקמה = אין עדיין הרשאות בנק ⇒ לא בתור ולא במדדי היום** (זה היה הבאג האמיתי, לא הארכיון). **ארכיון לא מופיע בשום מקום** — לא בתור, לא במחליף החברות בטופבר, לא במוני "חברות במעקב"; נשלף רק בבחירה מפורשת דרך מתג המצבים בתחתית תיבת התור (`GN_QST`) או הפילטר במסך לקוחות (`CLV_ST`). כרטיס ארכיון מציג "בארכיון מאז · סיבה" ומחליף פעולות ל"החזרה לפעילות". **דמו בקנה מידה אמיתי:** ~100 חברות נוצרות דטרמיניסטית ב-`seedPortfolio()` (בלי Math.random, כדי שהצילומים יציבים) — 80 פעילות · 8 בהקמה · 14 ארכיון, ו-49 כבר תופעלו היום.

**שלב "הודעות לקוח" — שיחה קבועה משמאל, חלון עבודה מתחלף מימין (`msgStage`):** בלי פופאפ. **משמאל (`inline-end`) ההתכתבות עם הלקוח, קבועה** — כל ההודעות והמסמכים בסדר כרונולוגי, בועות בסגנון וואטסאפ (הלקוח מימין, מנהל התזרים משמאל), צ'יפ קובץ מצורף לכל מסמך, ו"שאלה — דורשת מענה" להודעת מלל; הפריט הנבחר ממוסגר. **מימין (`inline-start`) חלון העבודה של הפריט הנבחר** — טופס ההזנה + המסמך למסמכים, שרשור ומענה להודעות — עם מונה "N מתוך M" וכפתור "הבא". לחיצה על פריט בשיחה מחליפה את החלון (`msgPick`/`msgNext`). ההתכתבות לא נעלמת בין פריט לפריט. **המחלקות בקידומת `msgc-`** — `mc-*` כבר תפוס ביומן המנהל ב-portfolio.css וגרם לבועות להיערם לרוחב.

**כפתור "סיום תפעול" בבאנר** (`#opsEndBtn` → `finishOps()`) — הפעולה הראשית, לבן על הרקע האדום, לפני "רענון נתונים" ו"השהיה". *הכפתור היה קיים ב-HTML אבל `enterOps` דרס את התווית שלו ל"רענון נתונים" בכל מקרה, ולכן מעולם לא הופיע.* כשהתפעול כבר הושלם התווית הופכת ל"סיכום התפעול".

**רענון הנתונים הוא השער למסך הסיום** — גם כשהדמו מדלג על השלבים. `enterOps` בדילוג קורא ל-`finishOps()` ולא ל-`finishOps2()`, כך שעוברים דרך פופאפ הרענון (סריקת כפילויות בתזרים ובאשראי) שחוסם עד שכל החשדות טופלו. אייקון הסטטוס במסך הסיום הוצמד לכותרת (`.fin-tx`) — קודם הוא צף לבד באמצע הכרטיס ונקרא כקישוט מסתורי.

**חריגות בסיום התפעול — ארבעה מצבים על שני צירים (`FIN_EXC` ב-ops.js):** **מתי** — `בפועל` (החשבון כבר במינוס, אדום) מול `צפויה` (התחזית נכנסת למינוס, כתום). **פתרון** — `יש` (יתרה חיובית בחשבון אחר שמכסה, נכתב בירוק) מול `אין` ("אין פתרון בחשבונות — דורש החלטה", אדום). ארבעת הצירופים: בפועל+פתרון · בפועל+אין · צפויה+פתרון · צפויה+אין. **HK מדווחת, לא מבצעת** — אין כפתורי "ביצוע העברה" או "תזמון"; שתי הפעולות היחידות הן **"הודעה ללקוח"** ו**"התעלם"** (עם ביטול). **חריגה שלא דווחה חוסמת את שליחת התזרים** — כפתור השליחה מנוטרל עם הסבר. פריסה: **שני צדדים** — חריגות מימין, פאנל השליחה משמאל (`.fin-2col`). *טרם מוכן ולכן לא מוצג: ההסבר מה גרם לחריגה מאתמול.*

**דגלי הדמו בתפעול (`js/ops.js`) — שלושה, מדורגים:** `OPS_PREDONE` = סוגי המשימות שמסומנות כטופלו מראש (קובע על איזה שלב נוחתים). `OPS_SKIP_STAGES` = דילוג על כל שלבי התפעול ושער הכפילויות. `OPS_SKIP_CHECKS` = דילוג גם על חמשת שלבי הבדיקה — `finishOps2` קופץ ישר ל-`finAllDone`. **כרגע שלושתם דולקים ⇒ הכניסה לתפעול נוחתת ישר על מסך "התפעול הסתיים"** (עם משך פלוסיבילי כדי שלא יוצג 0:00). לחזרה לזרימה המלאה: `OPS_SKIP_STAGES=false`, `OPS_SKIP_CHECKS=false`, ולצמצם את `OPS_PREDONE`.

**"מחק הכל" — רק בעמודת "לא צפויות":** מחיקה מרוכזת (`nrAll`) שפותחת את אותו פופאפ תגי הסיבה של המחיקה הבודדת ורושמת את התג שנבחר על כל השורות הפתוחות. אותה דיסציפלינה — **אי אפשר למחוק בכמות בלי סיבה**. הכפתור לא קיים ב"נגררות" (שם אין מחיקה אלא התעלמות, ומחיקה בודדת בלבד).

**המודל הנכון של התכנון החשבונאי — התיקון המהותי:** **השורות זהות בשתי התצוגות.** הן תמיד **הסעיפים החשבונאיים**, ומה שמתחלף הוא **המספר** — "כאן שיווק ומכירה חשבונאית, וכאן אותו סעיף תזרימית". *זה הוואו: אותן קטגוריות, שתי שפות.* (טעות קודמת: הצגתי שתי רשימות קטגוריות שונות — ומשם נבע גם "פתאום נהיה ארוך".)

**עריכה — עורך דו-שדות בתא:** לחיצה על תא פותחת עורך עם **יעד** ו**בפועל** יחד (`openCell`), כי בחודש הנוכחי ובחודש האחרון צריך לעדכן את שניהם. בעתיד — יעד בלבד; בתצוגה התזרימית הבפועל נעול (מגיע מהבנק). ↺ מחזיר לברירת המחדל. **תא תכנון-מול-ביצוע**: הביצוע גדול, ומתחתיו רק **הסטייה** עם חץ (▲/▼) — התכנון המלא בטולטיפ, כי שלושה מספרים בשורה אחת היו עמוסים מדי. **בורר חודשים**: 12 חודשים בזיכרון וחלון של 5 שמוזז בחצים (`slide`/`WIN`), עם `PAST()` דינמי. **הפרדת הגוונים מוגברת** — רקע, כותרות קבוצה, עמודת השם, עמודת החודש הנוכחי ושורת הרווח כולם נגזרים מ-`--tone`, כך ששתי השפות נקראות במבט אחד. רווח אנכי בין שורת הסה״כ לקבוצה הבאה.

**מצב שלישי — "מאוחד" (לצפייה בלבד):** משווה את ה**בפועל** התזרימי מול החשבונאי באותה שורה. הפער הוא הגיבור (כתום כשהוא מהותי — מעל 5% ומעל 500 ₪), ומתחתיו שני המספרים בצבעי השפות. שורות הסה״כ ושורת הרווח מציגות את הפער ולא סכום. הגוון ניטרלי אפור בכוונה — זו תצוגת השוואה, לא שפה שלישית. **אין עריכה במצב הזה.**

לכל **סעיף × חודש** יש **שני מספרים תמיד**: **יעד** ו**בפועל**. חודש סגור מציג בפועל גדול + "תכנון X" קטן + הסטייה (ירוק/אדום); חודש פתוח מציג יעד בלבד. המתג תזרימי/חשבונאי מחליף איזה **זוג** רואים:

| | יעד | בפועל |
|---|---|---|
| **תזרימי** | נערך | מהבנק — לא נערך |
| **חשבונאי** | נערך | **מהנה"ח — היועץ מזין כל חודש** |

היעד החשבונאי אינו רק השתקפות של התזרימי (פחת, הפרשות, הפרשי עיתוי) — המיפוי נותן ברירת מחדל וניתן לדרוס. מה שלא משויך לאף סעיף מופיע כשורת "לא משויך לסעיף" בתצוגה התזרימית, כדי שהסה״כ יישאר התזרים האמיתי. **המיפוי הוא הגדרה** ונפתח מכפתור ⚙ בכותרת — לא לשונית באמצע המסך.

**דריל-דאון ומלוא יכולות התכנון (`accounting-plan.html`, סבב 3):** נבחר מתוך שלושה כיוונים — *חשיפה הדרגתית · פאנל בורר · שורה שנפתחת* — והבסיס הוא **שורה שנפתחת**, היחיד שבו הדריל-דאון באמת קריא בשלוש רמות. **ההיררכיה: סעיף חשבונאי ← קטגוריית תזרים (עם אחוז המיפוי) ← תיאור.** כל רמה יורשת את אותה גריד חודשים, כך שהעין ממשיכה לקרוא עמודה אחת גם בעומק 3, ובכל רמה יש בפועל, סטייה, ואפשרות להגדיר יעד. **12 היכולות מהמסך הישן** (`monthly-goal-index.html`) קופלו פנימה: עורך יעד עם השוואה חיה לממוצע 3 ח׳ · מתג "פתח יעד ללקוח" (הכנסה=לעבור מעל · הוצאה=לא לעבור) · סימולציית מה-אם · סיווג קבוע/משתנה עם המלצה לפי חזרתיות ומיזוג תיאורים · פירוט 6 חודשי תנועות · הצג/הסתר בפועל ויעד · ארבעה פילטרים · טווח חודשים · טען מחדש · ייצוא · שורות רווח גולמי ותפעולי. **היתרה המתגלגלת** הושתלה מכיוון "חשיפה הדרגתית": שורה תחתונה שמראה איך היעדים מגלגלים את היתרה חודש אחרי חודש, עם **יתרת פתיחה נערכת** שנשמרת.

**"תכנון חשבונאי" (`accounting-plan.html`, טאב `acct`) — מסך חדש, לא נגע ב"תזרים עתידי".** נבחר מתוך שלושה עיצובים עצמאיים + שופט (עוגן / תאומים / צפיפות). **הרעיון שפותר את "פתאום נהיה ארוך ולא רואים את החודשים": הגיאומטריה נעולה** — רוחב עמודות, ראש החודשים, כותרות הקבוצה, שורות הסה״כ ושורת הרווח יושבות באותו מקום בדיוק בשתי התצוגות; מה שמשתנה הוא תוכן השורות והגוון (תכלת=תזרימי, סגול=חשבונאי) דרך טוקן `--tone` אחד. שני הצדדים נכנסים בלי גלילה. **צ'יפי המקורות ירדו** והוחלפו ב"N מקורות" + **עמוד שדרה צבעוני** לכל שורה (הצבע = הסעיף החשבונאי, גרדיאנט = פיצול), **DNA** של הקבוצה בכותרת, ו**רצועת מיפוי קבועה** בתחתית שנחשפת בריחוף — קשר בין השפות בלי לגזול פיקסל. צ'יפ **"זהה בשתי התצוגות"** על סה״כ שמתאים בשתיהן. שתי לשוניות: *התכנון* ו*מיפוי קטגוריות* (סדנת הגרירה/פיצול/הוספת סעיף, לא-ממופות ראשונות).

**ווידג'ט "פערי תקציב" (`widgets/widget-budget-gaps.html`) — החליף את "מה השתנה בתזרים" בלוח החברה.** מבוסס על מודל **מעקב ופערים** אחד לאחד: פער = יעד − בפועל − צבוע בתזרים − שורה תקציבית; המהותיות נגזרת מהסף (רצפה / אחוז / תקרה, נקרא מ-`localStorage.hkGapTh` ומתעדכן ב-postMessage כמו במסך). מציג **בר לכל קטגוריה בשפה הוויזואלית של מעקב ופערים** — מסלול מקווקו כתום = היעד שטרם נסגר, כחול מלא = בפועל, מקווקו תכלת = צבוע בתזרים, ואדום שיוצא מעבר לקצה = מעל היעד; רוחב הבר יחסי לגודל היעד, כך שקטגוריות נמדדות זו מול זו. **שש החריגות הגדולות בלבד, רשימה שטוחה בלי כותרות קבוצה**, ו"ועוד N בפער קטן יותר". חצי רוחב לוח. **סיבת הפער לא מוצגת כאן** — "את היועץ לא מעניין סיבת הפער"; הטקסונומיה (מחכה לחומר / המציאות השתנתה / התזמון זז / טעינו בבנייה) היא כלי עבודה של מנהל התזרים ונשארת במסך מעקב ופערים. גם פילטר סוגי הפער ירד מהווידג'ט. מעליהן פסי הקבוצות (הכנסות · עלות המכר · תפעוליות · מימון) ובורר חודש. לחיצה על "פתיחת מעקב ופערים" שולחת `postMessage({hk:'tab',tab:'budget'})` — הוספתי ל-board.js מטפל שפותח מסך לפי בקשת ווידג'ט.
**טאב "זיכרון לקוחות" ליועץ (`js/how.js`):** תרשים ההסבר שמציגים ליועצים — שישה ערוצים סביב המסך המרכזי (פגישות · שיחות טלפון · קבוצת הוואטסאפ · התזרים · הצ'אט של הלקוח עם ה-AI · מנהל התזרים), כולם מזינים זיכרון אחד. הטקסט מדגיש שיש **סים ייעודי של HK שאפשר לתת ליועצים** — כל שיחה ממנו נכנסת לזיכרון בלי שהיועץ יעשה דבר.

**"התהליך שלי" (`js/flow.js`, טאב `flow`):** שכפול המסך הקיים בשפה שלנו, בלי עיצוב מחדש — **תוכנית ליווי** (GOLD) = רצף 27 מפגשים, כל מפגש נושא רשימת נושאים מתוכננים ונקשר לפגישה אמיתית (תאריך · שעה · יועץ) עם "אישור הסיכום". כותרת + פס "שלב N מתוך 27", כותרת תוכנית עם מונה, מחיקה וקיפול, ＋ צף להוספת מפגש, ולכל שורה ידית גרירה ותפריט ⋮. אישור סיכום מקדם את המונה. *הערות שנרשמו ולא טופלו לבקשת אייל (לבנות כמו שהוא): ההתקדמות נמדדת באישור ולא בקיום המפגש (מפגש שהתקיים = שלב 0); 27 אקורדיונים שטוחים בלי "איפה אני"; אין תאריכים/איחורים; רשימת הנושאים היא תוכנית ולא תוצר; אין חוט לתזרים ולמדדים; עריכה מעורבת בצפייה.*
**עוזר ה-AI של היועץ:** **שאלה ותשובה, בלי קישוטים.** היועץ שואל על **הלקוח**, והעוזר עונה מתוך **זיכרון הלקוח, קבוצת הוואטסאפ, הפגישות והתזרים** — חלון החשיבה מראה מאיפה נשלף. חמישה סבבים: מה בוער לפני הפגישה · מה קרה בקבוצה · מצב המכרז (ולמה הוא לא צבוע בתחזית) · ממה הלקוח היה מתוסכל · איך נכון לדבר איתו. *נפסל: צ'יפי מקורות, תיבות טיפ, דגלי "טרם נענה" וניסוח מוצע בתיבה — "הגזמת, רק שאלה ותשובה".*

**ההקלטה לא חוטפת את המסך — פס גלובלי + תצוגת מגיש (`js/recbar.js`, `client-view.html`):** התובנה של אייל — *"אני רוצה שהוא ימשיך לעבוד במערכת ולהראות ללקוח"* ו*"אל תשכח שהוא עושה זום עם הלקוח"*. לכן **המודל הוא תצוגת מגיש כמו ב-Keynote**: **`client-view.html` הוא טאב נפרד שהיועץ משתף בזום** — נקי וממותג קלות, עם תג "השיחה מוקלטת", המסך שהיועץ דוחף (מטרה / תזרים / תקציב), ו**"מה סיכמנו"** שההחלטות נכתבות בו מול הלקוח בזמן אמת. **כלל ברזל בקוד: לטאב הלקוח לא נכנס שום דבר פנימי** — זיכרון, רגישויות, משוב על היועץ ומדדי שימוש נשארים אצל היועץ. **פס ההקלטה** נשאר sticky בראש **כל מסך במערכת** — טיימר, גל קול, "סמן רגע" (רווח), "מסך ללקוח", "לוח המגיש" ו"סיום". **לוח המגיש** נפתח מהפס: איזה מסך הלקוח רואה · שדה להוספת החלטה שמופיעה מיד אצלו · והרגעים שסומנו. הסנכרון בין הטאבים דרך `localStorage`. **הרווח:** הלקוח מאשר את הסיכום בזמן אמת, ולכן נעלמת החיכוך של "סיכום ממתין לאישור", וכל מסך שמוצג נרשם כרגע בהקלטה אוטומטית.

**"חדר הפגישה" (`meeting-room.html`, טאב `meetings` דרך iframe):** נבחר מתוך שלושה כיוונים + שופט (חדר הפגישה 50.5 · חוט אחד 47.5 · לולאת ההתמכרות 46). **מסך אחד, שלושה מצבים** של אותה ישות — הכנה ← הקלטה חיה ← תוצרים — במקום ארבע לשוניות. **הבעיה שנפתרה: "אנחנו צריכים שהיועצים יתמכרו להקליט".** שש שכבות ההתמכרות: (1) **הבטחה לפני הלחיצה** — "45 דקות שיחה הופכות לשישה תוצרים" עם רשימה מפורשת; (2) **עזרה בזמן אמת** — לוח חי שעובד רק כשמקליטים: צ'קליסט נקודות פתיחה 2/3 עם הפתוחה בכתום, המטרה נעוצה, "סמן רגע" (רווח); (3) **תמורה שנצברת** — "נאסף עד עכשיו" מטפטף במהלך; (4) **קבלה מיידית** — "הפגישה הזאת ייצרה בשבילך" עם 5 אריחים ופס פייפליין, "מוכן תוך 38 שניות"; (5) **ההפסד גלוי** — רצועת "אם לא היית מקליט" (0/0/—/—/0) ופגישה שלא הוקלטה מסומנת ברשימה; (6) **סגירת הלולאה** — הרגעים שסומנו הם הצ'אפטרים, והזיכרון מזין את ההכנה הבאה. **מטבע ההוכחה:** כל סעיף סיכום, משימה ופריט משוב נושא **חותמת זמן** שקופצת לרגע בהקלטה — ארבע הלשוניות הפכו ל**ארבע עדשות על מקור אחד**. `meetings-screen.html` נשאר בקוד לגלגול אחורה.

**מסך היועץ "הבוקר שלך" (`js/advhome.js`, CSS בקידומת `.advx`)** — נבחר מתוך שלושה עיצובים עצמאיים + שופט (תדריך בוקר 49 · תא טייס 45 · היום כציר 45). **המסך הוא תדריך, לא דשבורד:** כותרת ראשית אחת — הדבר שנולד היום ("תיכנס לחריגה בעוד 9 ימים") עם תג **"חדש — לא היה אתמול"**; לצידה **גרף המסלול** (התחזית של היום מול המקווקו של אתמול, שתי נקודות החיתוך מתויגות "היום · יום 9" מול "אתמול · יום 12"); ולצידה **שרשרת הסיבתיות** — `09:00 פגישה הוקלטה → 11:05 נולדה ההתראה מהזיכרון → 17:00 כאן נסגרת`, ושורת ה-17:00 ביומן מסומנת באדום. פס עליון טיפוגרפי בלי כרטיסים, שבו הלקוחות מופיעים גם כמספר וגם כ**מפת תאים** שבה הבוערים אדומים ופועמים. הפיד **מקובץ לפי לקוח** עם אווטאר, פילטרים לפי מקור (מהזיכרון/מהתזרים/מהקבוצה) ופעולה אחת לשורה, ובתחתיתו "שאר N הלקוחות — בלי התראות פתוחות היום" (גם השקט הוא מידע). המונים מגיעים מהנתונים האמיתיים (`coState`, `ADV_RISK`, `ADV_MGR_TASKS`, `ADV_RADAR`). הפס החי של הפגישה הבאה הוסר מהעיצוב — לאפליקציה יש באנר טרום-פגישה משלה. **כותרת העמוד הישנה מוסתרת** במסך הזה כדי לא לכפול את "הבוקר שלך".

**משימות היועץ בתיק גדול:** מחולל המשימות פלט שורה לכל חברה, ובתיק של 100 זה התפוצץ ל-50 שורות שרובן זהות ("שליחת זמנים לפגישה חודשית — X"). **כלל: משימה חוזרת היא שורה אחת עם מונה**, לא N שורות — צ'יפ מספר, שורת משנה, chevron לפתיחת רשימת החברות (כל אחת לחיצה ישירה), ו"שליחה לכולם". ארכיון ובהקמה לא מייצרים משימות בכלל, וקוביות היועץ (סטטוס לקוחות / חברות בסיכון) סופרות תיק בלי ארכיון ומציגות את הפילוח פעילים · בהקמה · בארכיון.

**שני סטטוסים נפרדים, לפי מי מחליט:** **סטטוס התקשרות (ארכיון) — ב"פרטי לקוח"**, כי גם היועצים משנים אותו: פס "בהתקשרות / בארכיון" עם העברה לארכיון (אישור דרך `hkConfirm`, לא דיאלוג דפדפן), החזרה בקליק, ושדה סיבת יציאה כשבארכיון. **מצב תפעולי (פעיל / בהקמה) — בטאב "תפעולי", שהוא פנימי ל-HK** ומוסתר ליועצים (`coTabs()` מסנן אותו, ו-COSET_TAB נופל ל-details). חברה בארכיון לא מציגה מצב תפעולי אלא הפניה לפרטי לקוח. הקישור "העברה לארכיון" בכותרת ההגדרות היה טוסט ריק — חובר לפעולה האמיתית ומתחלף ל"החזרה לפעילות". **תג המצב בכותרת המסך הפסיק להיות קשיח** — נגזר מ-`coState` (קודם כל חברה הוצגה "פעיל" בירוק, גם כשהייתה בהקמה).

**ציר הגבייה (D4 — הסינתזה, אוג׳ 2026):** נבחר מתוך שלושה עיצובים עצמאיים + שופט (editorial 47 · datavis 49 · spatial 45). **הציר האנכי הוא קו האפס**: ימינה ממנו יוצא הפער שחסר, שמאלה המסלול הוורוד עם המילוי הירוק — **סקאלה ליניארית אחת משותפת לשני הצדדים** (`--w = ערך/MX`, `--trk:74px`), ולכן הזנב הוורוד הריק זהה באורכו לפס האדום שמעברו. המילוי **מפולח לפי צ'יפים** — כל תקבול הוא מקטע נראה, וזה גם מה שמאפשר 10 תקבולים ביום אחד בלי לנפח את השורה (הרשימה נחתכת ל-5 + "עוד N"). **אפס כרטיסיות** — ההיררכיה חיה במקטע הקו (`.hkt-node::after`: אפור/אדום/ירוק/כתום-נגררת/נייבי-נבחר), והצל היחיד ברכיב הוא על הצ'יפ. צד ימין נושא מתי (יום גדול + יום בשבוע + "בעוד X ימים") **וגם כמה** (נדרש/צפוי + "חסר") — זה מה שהרג את החד-צדדיות שנפסלה. **"נשאר לגבות" הוחלף ב"חסר לכיסוי"** + "כדי שהיתרה בבנק לא תרד למינוס" — "לגבות" מצמצם לגבייה, והכסף יכול להיכנס גם מהלוואה/בעלים/דחיית תשלום. **ללא-תאריך למעלה** ברצועה דביקה (`.hkt-pool`) שהיא גם יעד הפלה לביטול תזמון. **אין פער → לא כותבים כלום** (יום בלי פער מתכווץ, בלי טקסט "אין פער"). עמידה על יום בציר מאירה את שורותיו בטבלה (`matHover` לפי `data-d`, בלי רינדור מחדש). נוספו: ＋ הוספת תקבול (טופס במקום) ותג מקור ידני/אוטומציה בכל שורה. קוד: `renderMatInc()` ב-ops.js, CSS `.hkt*` בסוף theme-hk.css.
**ציר ההוצאות — התמונה הראית של ההכנסות:** אותו רכיב `.hkt`, אותה סקאלה, אותה גרירה. בהכנסות הקו הוא **האפס** והפער יוצא ימינה; בהוצאות הקו הוא **התקרה**, שמאלה נערם מה ששובץ לתשלום (מקטע לכל ספק, חיובי חובה בירוק כהה ונעולים) וימינה יוצאת **החריגה**. **התקרה נגזרת מהתזרים** — `cap(d)=max(0, יתרה צפויה ב-d)`; אין יתרה → "אין יכולת תשלום ביום הזה" וכל מה שנעול הופך אוטומטית לחריגה. לחיצה על צ'יפ = שיבוץ/ביטול לתשלום (`SIM.pay`), גרירה לתאריך אחר = **דחיית תשלום** (`SIM.exp`) — הכל בסימולציה עד "שמירה לביזיבוקס".
**קוהרנטיות התזרים בשני הצירים:** היתרה הצפויה (`balAt`) היא מקור אמת אחד לשניהם. בהכנסות — מה שכבר בתזרים **מגולם ביתרה השלילית ולכן לא סוגר את החור** (הצ'יפ מוצג מעומעם/מקווקו); את החור סוגר רק מה שהמשתמש שיבץ עכשיו. החוסר שנותר נגזר מהיתרה המסומלצת, ולכן **עודף שנגבה מוקדם סוגר מעצמו חורים מאוחרים** ("נסגר מעודף קודם", מקטע ירוק מקווקו).

- **מה נפסל בציר (לא לחזור):** כרטיסיות פר תאריך · ציר בצד ימין · ציר חד-צדדי (זהות זמן בלבד) · ציר שתופס את כל המסך על חשבון ניהול הגבייה · הצגת תאריכים שאין בהם חוסר בלי ערך מוסף · סרגל מספרי + קווי גריד (מיותרים ברוחב 420px) · טאב/כרטיסייה נפרדת לציר.

**ניהול קטגוריות (פופאפ במעקב ופערים, כפתור ⚙ הגדרות — ישב בהמשך בכמה מסכים, בלי טאב בסרגל):** שתי עמודות. **לא מורשות** = קטגוריה חדשה מהסנכרון, בהסגר עד הכרעה: שורת כותרות רו"ה בלחיצה אחת (הכנסות/עלות המכר/הוצאות תפעוליות/**השקעות ומימון · זכות (ירוק) / חובה (אדום)** — בשורה שנייה) או "קטלוג מחדש בתפעול". **מורשות** מקובצות לפי כותרות רו"ה, עם תגי זכות/חובה במימון, ⇄ העברה בין כותרות (אותה שורת כותרות) ו"→ הוצאה" בהאבר. ההשלכות: לא-מורשות לא בדוח החודשי + תנועותיהן עולות לקיטלוג מחדש; קטגוריה שאושרה = "בלי יעד" עד הבנייה הבאה. **"＋ יצירה ראשונית"** — כפתור אחד + פופאפ אזהרה שדורס את הקיים (תפריט בחירת מקור נפסל). **שורה תקציבית — חלוקה בין חשבונות** באחוזים שסכומם 100 (שורות הדמה נצבעות לחשבון ספציפי בביזיבוקס).

**טבלאות הזנה מנוהלות (במקום גוגל שיט להזנה):** גוגל שיט ירד מזרימת ההזנה בלבד — **המדדים נשארים על גוגל שיט**. הגדרת "חומר" (קריטי): **HK היא תזרים, לא הנה"ח** — חומר = צפי קדימה (תשלומים לספקים שצריך לשלם החודש, תקבולים צפויים מלקוחות, מועדי פירעון לשיקים), לא חשבוניות על העבר (העבר נשאב אוטומטית). התבניות: תשלומים לספקים · צפי / תקבולים מלקוחות · צפי. **הזנה ידנית בלבד** — לא משנה מאיזו דלת (אפליקציה/קישור/בוט), אין תג ערוץ ואין "מי הזין" (נבדק ונפסל — לא מוסיף ערך). **ציר סטטוס אחד**: ⏳ ממתין לאישור → ✓ בתזרים (אושר ונצבע קדימה) → ● בוצע (התאמה אוטומטית מהבנק, סוף חיי השורה); עבר המועד ולא בוצע → ⚠ נגררת (בהוצאות) / **חוב לגבייה** (בהכנסות — פס גבייה + "תזכורת גבייה בשם הלקוח", רעיון של אייל). "עודכן" = הערה בלבד, ומחזירה את השורה ל"ממתין לאישור". אמצעי תשלום: **העברה / שיק** בלבד (שיק ← אסמכתא חובה = מפתח ההתאמה). טאבים: פעילות / בוצעו / הכל; בוצעו נשמרות 90 יום. `DATA_TABLES`/`openDataTable` ב-ops.js, מודל `#dataTblOv`. הקוביות במסך הראשי עודכנו ("הזנות לקוח").

**עקרון תור התפעול (חשוב):** התור מציג **רק דברים שקיימים ודורשים פעולה** — היעדר אינו פריט עבודה. **המתפעל מזין מה שיש**; חומר שלא הגיע אינו חוסם אותו ואינו מוצג בתור (הוסרו פאנל ושורת "בקשות חומר פתוחות"). חומר חסר חי בטבלאות ההזנה בלבד, והבוט מתזכר בקבוצה אוטומטית לפי ההגדרות.

**לוג שינויים בתזרים:** התשובה ל"למה פתאום יש חריגה שאתמול לא הייתה" — הכאב שאייל העלה.

*ארכיטקטורה (נקבע מול הצנרת האמיתית):* הכל מגיע מביזיבוקס. `raw_data` מתעדכן רק כשיש שינוי (hash), וממנו נבנה `process_data` — תמונת התזרים שוטף + 3 חודשים, שממנה ניזונים כל הצרכנים (ווידג'טים, AI, הודעות/התראות). **הלוג נכתב באותה ריצה שבונה את `process_data`, כטבלה אחות append-only — לא כשדה בתוכו** (process_data נדרס בכל בנייה ונקרא בכל בקשה; הלוג חייב להצטבר). זו הנקודה היחידה שבה מחזיקים ביד את ארבעת החלקים — raw ישן/חדש ו-process ישן/חדש — כלומר גם את **הסיבה** וגם את **ההשפעה**.

- **מקור השינויים:** `bizibox_monthly_transaction_snapshots` (פר חברה × חודש × scope, `transactions` jsonb + `transactions_hash`). דיף בין ה-snapshot החדש לקודם לפי **`trans_id`** (id יציב בתוך ה-jsonb): id חדש = נוספה · id שנעלם = נמחקה · אותו id עם `date` שונה = **תאריך זז** · אותו id עם `debit/credit` שונה = שינוי סכום · `category` שונה = קיטלוג. `date` (ms) = ה-value_date, ו-`trans_time_type: past/future` = הציר צפי/בפועל.
- **מבנה שורת change-set:** `run_id`, `company_id`, `ts` · `curve_delta` (השינוי ביתרה הצפויה לכל תאריך בטווח — נשמר דחוס, רק תאריכים שזזו) · `events[]` (הדיף המסווג: trans_id, סוג, value_date לפני/אחרי, סכום לפני/אחרי, delta, תיאור, קטגוריה, חשבון) · `flags` **מחושבים בזמן הכתיבה** (התאריך שהכי הורע, האם נוצרה חריגה שלא הייתה, האירוע הגדול ביותר).
- **החישוב — הפירוק של אייל (תחזית ליום D = יתרה נוכחית + Σ עתידיות עד D):** `Δתחזית(D) = Δיתרה נוכחית + Δתנועות עתידיות עד D`. שני שלבים בלי חפיפה: (1) **Δיתרה נוכחית** — רק תנועות בפועל חדשות מזיזות אותה; כל אחת מסווגת מול הצפי (בוצע / בפועל≠צפי / לא צפויה), ובקרה: Σ החדשות = Δיתרה, שארית → אירוע "סנכרון יתרה". (2) **Δעתיד** — דיף trans_id על צד העתיד: נוסף/נמחק/סכום/תאריך זז; אשראי מצטבר חי כאן (לא נוגע בעו"ש עד יום החיוב). הדלתא היא **עקומה**, לא מספר — לכן הווידג'ט מבקש קודם נקודה על הציר. נגררת = אירוע ממעבר יום: צפי שעבר תאריכו בלי בפועל — לא ביתרה ולא נעלם מהעתיד.
- **שני טריגרים:** שינוי hash (אירועי נתונים) + התחלפות תאריך (אירועי זמן — נגררות שנולדו, חיוב אשראי שהגיע). שניהם כותבים לאותה טבלת change-sets.
- **למה flags בכתיבה:** כך ההתראה היא **אירוע** ולא שאילתה — "נוצרה חריגה ב-24.08" נדלקת ברגע הריצה, לא כשמישהו פותח מסך.
- **קצה שצריך אמירה כנה:** אם תשלום זז אל **מחוץ** לטווח 3 החודשים, העקומה מראה שיפור אבל הכסף לא נעלם — הטקסט חייב לומר "זז ל-15.12, מחוץ לטווח התחזית".
- **פתוח:** במעבר צפי→בפועל — האם ה-`trans_id` נשמר (אז "בפועל≠צפי" נופל מהדיף מיד) או שנולד id חדש (ואז צריך התאמה: סכום קרוב + תאריך קרוב + אותה קטגוריה, אחרת ייספר כשני אירועים).

**סיווג פערים — מסך "מעקב ופערים" (`budget-flow.html`, `gapHtml`; עד אוג׳ 2026 נקרא "תקציב תזרימי" — שוּנה כי "תקציב" נשמע הנה"ח והמסך הוא מעקב יעד-מול-ביצוע ופערים):** לכל פער (`יעד − בפועל − בתזרים`) יש **type**, שעונה "מי חייב לי פעולה?" — לא תיאור אלא בעלוּת. **עקרון היסוד (של אייל): הפער = יעד − בפועל − צבוע, וכל מה שמאמינים בו כבר צבוע.** לכן הפער הוא החלק מהיעד שאין עליו שום ידיעה — המרחק בין התוכנית שנבנתה עם הלקוח בשיחת התחילה לבין האמונה הנוכחית. **"טרם הגיע הזמן" אינו סיבה** (אם זה רק זמן — זה צבוע) והוסר. **את היעד לא מתקנים באמצע חודש** — רוצים לראות איפה טעינו וללמוד; `tgtEdit` נעול למתפעל (toast הסבר), תיקון רק בבניית שלושת החודשים הבאים.

**צפי חומר — נפסל.** נבנה כשכבה צבועה שסוגרת פער עד יום החומר, ואייל הוריד אותו ("לא רוצה את הצפי חומר"). הפער נשאר `יעד − בפועל − צבוע` נקי; הסוג "מחכה לחומר" נשאר כסיבה לפער.

**שורה תקציבית במעקב ופערים:** שורה תקציבית = מנגנון שמפזר את הפער כשורות דמה על תאריכים מוגדרים (הפער ÷ התאריכים) כדי שעקומת התחזית תישאר ריאלית. **הכרעה (עודכנה): שורה תקציבית סוגרת את הפער** — יש שורה תקציבית → **פער 0** ("✓ מכוסה בשורה תקציבית"), ואין צורך בסיווג. `rawGap` = מה שלא נצבע כלל; `gapOf = rawGap − blOf`. *(קודם נקבע ההפך — ששורת דמה אינה ידיעה ולכן אינה סוגרת — ואייל הפך את ההחלטה.)* תצוגה: סגמנט פסים אפור-כחול בבר + "שורה תקציבית X" + תווית "פער בשורה תקציבית" במקום "נותר לצביעה", ומתחתיה **ניהול ה"מפחיד"**: "בעקומה כשורות דמה (5·15·25 בחודש) — נותרו 18 ימים · 1,255 ₪ ליום שצריכים להפוך לאמיתיים". ההבחנה מצפי חומר: צפי חומר = ידיעה שמחכה לפירוט (סוגר פער); שורה תקציבית = תקווה מפוזרת (לא סוגרת). **זמין בכל קטגוריה** (גם בלי פער — להגדרה מראש), הצ'יפ תמיד בשורה נפרדת מתחת לשם, ו**ההגדרה ממשיכה לכל החודשים קדימה** (הסכום מחושב מחדש פר חודש). **שורת סיכום לכל קבוצה** — פס נייבי בתחתית הקבוצה (Σ סה"כ), באותו מבנה עמודות כדי שאפשר לקרוא אנכית. תווית הפער בכל המסך היא **"פער"** (לא "נותר לצביעה בתזרים"). **עריכה מהמסך** (`blOpen`): צ'יפ סגול קומפקטי "שורה תקציבית פעילה ✎" (הטקסט הארוך נפסל) / "＋ שורה תקציבית" ← פופאפ עם **שלושה מצבי פיזור**: כל יום / **פעם בשבוע (עם בחירת יום א׳–ו׳)** / **לפי תאריכים עם משקל באחוזים פר תאריך** — סה"כ חייב 100% (מונה ירוק/אדום, שמירה נעולה עד שמתאזן, "חלוקה שווה" בלחיצה, ליד כל משקל ה-₪ המחושב). **כל יום 1–31** (בורר מהיר + שדה חופשי), **אחוז לכל תאריך שמסתכם ל-100% מהפער** — הפיזור נשמר כ**הגדרה** ולא כסכומים, כי הפער נגזר מחדש בכל חודש. *(נבנו ונפסלו: **קיבוע סכום ב-₪ פר תאריך** עם יתרה שמתחלקת על השאר — "בוא נוותר על קיבוע סכום"; כפתור "חלוקה שווה" — החלוקה קורית אוטומטית בכל שינוי תאריך.)* ביטול כל הימים = ביטול השורה והפער חשוף. פסים **סגולים** (אפור-כחול נטמע ברקע ונפסל).

**גם הכיוון ההפוך מסווג (רעיון של אייל):** עודף מוגזם — בפועל+צבוע עוברים את היעד משמעותית — "מבחינתנו זה לא פער וזה לא פחות חשוב". אותו סף מהותיות בדיוק (`matMag` דו-כיווני, `matNeg`), אותו פופאפ, אותו גייט: חריגה מהותית בלי סוג חוסמת את הבדיקה. ההבדל היחיד: בעודף אין "מחכה לחומר" (הכסף כבר כאן) — רק המציאות השתנתה / התזמון זז (הוקדם) / טעינו בבנייה. כפתור "+ הגדרת החריגה".

ארבעה סוגים. *הפער לא אמיתי*: **מחכה לחומר מהלקוח** (→ תזכורת בוט, מטופל יום-יום — הסיבה החשובה ביותר). *הפער אמיתי — התחזית טעתה, לומדים ולא מתקנים*: **המציאות השתנתה** (העולם זז — לפגישה ולזיכרון; מלמד על הלקוח) · **התזמון זז** (הסה"כ נכון, הפריסה טעתה) · **טעינו בבנייה** (**ברירת המחדל — "בעצם לא יודעים למה"**: אם אין הסבר אחר, ההערכה מהבנייה לא פגשה את המציאות; מגיע מסומן מראש בפופאפ, בלי חובת הסבר). "המציאות השתנתה" ו"התזמון זז" דורשים שורת הסבר — הם טוענים לידיעה; "טעינו בבנייה" לא, כי הוא בדיוק היעדר הידיעה. נפסלו כסוגים: "אצל המתפעל טרם נצבע" (=ההזנה לא הושלמה), "טרם התגבש" (אם זה רק זמן — זה צבוע), "היעד לא מעודכן" (פעולת העריכה מחקה את מדידת הטעות), **"נרשם במקום אחר"** ("אין דבר כזה — אם יהיה, המנהל תזרים יקטלג נכון": קיטלוג שגוי מתוקן בשלבי הקיטלוג, לא מוסבר כפער). תוצר עתידי: **דוח דיוק תחזית** פר חברה/קטגוריה — כמה מהפער היה חומר, תזמון, בנייה.

- **רק המתפעל יודע** אם חסר חומר. המערכת **מציעה** משני מקורות, ואינה סמכות: (1) שורות פתוחות בטבלת ההזנה → "מחכה לחומר" מסומן מראש; (2) **המלצת AI מהזיכרון של הלקוח** (`AI_SUG`, רעיון של אייל) — הזיכרון יודע דברים שהמספרים לא ("מוסך רם עוזב", "החשבונית תוגש ביולי"): קופסה סגולה עם הנימוק + המקור + כפתור "אימוץ ההצעה" שממלא סוג והסבר. שתי ההצעות יכולות להתנגש — המתפעל מכריע. ברירת המחדל כשאין הצעה: "טעינו בבנייה".
- **חסימה — בשלב הבדיקה בלבד.** שלב ההזנות לעולם לא חוסם ("המתפעל מזין מה שיש"); שלב הבדיקה **כן** — אי אפשר לסיים עם פער מעל הסף בלי סוג. הזנה = "תכניס מה שיש", בדיקה = "תגיד שאתה מבין את התמונה".
- **סף מהותיות = שלושה פרמטרים**, כי סכום לבד ואחוז לבד שניהם שקריים: **רצפה מוחלטת** (מתחתיה אף פעם לא) · **אחוז מהיעד** (ההכרעה באמצע) · **תקרה מוחלטת** (מעליה תמיד). ברירת מחדל 1,000 ₪ · 5% · 25,000 ₪, **כוונון פר חברה בהגדרות חברה**. אותו כלל מזין גם את התצוגה (אפור/כתום) וגם את הגייט — מנגנון אחד.
- **שסתום נגד הצטברות:** פוטר "N קטגוריות מתחת לסף · X ₪" — שהרבה פערים קטנים לא יסתירו חור אמיתי.
- **תוצר:** הסיווג הופך לנתון אמין → "N קטגוריות מחכות לחומר · X ₪" כפריט עבודה בתור, ולקוח שחוזר חודשית עם "מחכה לחומר" = סיגנל חיכוך לראדר.
- **גם בשלב 3 בבדיקות (דוח תקציבי):** "חסר בתזרימים" הוא פער לכל דבר — על קטגוריה שאין לה עדיין שורה תקציבית (היעד נגזר מההיסטוריה). אותם ארבעה סוגים, אותו סף. **הכפתורים החופשיים הוסרו** ונגזרים מהסוג: "מחכה לחומר" → תזכורת בקבוצה · "היעד לא מעודכן" → פתיחת שורה תקציבית. `brGtOpen`/`brUntyped` ב-ops.js; `brGo` חוסם על שני המקורות. אגב כך הוסר `prompt()` מ-`brBudget` (הפר את כלל "אין דיאלוגי דפדפן").
- **מומש (מאוגוסט 2026):** `budget-flow.html` — `GAP_TYPES`/`GAP_TH`/`matOf`/`gtSuggest`, צ'יפ סוג על שורת הפער + "+ הגדרת הפער", פופאפ `openGtPop` (UI פנימי, לא נסגר ברקע, "הבא ←" לעבור בין הפערים), באנר גייט למעלה, פוטר תת-סף פר קבוצה, פירוק הפער לפי סוג בכרטיסי הסיכום. `js/cosettings.js` — סף מהותיות בטאב "תפעולי" עם **דוגמאות חיות** שמראות מה הסף מכריע (`coGapEx`), נשמר ב-localStorage ומשודר ל-iframe. `js/ops.js` — `gapGate()`/`brGo` חוסם את שלב 3 בבדיקה, ופאנל "מחכות לחומר" בראש שלב ההזנות עם תזכורת פר קטגוריה. `js/alerts.js` — ההסברים של "לא יתממש"/"יעד לא מעודכן" עולים ל-`ADV_PREPS`. הסנכרון בין ה-iframe להורה: `postMessage({hk:'gapGate'})` → `window.HK_GAPGATE`.

**שלבי הבדיקה בסיום התפעול (5):** שורה תקציבית · דוח חודשי · דוח תקציבי · **חומר מהלקוח** · **שינויים מהותיים בתזרים**.

- **מהותיות נקבעת מהסף בלבד — לא ידנית.** פופאפ "תיוג מהותיים" **הוסר** (איפשר למתפעל להוריד קטגוריה מהבדיקה בלחיצה = חור). אותם שלושה פרמטרים חלים גם על חסרים (`brMat`) וגם על חריגות (`brMatOver`, גודל החריגה מול התקציב). מה שמתחת לסף לא נעלם — פוטר מצטבר בכל עמודה.
- **שלב 4 · חומר מהלקוח:** סגירת יום. הרשימה **נגזרת** מהפערים שסווגו "מחכה לחומר" (תקציב + דוח תקציבי). שלוש דיספוזיציות: **הזכרתי · אין מענה** (לחיצה אחת — המקרה הנפוץ) / **יש מידע** (טקסט) / **דחייה לתאריך**. אין כפייה לכתוב סיבה כשאין — זה ייצר רעש שנראה כמו נתונים. התוצר האמיתי = **המונה** ("6 ימים · 4 תזכורות · לא ענה") ואחריו הצעת אסקלציה ליועץ (סיגנל חיכוך לראדר). **ימי חומר פר חברה** (הגדרות חברה › תפעולי) — פריט מאחר רק ביחס ליום שלו; בלעדיהם השלב הופך לחותמת גומי. הבסיס לאמינות המונה: **כל החברות מתופעלות כל יום — זה המוצר.**
- **שלב 5 · שינויים מהותיים בתזרים:** נשען על לוג השינויים. מציג נקודה על הציר (התאריך שהכי הורע), הדלתא, האירועים המובילים, ותג "העבודה שלך" על שינויים שנבעו מהתפעול של היום. **מדווח ולא חוסם — למעט חריגה חדשה שלא הייתה אתמול**, שדורשת החלטה (העברה בין חשבונות / התראה ללקוח ויועץ / נבדק).

**מעקב ופערים — סבב שקט ופילטרים:** **סינון פערים לפי סוג** (`FLT_SET`/`fltRender`) — חלונית צ'קבוקסים עם נקודת צבע ומונה פר סוג ("אילו פערים להציג?"), בחירה מרובה, "כל הפערים", והכפתור מציג את מצב הסינון. **סף הפער מהמסך = קריאה בלבד** — נבנה עורך מהיר ונפסל מיד (סתר את "מתפעל לא קובע לעצמו מה מהותי" — עוקף את הגייט); הכפתור מציג את הכלל (גם ₪ וגם %, מעל התקרה תמיד) ומפנה להגדרות החברה (postMessage hk:'coset'). **עיקרון "אין פער — לא מציגים כלום"** בכל הרמות: פער 0, מתחת לסף, ושורת הסיכום — עמודת פער ריקה (הפוטר של הקבוצה שומר את המצטבר). **המקרא ירד לכפתור "מקרא"** (חלונית עם הסברים, כולל ירוק="מעל היעד — עברנו את היעד" בהכנסות); הפס העליון שורה אחת. **שורות הרווח** — פס נייבי צר על אותו גריד עמודות כמו השורות (`bl-row pr-row` עם `bl-tgt`/`bl-act`): יעד מתחת ליעד, "בפועל + בתזרים" לידו; צ'יפי הכותרת של הקבוצות הוסרו. **לקח אימות:** לבדוק ב-index.html (ה-iframe), לא בקובץ העצמאי — עיגון חלוניות RTL התהפך שם ונחתך; סלקטור `.bl-tgt span` דלף ל-span הפנימי של `nfs` — עבר ל-`>span`.

**מסך מעקב ופערים — עיצוב (סבב ניקוי):** ירדו כמיותרים: באנר הגייט (הגייט חי בשלב הבדיקה), צ'יפי פירוק-לפי-סוג בכרטיסי הסיכום, "היתרה עוד לא נצבעה בתזרים"/"היעד מכוסה", כפתור "פירוט", **והעיפרון ליד היעד** (סתר את נעילת היעד — `tgtEdit`/`tgtSave` הוסרו). נוספו: **חץ ‹ קבוע** ליד שם הקטגוריה (אחרת לא ברור שהוא לחיץ), **המספרים בשורה כלינקים מסוננים** (לחיצה על "שורה תקציבית" פותחת רק את שורות הדמה), **סיכום פר קבוצה בכותרת** (פער וחריגה בנפרד — הם לא מקזזים), ושורות הדמה נכנסו לפירוט התנועות. **חריגה מוצגת כסכום בסגנון רגיל** (לא אחוז, לא אדום). **תצוגת חריגה בבר** — ארבע גרסאות נפסלו (בלוק אדום בתוך הבר / קו יעד על בר משתנה / גלישה מחוץ לבר / קו יעד קבוע ב-86%) עד לפתרון: **הבר עצמו נגמר במקום היעד** (86% מהרוחב, מסלול קבוע לכל השורות), ורק חריגה ממשיכה אחריו בפסים.

**מספרים שליליים:** המינוס **תמיד בצד שמאל** של המספר (`nfs()` עוטף ב-`dir="ltr"` — אחרת ה-RTL מעיף אותו לצד השני), וסימן ה-₪ מיד אחרי המינוס. **הפער בגרף** = כתום מקווקו (הקטע שלא נצבע), כך שהבר מספר את הסיפור בלי לקרוא מספרים: כחול=בפועל · תכלת מפוספס=בתזרים · סגול=שורה תקציבית · כתום=פער.

**כללי UI מחייבים:** אין `prompt/alert/confirm` של דפדפן ואין `<select>` נטיבי בזרימות מרכזיות — הכל UI פנימי; פופאפים לא נסגרים בלחיצת רקע; קטגוריה חדשה בתזרים חייבת כותרת רו"ה; ירוק=הכנסות בלבד; פער תזרים = כתום "נותר לצביעה בתזרים".

## 4. מצב נוכחי — מה עובד בפרוטוטייפ

**דגל דמו `OPS_SKIP_STAGES` (ops.js, ראש הקובץ):** `true` = לחיצה על "תפעול" נוחתת ישר על **שלבי הבדיקה** — כל משימות התפעול מסומנות done ב-seedOps ושער הכפילויות מדולג. `false` = הזרימה המלאה (5 שלבי תפעול ← רענון וכפילויות ← בדיקות). שינוי הפיך בדגל אחד.

הכל מהרשימה ב-`docs/ADVISORS_FEATURES.md` (18 פיצ'רים ליועץ) + אצל המנהל: תור תפעול מלא (נחיתה ישירה על שלב 3 — seedOps מסמן ai/payee כ-done, שינוי דמו הפיך), נגררות עם כיוונים, ראוטינג קבוצה, רדאר נטישה, יומן עם ניווט. אדמין: כל adminScreens מוטמעים + ניהול קטגוריות זיכרון. הגדרות חברה 5 טאבים.

**מה נוסף בסבב האחרון (לא נדחף):**

- **חדר הפגישה** (`meeting-room.html`) — מסך אחד, שלושה מצבים: הכנה · הקלטה · תוצרים.
  נוסף מצב רביעי — **פגישה קודמת**: לחיצה על שורה ברשימת הפגישות פותחת את מה שיצא
  מאותה פגישה ומה קרה מאז. ארבעה מצבים (הושלם · לאישור · בעיבוד · לא הוקלטה).
- **פס ההקלטה הגלובלי** (`js/recbar.js`) — ההקלטה לא חוטפת את המסך. `R` מתחיל/עוצר,
  רווח מסמן רגע מכל מקום במערכת. פאנל המגיש דוחף מסכים ל`client-view.html`.
- **המסך שהלקוח רואה בזום** (`client-view.html`) — טאב נפרד, מסונכרן ב-localStorage.
  **כלל ברזל בראש הקובץ:** לא נכנס לשם שום דבר פנימי — לא זיכרון, לא רגישויות,
  לא משוב על היועץ ולא מדדי שימוש. רק מספרים והחלטות שמסוכמות מול הלקוח.
- **פגישות של כל הלקוחות** (`meetings-arena.html`) — רק הקלטה מעלה את המספר; סיכום ידני
  סוגר את החור בזיכרון אבל לא מחזיר את הרצף.
- **עוזר ה-AI ליועצים** (`ai-assistant.html`) — סריקה שרצה לפני שהיועץ נכנס מעלה ממצאים,
  ולחיצה פותחת שיחה שכבר התחילה במקום תיבה ריקה. כל טענה נושאת ציטוט ממוספר, יש חלון
  "מה שלפתי ומה לא שלפתי", וכל תשובה נגמרת בפס פעולות. הכל עובר אישור והכל הפיך.
- **הלקוחות והכרטיס** (`clients.html`) — טבלת פוזיציות במקום גריד כרטיסים; הסיבה שהחברה
  שם היא טקסט לחיץ שפותח את הכרטיס על הבלוק הרלוונטי. בזיכרון: שלושה סוגי מקור
  (HK AI · נתון מערכת · היועץ עם מקור מוצהר), וכשיש מחלוקת — **הגרסה של היועץ קובעת**,
  ומה שהמערכת שמעה נשאר גלוי ומעומעם, לא נמחק.

**דחיפה:** יש קומיטים מקומיים לפני origin. דחיפה רק כשאייל כותב "תדחף".

## 5. משימות פתוחות / צעדים הבאים

0. **התאמות המשך למסכים החדשים:**
   - `clients.html` ו-`ai-assistant.html` מקבלים את התיק והמספרים מההורה ב-postMessage
     (`hkClReady`/`hkClNames`, `hkAIReady`/`hkAICounts`). התוכן העשיר — פיננסים, זיכרון,
     משתמשים — קיים רק ל`energy`; לכל השאר יש מצבים ריקים מפורשים.
   - הזיכרון החדש (קול כפול) חי בתוך `clients.html`. `js/memory.js` עדיין נפרד — צריך
     להחליט אם המסך הישן יורד או נשאר לאדמין.
   - טאב `prep` (הכנה לפגישה) הפך למיותר — ההכנה חיה בחדר הפגישה, ו`goPrep` מוביל לשם.
     הטאב עצמו נשאר בקוד אבל שום קישור חי לא מגיע אליו.
   - העלאת הקלטה בדיעבד קיימת ב-`meetings-arena.html`, ב"פגישה שלא הוקלטה" בחדר הפגישה
     ובכרטיס לקוח בלי הקלטות; לא נוספה למצב ההכנה בכוונה — שם היא הייתה מייצרת מסלול
     לעקוף את ההקלטה עצמה.

1. **tags.html (אדמין)** — טאב החרגות חדש: (1) הוצאה מהדוח (2) "קטגוריות לקטלוג" (גם מוחרגות וגם התנועות מקוטלגות). התחיל, לא הושלם.
2. **היסטוריית גרסאות אמיתית בכרטיס הזיכרון** — רשימת גרסאות עם "מה השתנה" (הוחלט, לא נבנה).
3. **דוח ערך ללקוח** ("מה HK עשתה בשבילך החודש") — הוצע, אושר עקרונית כשלב הבא אחרי הפגישות.
4. **עוזר AI ברמת התיק** ליועץ — הוצע, טרם נדון לעומק.
5. **בריף קבוצת הוואטסאפ** — התזה סוכמה בשיחה (ראוטר, לולאת בקשות); טרם נכתבה ל-brief.
6. עדכון `meetings-brief.md` סעיף 6 (עדיין מזכיר פס סטטיסטיקה ומקצב אצל יועץ שהוסרו) + הוספת בר טרום-פגישה, מרובה משתתפים, SIM ליועץ פעיל.

## 6. פרטים טכניים חשובים

**זרימת אימות (חובה לפני כל דיווח על UI):**
```bash
cp js/main.js $SCRATCH/hk-main.orig.js
cat >> js/main.js <<'EOF'
setTimeout(()=>{setRole('advisor');/* טריגר דמו */},400);
EOF
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu \
  --window-size=1600,1150 --hide-scrollbars --virtual-time-budget=14000 \
  --screenshot=out.png "file://$PWD/index.html"
cp $SCRATCH/hk-main.orig.js js/main.js   # שחזור תמיד!
```
iframes דורשים budget 12-16 אלף. עריכות קוד: python heredoc עם `assert old in text` — אם נכשל, לקרוא את הטקסט העדכני (עריכות קודמות משנות עוגנים).

**טריגרים לדמו:** `setRole('advisor'|'manager')`, `selectClient(i)`, `showTab('meetings'|'prep'|'coset'|...)`, `gnavGo('meets'|'clients')`, `openMeeting(ix)`, `openMemCard(i)`, `advPop('mem'|'radar'|...)`, `opsQueueEnter(0)`, `meetNew()`, `showPreMeetBar(mi)`, `startMeetRec(name,mi)`.

**מבני נתונים מרכזיים:** `MEETINGS[]` {status: upcoming|ai|summary|done|noshow, who: string|string[]|null, rec, link, sent}; `MEM_CATS[]` {key, scope:'company'|'user', internal, roles, appliesTo, prompt, desc}; `MEM_UPDATES[]` {ci, cat, catName, line, sev, src, when}; `ADV_RADAR`, `OPS_REQS`, `MEETS_UP/PAST/CALLS/CALLS_ADV`, `ADV_PREPS` (לפי שם לקוח).

**מנגנונים לא-מובנים-מאליהם:**
- `MEM_HOST` — רנדור כרטיס הזיכרון לתוך טאב הגדרות חברה; `openMemCard` מאפס אותו. אסור לאפס אחרי רנדר בטאב (קליקים פנימיים ירנדרו לפופאפ).
- מודל המשימה `mtkOv` משותף: `window._mtkTarget='adv'` מנתב שמירה ל-`ADV_TODO`.
- `whoPicker(pfx,label)` — בורר משתתף משותף (עמוד פגישה `ms`, מודל קביעה `mn` דרך `window._mnWho`).
- `body.bar-on` — padding-top:48px + `.topbar{top:48px}` כשיש בר עליון.
- הקלטה: `MREC.mi` מקשר לפגישה ב-`MEETINGS`; סיום מעדכן אותה ל-`ai`.
- `window._preBarShown` — בר הטרום מוצג פעם אחת (טריגר ב-renderAdvisorHome וב-renderMeetsArena).
- hover-reveal גורף: `.att-row/.mc-item/.mf-row/.ms-row/.mtcard/.mc-todo-row` — כפתורים ב-opacity:0 עד hover.

## 7. מלכודות ונקודות זהירות

- **דחיפה לגיט רק כשאייל כותב "תדחף"** — אף פעם ביוזמתי. עברית בלבד בכל תשובה.
- **עוגני טקסט נשברים**: theme-hk.css הוא append-only — כלל חדש תמיד גובר; ב-js, replace קודמים משנים את הטקסט (ה-assert נכשל הרבה — תמיד grep לפני).
- `node --check` על כל js אחרי עריכה; שחזור main.js אחרי כל צילום (diff לוודא).
- `seedOps` מסמן ai/payee כ-done (דמו "ישר לשלב 3") — להחזיר כשנרצה להדגים קיטלוג.
- `.mc-drop` (סלוטים ריקים ביומן) נסתרים עד hover — זה בכוונה, לא באג.
- הפרומפטים ב-MEM_CATS הם תוכן אמיתי לאפיון — לא לגעת בלי סיבה.
- אין להוסיף שדות/פיצ'רים שאייל לא ביקש (נלמד בדרך הקשה: אסקלציה ליועץ, "יועצי HK"). כשמשהו לא ברור — לשאול, "תחשוב טוב, אל תעשה סתם".
- מסך ההכנה `viewPrep` קיים רק ליועץ (advisor-only tab).
- `hkConfirm(title,msg,btn,cb)` — האישור המערכתי; להשתמש בו, לא ב-confirm.

## מסכי היועץ מוטמעים במערכת (docs/adv3)
חמשת מסכי היועץ — `today` · `tasks` · `clients` · `meetings` · `memory` — חיים כקבצים עצמאיים
ב-`docs/adv3/` ומוטמעים ב-`index.html` כ-iframe עם `?embed=1`.

- **הטמעה**: לכל קובץ נוסף בסוף בלוק `hk-embed-css` / `hk-embed-js`. עם `?embed=1` הדף מסתיר
  את הניווט הפנימי שלו (סרגל המערכת מחליף אותו), מדווח גובה ב-`postMessage({hkAdv:'<key>',h})`,
  וקישור פנימי `*.html` נתפס והופך ל-`postMessage({hkAdvGo:'<file>'})` במקום ניווט בתוך המסגרת.
- **מכולות ב-index.html**: `#advToday` `#advTasks` `#advMeets` `#advMem` (+ `#clFrame` הקיים,
  שהופנה ל-`docs/adv3/clients.html`). כולן טוענות עצל — `src` נקבע מ-`data-src` רק בכניסה הראשונה.
- **ניתוב** (`js/screens.js`): `ADV_PVIEW` מקבל `home|tasks|clients|meets|how`; `ADVMAP` בתוך
  `setScope` מדליק את המכולה הנכונה, ו-`advTakes` מכבה את התצוגות הישנות (alerts/meets/how)
  ואת כותרת העמוד — לכל מסך יש פס וכותרת משלו.
- **סרגל הניווט**: נוסף פריט `משימות`, והסדר ליועץ הוא היום · משימות · לקוחות · פגישות · זיכרון לקוחות · יומן · הגדרות.
- **גשר ההודעות** (`js/board.js`): `d.hkAdv` מעדכן `minHeight` של המסגרת; `d.hkAdvGo` ממפה שם קובץ ל-`gnavGo`.
- **היומן**: סרגל הכלים של `calendar.html` עצמו הפך לכחול (רקע `--navy`, גובה 68px) — בלי פס נוסף מעליו.
- **יישור לפס**: ב-5 המסכים ל-`.wrap` אין ריפוד אופקי — הפאנלים מיושרים לקצוות הפס הכחול.
- **לקוחות**: הפס מציג רק את משפטי ההתראה — "4 לקוחות עם **בעיה תזרימית**" (אדום) ו-"לקוח אחד
  **באי שביעות רצון**" (צהוב); רק המילים צבועות ולחיצות ומסננות את הטבלה. שורת הסינון השנייה
  (אנשי קשר · Money/Meeting) הוסרה. טוקן הסטטוס — נקודת צבע + שם בגלולה — זהה בשלושת המקומות:
  צ'יפ הסינון, תא הטבלה, ועורך הסטטוס בכרטיס. בחלון צר התאים נשברים והכרטיס מצטמצם ל-360px
  כדי שעמודת ההתראות לא תיחתך.
- **מסכים נעולי-גובה**: פגישות · זיכרון לקוחות · לקוחות. הם מדווחים `hkAdvLock` במקום גובה תוכן,
  ו-`advFrameFill()` ב-`js/board.js` מותח את המסגרת עד תחתית החלון. זה גם מה שמאפשר לפופאפים
  (`position:fixed`) ליפול במרכז החלק הנראה — בלי זה הם נחתו באמצע ה-iframe הארוך, מתחת לקפל.
- **חוק הכחול**: בגוף המסכים הכחול הכהה שמור לכותרות פאנל בלבד (`.ph`) — לא לבלוקים שלמים.
  הטבלה בלקוחות: עמודות חברה · סטטוס · יעד · פגישה אחרונה · פגישה הבאה · התראות; היעד בעמודה
  נפרדת ובלי המילים "עבר"/"עד" (אדום = עבר), ולחיצה על טוקן הסטטוס פותחת תפריט להחלפה מהשורה.
- **פגישות**: הבלוק הכהה "מה חסר" (ארבעה מונים) הוסר; הזירה בצד שמאל היא הקבועה. המסך נעול
  לגובה החלון — הוא מדווח `hkAdvLock` ו-`advFrameFill()` ב-`js/board.js` מותח את המסגרת עד התחתית.

## דפי הלקוח (docs/cli)
`dashboard` · `messages` · `meetings` · `ai` · `metrics` — ניב הלקוח: אותה משפחה, רדיוס רך יותר
(פאנל 14), צפיפות נמוכה, פנייה בגוף שני, קרקע #F7F9FC.
- **`meetings2.html` — גרסה 2 של מסך הפגישות.** הבסיס הוא המסך שקיים היום במערכת
  (`js/climeet.js`): אותו סדר, אותם בלוקים, אותו טקסט — כרטיס הפגישה הבאה עם "מה מחכה ממני"
  בצד, שורת "גם ביומן", ורשימת "הפגישות שקיימנו" שנפתחת ל"מה סוכם" / "מה יצא מזה".
  מה שהשתנה: הקליפה של דפי הלקוח (ניווט + רצועת זהות) והרדיוס הרך. בלי בלוקים חדשים.
  `meetings.html` (גרסה 1) נשארה כפי שהיא — היא הרחיבה יותר מדי.

## פאנל התפעול — טאב "כללי"
הפאנל שמלווה את מנהלת התזרים לאורך כל שלבי התפעול והבדיקה קיבל טאב חמישי: **כללי** —
תיק החברה שאין לו מקום בסרגל. `OPS_BRIEF` (פר לקוח) ו-`OPS_STAGE_NOTE` (פר שלב) ב-`js/ops.js`:
- זהות: שם · ח.פ · ענף · חבילה · מנהלת התזרים · משרד הייעוץ
- **רלוונטי לשלב הנוכחי** — הערה שמתחלפת לפי השלב שבו המתפעלת עומדת (`window._stgIx`)
- מאיפה מגיע החומר (Bizibox, הנה״ח, מי שולח) · כללי עבודה קבועים · שים לב החודש · סנכרון אחרון
- קישורים למעקב ופערים, לפגישות ולדשבורד של החברה
הכיתובים בסרגל הטאבים הוקטנו כדי שחמישה ייכנסו בלי להיחתך.

## סדר שלבי התפעול
`הודעות לקוח · מוטבים · קטגוריות · נגררות ולא צפויות · הזנות ואוטומציה` —
מוטבים לפני קטגוריות: שם המוטב מהשיק הוא מה שמאפשר לקטלג נכון.

## טאב "כללי" — הכל נערך במקום
תיק החברה של מנהלת התזרים: ח.פ · ענף · חבילה · מנהל תזרים · משרד, ואז שלוש רשימות —
**מאיפה מגיע החומר · כללי עבודה קבועים · שים לב החודש** (`OPS_BRIEF` פר לקוח).
אין טופס הוספה: כל שורה היא `contenteditable` שנשמרת ב-blur (`opsGenSet`), ✕ בריחוף מוחק,
ובסוף כל רשימה שורת רפאים "שורה חדשה…" שהופכת לשורה אמיתית ברגע שכותבים בה.
שם החברה, בלוק הסנכרון והקישורים לא מופיעים — הם כבר בפס התפעול שמעל.

## שלב מוטבים — כיוון והיסטוריה
- לכל שיק יש `dir` (`out`/`in`). ברשימה: פס וגוון רקע — אדום להוצאה, ירוק להכנסה — ותגית
  "הוצאה"/"הכנסה" ליד הסכום. בכרטיס: פס כיוון מעל הטופס עם הסכום ב-−/+.
- **"בדוק היסטוריה"** ליד שדה שם המוטב (`payHist`). פותח בתוך הטופס את התנועות הקודמות
  של אותו מוטב מ-`PAYEE_HIST` — תאריך, מס׳ שיק, סכום בצבע הכיוון, והקטגוריה שבה קוטלג.
  אם כל התנועות באותה קטגוריה — כפתור "קיטלוג כמו קודם" שממלא את שדה הקטגוריה.
  אם אין תנועות — "זו הפעם הראשונה שהמוטב הזה מופיע".

## מקור התנועה על השורה — בנק או אשראי
`ACCTS` ב-`js/ops.js` קיבל `kind` (`bank`/`card`), ונוספו שני כרטיסי אשראי (מקס 4821, כאל 7712).
`srcIco(t)` מציג אייקון בתחילת כל שורת תפעול: אייקון בנק בתכלת, אייקון כרטיס בסגול, עם tooltip
שאומר "תנועת בנק / תנועת אשראי" ואת שם החשבון. שבב החשבון מקבל את אותו גוון.

## תיעוד שיחה עם הלקוח (מסך סיום התפעול)
בתחתית בלוק "חריגות לדיווח ללקוח" יש יומן אחד לכל החריגות — לא פר שורה. `FIN_CALLS` פר חברה,
`finCallAdd` מוסיף רשומה עם חותמת שעה (`finClock` = 10:54 + משך התפעול), ו-✕ מוחק.
כפתור התיעוד מציג את השעה שתירשם, כדי שברור מה נשמר.

## מעקב ופערים — עריכת יעד ופער אפור
- **הפער אפור** (`--gap:#5A6B7D`) במקום הכתום. הוא מספר לקריאה, לא אזהרה. שלושת המקומות
  שהציגו אותו (השורה, כרטיס הסיכום, כותרת פופאפ סיווג הפער) עברו יחד.
- **שלושת החודשים האחרונים** — יושבים בתוך פופאפ הפעולות של הקטגוריה, מתחת לסיכום:
  חודש · בר · סכום, ואז ממוצע 3 חודשים והשוואה ליעד הנוכחי. `h3Of` גוזר את שלושת הערכים
  דטרמיניסטית משם הקטגוריה, כך שבשורות "ממוצע 3 ח׳" הממוצע מתלכד בדיוק עם היעד.
- **עריכת יעד** — לחיצה על מספר היעד פותחת עורך באותו מנגנון של התא הנערך בתזרים העתידי:
  ערך חופשי, מתג "פתח יעד ללקוח" עם הערת כיוון (הכנסה לעבור / הוצאה לא לעבור), הפרש חי מול
  הערך שהיה בתחילת החודש (`r._base`) וכפתור חזרה אליו. תג המקור נגזר מהפעולה — יעד מוסכם /
  שונה ידנית / חזרה לממוצע. בתצוגת לקוח (`IS_CLI`) התא נשאר לקריאה בלבד.

## סטטוס חברה — חמישה מצבים, וסינון מהסרגל העליון
`CO_STATES` ב-`js/data.js`: **פעיל · בהקמה · חודש ניסיון · לא פעיל · חדש** (+ ארכיון, שהוא סטטוס
התקשרות ונקבע בפרטי לקוח). `coActive` = פעיל או חודש ניסיון — רק הם נכנסים לתור התפעול היומי.
- **הגדרות חברה → תפעולי**: "מצב החברה" הוא דרופדאון פנימי (`cos-stdd`) עם נקודת צבע והסבר
  לכל מצב, במקום שני המתגים שהיו.
- **הסרגל העליון**: נוסף דרופדאון "כל הסטטוסים" (`STAT_FILTER`/`statOk`) לצד מנהלי התזרים,
  חברות הייעוץ והמוצרים.
- **סרגל התפעול**: רשימה שטוחה של כל החברות — בלי קוביה סביב כל שורה, בלי מתגי המצבים
  שהיו בתחתית ובלי קיפול "הושלמו". הסינון עבר לדרופדאון שבסרגל העליון.

## תכנון חשבונאי — פישוט לקריאוּת ניהולית
המסך היה עמוס מדי לניהול. מה שירד: הרמה השלישית (פירוט תיאורים) — היא עדיין מחשבת ומסכמת
למעלה אבל לא מוצגת (`MAXLVL=2`); שורת "יתרה מתגלגלת"; שורת הרפאים "לא משויך לסעיף";
התצוגה המאוחדת; כפתור 📅 שישה חודשי תנועות; כפתור 🏷 סיווג תיאורים; מונה "N מקורות";
והתגית "זהה בשתי התצוגות".
עוד ירדו: רצועת "מפת מיפוי", הערת הגיאומטריה, ו"% מהמחזור" בשורת הנטו.

**המבנה הטבלאי:** כל חודש סגור מחולק לשלוש תת-עמודות — **בפועל · יעד · הפרש** — עם כותרות
ממורכזות מתחת לשם החודש; חודש עתידי נושא **יעד** בלבד ולכן צר יותר (`--colw3` מול `--colw`).
המספרים ממורכזים אופקית ואנכית, הגבול בין חודשים 2px, ורווח מפריד בין הקבוצות.
שורות הסה״כ צבועות בגוון התצוגה (`--tot-bg`: כחול #1B5C8A בתזרימי, סגול #5049B4 בחשבונאי)
עם רווח מעליהן. שורת "תזרים נטו" בהירה עם קו עליון בגוון התצוגה.

## מסך זיכרון לקוחות — נחשב מחדש
המסך הוא **קריאה ושליפה** בלבד. שתי משפחות זיכרון, כמו במערכת האמיתית:
**זיכרון החברה** (מצב תזרימי · פרופיל עסקי · כאבי לקוח · יעדים והסכמות · אירועים מהותיים ·
שיתוף פעולה, כחול) ו**זיכרון האנשים** (סגנון תקשורת · שביעות רצון · מצב אישי ולחץ · הקשר אישי
ואמון, סגול). הזיכרון האישי נצמד לאדם — גם כשהוא מחזיק כמה חברות (רימון יצחק · 2 חברות).
- **הפיד** — שורת אדם נושאת את שם האדם (סגול) והחברות שלו מתחת; הצ'יפים בשתי קבוצות מסומנות.
- **הבלוק הכחול = תיק הזיכרון** של מי שנבחר (`ENT`/`spotShow`): זהות, האנשים/החברות,
  והשורה העדכנית בכל קטגוריה. ברירת מחדל: העדכון האחרון. לחיצה על כל שם בפיד או ברשימה מחליפה.
- **שליפה** — טאבים חברות | אנשים; בחירת אדם מסננת את הפיד לפי `data-p` חוצה-חברות.
- ירדו: "לפני הפגישות של היום" (שייך למסכי היום/פגישות), רשימת 17 השקטים, ופופאפ הכרטיס.
הוחלט במפורש לא להכניס "מה הזיכרון דורש ממני" — הסכמות מפגישות כבר הופכות למשימות במסך המשימות.

## מסמך אפיון הזיכרון — docs/MEMORY_SPEC.md
נכתב 27.08.2026. מגדיר: מטרת הזיכרון (הקשר ל-AI ולא ארכיון), ההפרדה בין אפיון למדידה
(מספרים חיים לא נשמרים — נשלפים בשאילתה), 12 הקטגוריות (6 חברה + 6 אדם) עם מה נכנס
ומה לא, איפה הזיכרון יושב (**כרטיס החברה, לא הגדרות**), המסך הכללי, ומחזור החיים —
כולל שלושת פעלי העריכה ויומן העריכות שמחזיק את התיקון דביק מול הפרומפט.
שלוש הצעות מסומנות `[הצעה]`: פיצול "מצב תזרימי" לפי נושא המשפט, קטגוריית "מבנה קבלת
החלטות", ואיחוד "שביעות רצון" לתוך "הקשר אישי ואמון". שש שאלות פתוחות להכרעה.
פרק 8 מונה שישה באגים קיימים ב-`js/memory.js` (vis לא נקרא ב-company, pend מת,
MEM_EDIT מת, memDel לא נוגע ב-MEM_UDATA, הוספת קטגוריה זורקת TypeError).

## מסך זיכרון החברה — מודל שורות (טאב בכרטיס הלקוח)
`MEM_DATA` הומר: כל קטגוריה היא `{lines:[{id,t,src,d,st}], updated, src}` — התאריך והמקור
הם שדות, לא כתובים בתוך הטקסט. `st`: live · solved · dropped · stale.

**המסך** (`renderCoMem`, `js/memory.js`): שתי עמודות מקבילות — **העסק** (נקודה כחולה)
מול **האדם** (נקודה סגולה), כותרת נדבקת לכל עמודה.
- **השורה נקראת כפסקה.** אין קו מפריד, אין תבליט, אין מספור.
- **חותמת רק כשיש דלתא** — התאריך נדפס רק אם הוא שונה מתאריך הכרטיס, המקור רק אם הוא
  שונה. תוצאה: 14 חותמות על 47 שורות; ב-6 מ-11 הקטגוריות אפס. היעדר חותמת = השורה
  נולדה באינטראקציה האחרונה.
- **רייל אחד לכרטיס** מחוץ ל-`contenteditable`, גולש ב-`translateY` לשורה שמתחת לעכבר.
  live/solved → ✎ ו-✕ · dropped → ↺ בלבד. מרזב 44px שמור תמיד, אפס reflow.
- **`contenteditable` על המכל** ולא על כל שורה — Enter מפצל, Backspace ממזג, הדבקה עובדת.
- **אין מחיקה קשה**: שורה שנשמרה ונמחקה מהמקלדת חוזרת כ-`dropped` עם שם ותאריך.
- שורה שהיועץ הוסיף/תיקן מקבלת פס סגול בקצה ו-`src:'היועץ'`; הנוסח הישן יורד ל-`hist`.
- החיפוש **מסנן כרטיסים** (לא מעמעם) ומדגיש מילים; שורות בתוך כרטיס תואם נשארות בדיו מלא.
- קנוני לפי `DESIGN_NOTES`: `.ph` 17px/600 עם קו, ריפוד אופקי 20, צל הפאנל, צ׳יפ פעיל נייבי.

## (קודם) מסך זיכרון החברה — גרסת הפסקה
`renderCoMem` בסוף `js/memory.js`, `#viewMem` ב-`index.html`, טאב `mem` בסרגל הלקוח
(אחרי פגישות, מוסתר ללקוח). מיישם את פרק 5 של `docs/MEMORY_SPEC.md`:
- **העסק** — שש קטגוריות החברה ככרטיסים; **האנשים** — בורר אדם ואז הקטגוריות שלו.
- לכל כרטיס: שם · שבב "HK בלבד" · מקור ותאריך עם אייקון · "היסטוריה N" שנפתחת.
- **הכל נערך במקום** (`contenteditable`, שמירה ב-blur) — תיקון, הוספה ומחיקה הם אותה
  תנועה. עריכה נשמרת עם `src:'היועץ'`, `origin:'advisor'`, הנוסח הקודם יורד ל-`hist`,
  והכרטיס מסומן בפס סגול.
- חיפוש מעמעם כרטיסים שלא תואמים ומדגיש את המילה.
- התוכן יצא מהגדרות החברה; שם נשארות רק ההגדרות.

## שכבת המותג — docs/adv3/brand.css
סבב עיצוב על חמשת מסכי היועץ בכיוון של hak.co.il: **קורל-סלמון (#E4826E) הוא צבע הפעולה**
(כפתור ראשי = גלולת קורל), נייבי נשאר המבנה, **Heebo מחליף את Rubik ואת Frank Ruhl** —
כותרת המסך Heebo 800 עם טיק קורל קטן מתחתיה. גלולות במקום מלבנים (כפתורים, צ'יפים,
חיפוש), פאנלים ברדיוס 14 עם צל עומק עדין, ניווט לבן שהפעיל בו מסומן בקו קורל, ::selection
קורל וגלילה מעוצבת. הכל בקובץ override אחד שנטען אחרי ה-CSS הפנימי של כל קובץ (אותו דפוס
כמו theme-hk.css) — כך השינויים שנעשו אחרי ההטמעה נשמרים כלשונם. סמנטיקה: קורל = פעולה ·
אדום עמוק = אזהרה · נייבי = מצב/מבנה. עובד גם במצב embed דרך index.html.

## אפיון דפי הלקוח — docs/CLIENT_SCREENS_SPEC.md
נכתב 29.08.2026, לפני בנייה מחדש של docs/cli (הגרסה הראשונה המציאה יותר מדי).
העיקרון: לכל מסך לקוח יש מקור חי במערכת (תצוגת לקוח ב-index.html) — דף הלקוח הוא
הקליפה העצמאית שלו, שום בלוק לא נולד בלי מקור. הניב: גוף שני, קרקע חמה, מספרים
גדולים, קורל=פעולה ואדום עמוק=סכנה כספית בלבד, בלוק כהה אחד למסך, ושכבת המותג
של adv3 (hak.co.il). פגישות = meetings2 שאושרה. פתוח: גורל דף ההודעות, הדבר החי
ברצועת הדשבורד, ועומק המספרים.

## מסכי החברה של היועץ — docs/cli (בבנייה מחדש)
`docs/CLIENT_SCREENS_SPEC.md` מוסגר מחדש (29-30.08): אלה מסכי **יועץ פר חברה** —
הגרסה העצמאית של טאבי כרטיס החברה — לא דפי לקוח קצה. חמישה: דשבורד · קבוצת
הוואטסאפ · פגישות (זירה+הכנה פר חברה) · עוזר AI (גרפי רק על "מצב עכשיו", קול,
היסטוריה) · מדדים (המסך מתהפך: ביצוע למעלה בקצב-מול-זמן, הצנרת מקופלת למטה).
**dashboard.html נבנה מחדש (29.08):** פס פעולות כחול עדין (משימות למנהלת התזרים +
חדשה · העלאת מסמכים · יתרה + צפי 30 יום · פגישה הבאה — דינמי לפי מוצר), תקציר
הבוקר (צעד החיבור של הג'וב הלילי — קורא זיכרון+מספרים, לא מקורות גולמיים) עם
עמודת חתימה בצד, וגרירת קובץ לכל המסך.

**דף ההכנה דינמי פר יועץ (29.08) — במודל של זיכרון הלקוח.** `PREP_CATS` ב-
`docs/cli/meetings.html`: לכל קטגוריה `key · name · desc · on · prompt`. היועץ בוחר
אילו קטגוריות ייכנסו לדף ההכנה שלו, בסדר שהוא קובע, ו**כותב את הפרומפט של כל אחת** —
בדיוק כמו `MEM_CATS` ב-`js/memory.js`. ההגדרה שייכת ליועץ ולא לחברה: היא חלה על כל
לקוחותיו. שבע קטגוריות ברירת מחדל (מספרים · תמונת פתיחה · נקודות לדיון · מטרה ·
מה נסגר · מה פתוח · איך לדבר איתו), ו"+ הוספה" מייצר קטגוריה עם שלד פרומפט.
ההכנה **חיה** — לא נכתבת פר פגישה, אלא מתעדכנת מהזיכרון ומהמספרים.

**הווידג'טים הם המקוריים של המערכת — לא שוכפלו.** נטענים כ-iframe מ-`../../widgets/`
עם `#embed`, ומדווחים את גובהם ב-`postMessage({hkEmbed,src,h})` — אותו פרוטוקול של
`js/board.js`, כולל fallback שמסיר את שלד הטעינה אחרי 2.6 שניות. אסור לגעת בהם:
כל הכרום (באדג' אדום, "שאל את העוזר") יושב ברצועת מטא של 24px **מעל** הכרטיס,
כדי לא לכסות תוכן.

**הפריסה** — רוחב מלא בלי גבולות בצדדים; 1/2/3 עמודות ב-media queries קבועים
(980 / 1400), ו-`layout()` נותן לכל ווידג'ט `grid-column` מפורש. בלי זה ה-masonry
מסדר לפי גובה ומחליף בין "מעקב ופערים" ל"בקרת תקציב". הסדר: **תחזית תזרים ·
רווחיות · סה"כ הכנסות · מעקב ופערים · בקרת תקציב** — התזרים ראשון כי הוא מה שהתקציר
מצביע עליו. ב-3 עמודות "מעקב ופערים" נפרש על שתיים (הוא היחיד שנהנה מרוחב;
`widget-budget-control` נעול ל-640px משלו) — כך אף עמודה לא נשארת ריקה למטה.
מתחת ל-1400px הפס הכחול נשבר לשתי שורות במקום להיחתך.
הנתונים במעטפת אמיתיים מהמערכת: `ADV_TASKS[0]` (2 משימות ללירון, אחת באיחור),
`MEETINGS` (05.08 צחי עובד), יתרה 75,187 ₪ כמו בווידג'ט התזרים.

**כרום משותף — `cli.css` + `cli.js` (29.08).** הניווט, רצועת החברה והפס הכחול
נכתבים פעם אחת ומרונדרים בכל מסך דרך `hkChrome('<key>')`; נתוני החברה חיים
באובייקט `CO` ומשם נגזרת הדינמיות של הפס לפי המוצר. מסך חדש טוען את שני הקבצים,
קורא ל-hkChrome, וכותב רק את התוכן שלו. זו התשובה ל"הכל מבולגן, חוזר לי בין
מסכים" — בלי מקור אחד החמישה נפרדו זה מזה (הארבעה היו עדיין בגרסת לקוח).
מונה המשימות בפס הוא כפתור שפותח את הרשימה עצמה עם "סמן כבוצע".

**ארבעת המסכים הנותרים נבנו (29.08):**
- `messages.html` — קבוצת הוואטסאפ: סרגל קבוצות + משתתפים (כולל מספר לא מזוהה),
  זרם עם גלולת יום, וכרטיס הסיכום הלילי הנייבי מקובץ למשפחות (העסק תכלת / אדם סגול)
  עם שורת אזהרה על מספר לא מזוהה. אין תיבת כתיבה — כפתור קורל "המשך בוואטסאפ".
  הגלילה בתוך `.mg-scroll` בגובה מחושב, כך ששם החברה וגלולת היום לא נגללים.
- `meetings.html` — הפגישה הבאה פרושה (הכרטיס הכהה היחיד) עם דף ההכנה פתוח
  והקלטה מהירה בקורל, ומתחת זירת הפגישות של החברה. התרעה כשאין פגישה קרובה.
- `ai.html` — שיחה נקייה: תשובה גרפית רק על "מצב עכשיו" ועל שאלות זיכרון
  (`ROUTES`), כל השאר טקסט. שאלות מוכנות, הקלטת שאלה, שיחה קולית, היסטוריה.
- `metrics.html` — בלי טאבים וללא צ'יפי סינון: רשימה אחת ממוינת לפי מצב, בר
  קצב-מול-זמן עם סמן קורל ופער מכומת ליום עבודה, "בקש מהלקוח" על מדד שלא עודכן,
  ו"ניהול המדדים" מקופל בתחתית. `MONTH_OPEN` קובע את הניסוח (בקצב / נסגרו ביעד).

**דפוס "רשימה + זירה" (29.08).** אחרי סבב המשוב, פגישות ומדדים עברו לאותו מבנה:
הרשימה מימין, וכל פריט שעומדים עליו הופך את הצד השמאלי לזירה שלו. בפגישות הרשימה
היא **ציר זמן** — עתידיות בקולאפס, סמן "עכשיו" בקורל, ואז מה שהתקיים; הזירה מציגה
דף הכנה לפגישה עתידית וסיכום לפגישה שהתקיימה. במדדים הרשימה היא כל המדדים ממוינים
לפי מצב, והזירה היא מספר גדול + קצב-מול-זמן + מגמת ששה חודשים + חוקי התראה.
כך "הפגישה הבאה" היא חלק מהפגישות ולא פאנל שדוחף אותן מתחת לקפל.

**הרצועה והפס הכחול — רק בדשבורד.** `hkChrome(key,{strip:false})` מוריד את שניהם
ומחליף בפירורית לחם קומפקטית בניווט. הפס מרכז את מה שעושים מול HK, והמקום שלו הוא
מסך הפתיחה. מודל המשימה וגרירת הקובץ נשארים בכל מסך — הם שכבות, לא פס.

**פגישות — ההכנה היא דף חי, לא נספח לפגישה.** ברירת המחדל של הזירה היא **ההכנה
החיה**: לא שייכת לפגישה מסוימת, נבנית מהזיכרון ומהמספרים ומתעדכנת כל הזמן, וזה
המקום שסורקים בו הכל לפני פגישה. ההקלטה המהירה יושבת שם — מקליטים בלי פגישה קיימת.
לחיצה על פגישה בציר פותחת את **זירת הפגישה המלאה** כמו בזירה הרוחבית: סטטוס, נגן
הקלטה עם גל קול, וטאבים סיכום · משימות · זיכרון · תמלול, עם אישור פר משימה
ואישור/שליחת הסיכום. "→ להכנה החיה" מחזיר.

**מה נחשב מדד (הוגדר 29.08).** *מדד = יעד שנקבע ללקוח ורוצים להציג לו.*
לכן "חריגה צפויה בעו״ש" (התרעה) ו"פגישה חודשית · Money+" (התחייבות שירות) **ירדו**
מרשימת המדדים — נשארו 9. הכלל הזה קובע גם מה ייכנס בעתיד.

**רשימת המדדים:** כותרות עמודה שממיינות (מצב · שם · סוג · בפועל · יעד, לחיצה חוזרת
הופכת כיוון), חיפוש, ובורר חודש שהחצים שלו ב-RTL — "הקודם" מצביע ימינה.
**בזירה:** מתג "ימים · חודשים" — ימים מצייר גרף לאורך ימי העבודה של החודש
(נגזר מהערך החודשי בצבירה, דטרמיניסטי), חודשים מציג את ששת החודשים בעמודות.

**מדדים — בלי פאנל "יעדי החודש".** הוא היה מיותר; בורר החודש ו"+ מדד חדש" עברו
לכותרת רשימת המדדים. הרשימה עצמה היא טבלה: **שם · סוג · בפועל · יעד** — בלי בארים
ובלי גרפים; כל הגרפיקה חיה בזירת המדד.

**עוזר AI — שתי משפחות של שאלות מוכנות**, "מספרים" ו"החברה והאנשים", כדי שיהיה
ברור שאפשר לשאול מספרים פיננסיים ולא רק שאלות רכות. **השיחה הקולית היא הפעולה
הבולטת בתיבה** — כפתור קורל עם טבעת פועמת ותת-שורה, לא כפתור משני.

**זירת הפגישה** כוללת גם טאב **משוב** (משוב ה-AI על ניהול הפגישה, לעיני היועץ
בלבד — MS_FB ב-js/meetings.js), ובכותרת הציר יש "שאל את העוזר על הפגישות".
**תקציר הבוקר בדשבורד קוצר** ואינו נפתח בפגישה שהסיכום שלה ממתין.

**מסמכים ומשימות = פעולה אחת.** אין "העלאת מסמכים" נפרדת: כל קובץ נכנס דרך משימה,
עם ההסבר מה לעשות איתו. גרירה לכל מקום פותחת את מודל המשימה עם הקובץ מצורף.

**ניסוח הסיכום.** הסיכום חוזר מהתמלול תוך דקות, לא "במהלך היום" — פגישת 09:00
מופיעה כ-`summary` (ממתין לאישור) ולא כ-`ai` (בעיבוד).

ממתינות להכרעה: מצב לקוח לשיתוף מסך · סימולטור · בריף קולי.
**פתוח:** ווידג'ט התזרים מתארך את עצמו ל-27.04.2026 בעוד המערכת על 02.07.2026 —
תיקון נתוני דמו בתוך הווידג'ט, ממתין לאישור אייל.

---

## JAMPA — הגרסה למטפלים (`jampa/`)

התאמה של אותו ממשק לעולם הטיפול. **העיקרון: אותו core, מותג אחר.**
אין שכפול עיצוב — שני קבצי CSS מייבאים את שכבות ה-HK ודורסים טוקנים בלבד:

- `jampa/cli/cli.css` — `@import` של `docs/cli/cli.css` + טוקנים סגולים
- `jampa/adv/brand.css` — `@import` של `docs/adv3/brand.css` + טוקנים סגולים
  (המסכים של adv3 מגדירים `:root` בכל קובץ; brand.css נטען אחריהם ולכן דורס)

פלטה: דיו סגלגל `#37306B` · סגול `#7557E3` · אקצנט `#9C89F2` · קרם `#FBF8F2` · Rubik.
המיפוי מ-HK: נייבי→דיו סגלגל · קורל→סגול · תכלת→לבנדר.

### שני רבדים, עשרה מסכים
| רובד | מסכים |
|---|---|
| **המטפלת · לכל המטופלים** (`jampa/adv/`) | היום · משימות · מטופלים · פגישות · פרופילי מטופלים · יומן |
| **מטופל אחד** (`jampa/cli/`) | דשבורד · השיחה · פגישות · עוזר AI · התהליך הטיפולי |

`jampa/index.html` הוא מעטפת בלבד — פס עליון, מחפש מטופלים, סרגל דו-רמתי ומסגרת
לכל מסך. אין בה תוכן; היא רק מנתבת. אותם פרוטוקולים של המערכת:
`hkAdv`/`hkCli` (גובה) · `hkAdvGo`/`hkCliGo` (החלפת מסך) · `hkAdvOpen` (פתיחת מטופל) ·
`hkAdvLock` (מסך נעול-גובה — מטופלים, פגישות, פרופילים ויומן).

**היומן** (`jampa/adv/calendar.html`) הוא עותק של `calendar.html` הראשי. הוא כבר נבנה
לעולם טיפולי — סוגי הפגישות שם הם "טיפול שוטף", "פגישה ראשונה", "ייעוץ טלפוני" —
ולכן ההתאמה הייתה טוקנים, המשתמשים (ענת שלו), פנקס המטופלים, אירועי הדמו
ולינק הזימון (`jampa.ai/book/anat`). שכבת המותג יושבת כ-`:root` שני מיד אחרי
ה-`:root` המקורי בתוך אותו `<style>`, כדי שהקובץ יישאר צמוד למקור.

### כללי JAMPA — מה **לא** קיים
- **אין מנהל תזרים.** יש מטפל אחד מול מטופל אחד. לכן ירדו: טאב "ביקשתי מ-HK"
  במשימות, וסלוט הכספים בפס — במקומו **שלב בתהליך**, בלי מספר, כסולם.
- **אין מספרים כספיים.** שום ₪, יתרה, תקציב או חריגה.
- **אין קבוצות וואטסאפ.** הודעות ישירות בלבד — עמודת הקבוצות של HK ירדה
  והשיחה תופסת את כל הרוחב.
- **אין מעקב מצב רוח.** מה שג'מפה אוספת הוא הודעות, לא מדידות; במקום גרף
  יש "מהשיחה השבוע" — מה שהמטופלת כתבה בין הפגישות.

### מיפוי מושגים
זיכרון החברה → פרופיל המטופל · כאבי לקוח → דפוסים · הקשר אישי ואמון → אמונות ליבה ·
סגנון תקשורת → סגנון עבודה · שביעות רצון → ברית טיפולית · מצב תזרימי → כלים שעובדים ·
אירועים מהותיים → נקודות מפנה · הכנה לפגישה → הבריף (60 שניות) · משוב ה-AI → סופרוויזיון.

הסיכום הלילי נשאר סיכום לילי בשם **הזיקוק הלילי** — הוא מזקק את השיחה אל הפרופיל.


## הסבת HK לשפת JAMPA — שכבת המשטחים והסרגל

**הפרדת הטוקן.** `--navy` שירת בו-זמנית טקסט (187 שימושים) ומשטחים (65).
הבהרה גלובלית שלו הייתה מחווירה כל כותרת במערכת. לכן נולד `--band`:
טוקן **למשטחים בלבד**, בעוד הטקסט נשאר על `Main` מהפיגמה. מוגדר בחמישה
מקומות בלבד — `css/base.css`, `docs/cli/cli.css`, `docs/adv3/brand.css`,
`calendar.html`, והפולבק ב-`css/theme-hk.css`. שינוי הצבע הוא חמש שורות.

**מלכודת שעלתה בדרך:** השם הראשון שנבחר היה `--surface`/`--surface2` —
ובקוד כבר היו 11 הצהרות `background:var(--surface2)` **מתות**, שנשענו על
טוקן שמעולם לא הוגדר ולכן נפלו לשקוף. עצם ההגדרה החייתה אותן וצבעה בכחול
סרגלי טאבים שהטקסט שבהם הפך לבלתי־קריא. השם שונה ל-`--band`, וההצהרות
המתות הושארו כפי שהיו. **לפני שמגדירים טוקן חדש — לבדוק שאיש לא מפנה אליו.**

**רצועת הכותרת.** גובה אחיד 60px בכל המסכים, ופינות מעוגלות 14px בארבע
הפינות. הביטול `html.embed .strip{border-radius:0}` שהיה בחמשת מסכי adv3
גבר על הכלל הכללי — ולכן בעצמאיים היו פינות ובהטמעה לא. הוא עודכן במקום
להימחק, כדי שהספציפיות תישאר מפורשת. JAMPA שומרת על הכלל המקורי (רצועה
מרובעת) — היא מוצר נפרד, לא נגזרת.

## סרגל בעל העסק — שלושה יעדים

לבעל העסק (`client1`/`clientN`) הסרגל בתוך החברה הוא **דשבורד · פגישות ·
תמונת תזרים** בלבד. מעקב ופערים, תזרים עתידי, תכנון חשבונאי ותשלומי ספקים
הם כלי העבודה של היועץ והמתפעל — בעל העסק צורך את התוצאה, לא מזין אותה.
קבוצת הוואטסאפ ירדה גם היא מהסרגל שלו (`SEC` מסמן אותה `1` = מוסתר ללקוח).
הדשבורד עצמו זהה לזה שהיועץ רואה עבור אותה חברה.

**"תזרים עבר" הפך ל"תמונת תזרים"** — בלקוח וביועץ כאחד. "עבר" לא אמר מה
יש שם, ו"דוח" נפסל כמילה. השם מתחבר ל"תמונת מצב" שכבר חיה במערכת.
מפתח הטאב נשאר `past`; הוחלף רק ה-label ב-`TAB_LABELS`, וסרגל הלקוח קורא
ממנו במקום ממחרוזת כפולה.

## תכנון חשבונאי — היררכיית הדגש התהפכה

ראש הטבלה (`.chead`) היה לבן והיתרות היו ארבעה כרטיסים גדולים מעליו — כלומר
המסך פתח בהקשר ולא בעבודה. ההיררכיה התהפכה:

- **ראש הטבלה נצבע בכהה** (`--band`), עם פינות עליונות שמתלכדות עם הכרטיס.
  הוא עוגן המסך — ממנו מגיעים לתצוגה, למיפוי ולמתג תזרימי/חשבונאי.
- **שורות הסה״כ הובהרו** (`#1B5C8A → #2C79A8`, ובתצוגה החשבונאית
  `#5049B4 → #6E68C6`) — כשראש הטבלה כהה, סה״כ באותה עוצמה מייצר שני עוגנים
  שמתחרים על העין.
- **היתרות התקפלו.** ברירת המחדל סגורה, והשורה הסגורה עדיין נושאת את שלושת
  המספרים (פתיחה · נוכחית · צפויה) — כך שהקיפול לא עולה במידע, רק במקום.
  פתיחה חושפת את ארבעת הכרטיסים עם הספארקליינים ויתרת הפתיחה הנערכת.
- **ארבעת סימוני השורות** (ממוצע 3 ח׳ · שונה ידנית · יעד הכנסה · יעד הוצאה)
  ישבו כלגנדה קבועה מתחת ליתרות. הם ירדו מהמסך ועברו לפאנל "תצוגה ופילוח"
  תחת "סימון שורות", לצד שאר בוררי התצוגה — היכולת נשמרה, השורה הקבועה ירדה.
  `איפוס פילטרים` שכבר היה שם ממשיך לאפס אותם.

## שמות — שני "פגישות" באותו ניווט

בסרגל הגלובלי של היועץ ובסרגל שבתוך החברה הופיע אותו שם. **הפריט הגלובלי
שונה ל"כל הפגישות"; הפריט שבתוך החברה נשאר "פגישות"** — בתוך חברה ההקשר כבר
נאמר (שם החברה יושב מעל הסרגל), ואילו הפריט הגלובלי הוא זה שנשא הקשר סמוי
של "כולם". זה בדיוק הטיפול שכבר ניתן ל"התיק שלי" מול הרשימה שמתחתיו,
ול"כל הלקוחות" בקישור החזרה.

**נקודת ההתראה על "פגישות"** בסרגל החברה ירדה — בהתאם לכלל שאין התראות על
פריטי הסרגל.

## שמות הדמו — היועצים

`אשכנזי ייעוץ עסקי` הוחלף ב-`שחר ייעוץ עסקי`, ואיתו `אילון אשכנזי → אילון שחר`
ו-`מיכל אשכנזי → מיכל דגן`. שם המשרד חזר בשמות היועצים ויצר רושם של כפילות.

## תכנון פיננסי — פישוט: ערך אחד בתא, ו-＋ שפותח חודש

*(המסך נקרא קודם "תכנון חשבונאי". השם לא היה מדויק — הוא מחזיק את שתי
השפות, התזרימית והחשבונאית, ולא רק את האחרונה.)*

**המצב הקודם:** כל חודש סגור נשא שלושה מספרים (בפועל · יעד · הפרש) ולכן היה
רחב מהעתידי (`--colw3` מול `--colw`) — כלומר המסך נפתח בצפיפות המרבית שלו,
לכל החודשים בבת אחת, גם כשלא בודקים סטיות. בורר "מה מוצג בתא" הציע ארבע
אפשרויות (mix · act · tgt · vs) — ארבע דרכים לומר את אותו דבר.

**עכשיו:** ברירת המחדל היא **ערך אחד בתא** (`DM='one'`) — חודש סגור נושא את
הבפועל, עתידי את המתוכנן, וכל החודשים באותו רוחב. **＋ בכותרת כל חודש שניתן
לפתוח** מרחיב אותו לשלושת המספרים. זו אותה חשיפה הדרגתית של השורה הנפתחת,
רק על ציר החודשים — נפתח מה שבודקים, לא הכל.

- `MEXP` (Set של חודשים פתוחים) + `wideM(mo)` הוא הפרדיקט היחיד. שישה מקומות
  שבנו רוחב תא (`' m3'`) קוראים ממנו, וכך הראש, השורות, הסה״כ, הנטו, הרווח
  והיתרה המתגלגלת נשארים מיושרים באותה עמודה.
- **גם החודש הרץ נפתח** (`canOpen = mo.k==='act' || mo.now`) — הבפועל שלו הוא
  "עד כה", והיעד הוא בסיס ההשוואה. `PLAN` לא נזרע לחודש הרץ, ולכן שורת הסה״כ
  קיבלה `!=null` — בלעדיו החיבור החזיר `NaN`.
- הבורר בן ארבע האפשרויות ירד. `tgt` ו-`vs` נשארו במנוע לקישורי ההדגמה.

**מה עוד ירד כמידע כפול:** `modeChip` ("תצוגה תזרימית" — המתג אומר זאת),
`pastChip` ("2 בפועל · 3 תחזית" — כל עמודה נושאת תגית סגור/מתוכנן),
`cheadSub` ("16 קטגוריות · 10 סעיפים" — המספרים יושבים על המתג). נשארו בפס
העליון רק ההתראה על מיפוי חסר ובורר הטווח.

**טווח החודשים** הפך מחצים ‹ › לדרופדאון של כל החלונות האפשריים
(13−WINN אפשרויות), בסגנון `.pop` שכבר קיים במסך — נסגר בלחיצה בחוץ וב-Escape,
בלי `<select>` מקומי. **המתג תזרימי/חשבונאי** היה `1fr 1fr 1fr` ברוחב 376px
מימי "מאוחד" שכבר לא קיים — צומצם ל-`1fr 1fr` וברוחב 248px.

## תכנון פיננסי — סבב שני: מה נשאר על המסך

**כל מה שירד היה מידע כפול או בורר שהתייתר:**

| מה | למה ירד |
|---|---|
| `modeChip` "תצוגה תזרימית" | המתג שמתחתיו אומר זאת |
| `pastChip` "2 בפועל · 3 תחזית" | כל עמודה נושאת תגית סגור/מתוכנן |
| `cheadSub` "16 קטגוריות · 10 סעיפים" | המספרים יושבים על המתג עצמו |
| "היררכיה · סעיף ← קטגוריה ← תיאור" | העמודה עצמה מראה את ההיררכיה |
| פאנל "תצוגה ופילוח" | עומק הפתיחה חי בשברון של כל שורה; ארבעת סימוני השורות ירדו מהמוצר |
| "טווח חודשים" (4/5/6) | נבלע בבחירת מ־/עד |
| הלשונית השלישית במתג | "מאוחד" כבר לא קיים, ונשאר `1fr 1fr 1fr` ברוחב 376px |

**טווח הדוח** — שני בוררי חודש (`מ־` / `עד`), כל אחד עם הדרופדאון שלו,
וכפתור **הפקה**. `PF`/`PT` הן הבחירה הממתינה; הטבלה לא זזה עד ההפקה, כך
שאפשר לבחור שני קצוות בלי שלוש בניות ביניים. בחירה "הפוכה" גוררת את הקצה
השני במקום להיחסם. הכפתור מודגש כשיש שינוי ממתין, אפור כשאין.

**שתי השפות באמת שונות.** `BOOKF` הכיל מקדמים של `1.0` בדיוק, ואז המעבר בין
התצוגות לא שינה כלום. כולם הורחקו ל-10%–25%, **ובכיוון אחיד בתוך כל קבוצה**
— בכיוונים מעורבים הסעיפים מקזזים זה את זה והסה״כ חוזר לאותו מספר. הפער
בפועל: הכנסות +16.3% · עלות המכר −19.8% · תפעוליות +15.1% · מימון +16.6%.
בנוסף `targetOf` מפצל עכשיו גם חודשים עתידיים — קודם התחזית הייתה זהה בשתיהן.

**המספרים מתגלגלים** במעבר בין השפות (`tweenFace`, 460ms, ease-out). עצי
ה-cf וה-acc אינם זהים — 16 קטגוריות מול 10 סעיפים — ולכן הזיווג לפי אינדקס
שורה עובד רק על `.gr.tot` ו-`.gr.pnl`, שהן היחידות שמתאימות 1:1. יש רשת
ביטחון ב-`setTimeout`: בלעדיה, לשונית שיורדת לרקע באמצע הטווין הייתה משאירה
את המספרים על ערך ביניים לצמיתות.

**טעינה · Excel / PDF** — כפתור מפורש בסרגל הטבלה, **קיים רק בשפה החשבונאית**
(בתזרימי הנתונים מגיעים מהבנק ואין מה למשוך). מציג שכבת טעינה על הטבלה,
ואחריה `rerender` וטוסט. תחילה זה היה אוטומטי במעבר לחשבונאי — וזה הסתיר
את גלגול המספרים במקום להוביל אליו.

**החודש הרץ נושא בפועל אמיתי.** קודם `actualOf` החזיר לו את אותו ערך כמו
היעד, ולכן ההפרש היה תמיד אפס — כלומר "החודש עוד לא הסתיים" לא נראה בשום
מקום. עכשיו `MTD` (חלק החודש שחלף) מוכפל במקדם דטרמיניסטי לפי המפתח,
**ומוחל רק על העלים** כדי שהסכומים יתגלגלו נכון. `actFor` נוסף כי `totFor`
עובר ליעד בכל מה שאינו חודש סגור, ושורת הסה״כ של חודש פתוח צריכה בפועל.

## תכנון פיננסי — סבב שלישי: המיפוי, הפיצול, והצירים

**המיפוי חזר להיות אחד־לאחד.** פיצול באחוזים ישב בשכבת המיפוי — כלל קבוע
שאומר "35% מהשכר הם הנהלה וכלליות". זה לא מה שקורה בפועל: מה שמתחלק הוא
**סכום של חודש מסוים**, ולכן הפיצול עבר למקום שבו הוא באמת מתרחש.

- מסך המיפוי הוא פעולה אחת: **גרירה**. הרשימה שמימין מחזיקה רק את מה שעוד
  לא שויך, מה שנגרר נעלם ממנה, וכשהיא מתרוקנת העמודה יורדת והסעיפים
  מקבלים את כל הרוחב (`.mw-grid.solo`). ביטול — ה-✕ שעל הקטגוריה בתוך הסעיף.
- הכותרות דביקות (`position:sticky`), כי גוררים לאורך רשימה ארוכה.
- **מיפוי ישן ששמור ב-localStorage מנורמל בטעינה** ליעד הראשון בלבד. בלי זה
  דפדפן שכבר ביקר במסך היה ממשיך להציג אחוזים מגרסה שכבר לא קיימת.

**הפיצול — `SPL[cfId][abs] = [[accId,סכום],[accId,סכום]]`.** בסכומים ולא
באחוזים. נפתח **רק בתזרימי, רק על בפועל של חודש סגור, רק על שורת קטגוריה**.
בוחרים סעיף שני, מזינים כמה מתוכו שייך אליו, והשארית נשארת בסעיף המקורי.
- `accsOf(cfId)` — לאילו סעיפים הקטגוריה מגיעה: המיפוי הקבוע ועוד כל יעד
  שנולד מפיצול. זה מה שגורם לקטגוריה להופיע תחת שני סעיפים.
- `shareAbs(cfId,accId,abs)` — כמה שייך לסעיף באותו חודש. `splitDesc` קורא
  ממנו, ולכן הפיצול מתגלגל מעצמו לתיאורים, לקטגוריות, לסעיפים ולסה״כ.
- תא שפוצל מסומן בקו תחתון סגול — אותה שפה כמו כל ערך שאדם נגע בו.

**"פתח הכל" עובד על שני הצירים** — השורות נפתחות לקטגוריות והחודשים הסגורים
נפתחים ל־בפועל · יעד · הפרש. יושב בתא הריק שמעל עמודת השמות, במקום תווית
"היררכיה" שרק חזרה על מה שהעמודה מראה.

**הרווח המיותר מעל כל מסך מוטמע** היה `.client-head` — רצועה בגובה 37px
שנשארה ריקה אחרי ששם החברה עלה לפס העליון ותג "פעיל" ירד. `tidyClientHead`
מכווץ אותה, **ורק מכווץ** — הוא לא מחזיר להצגה, כי שלושה מקומות אחרים כבר
קובעים מתי היא נראית ודריסה שלהם החזירה אותה גם איפה שלא צריך. הוא נקרא
מ-`showTab` אחרי השורה שקובעת `display` — היא זו שדרסה אותו קודם.

**עוד בסבב:** תג "פעיל" ירד מכותרת הלקוח (וגם `st` שנשאר בשימוש בשתי שורות
אחריו — זה הפיל את המסך בשקט); "פיננסים" ו"זיכרון לקוח" ירדו מהגדרות חברה;
`לירון בן כליפא` הוחלף ב-`טל מלקר` בכל הממשק; בוררי הטווח נדחפו לקצה הפס,
ושורת היתרות התאחדה איתם לשורה אחת.

## סבב רביעי — הפיצול ל-N, העוזר, והפס

**הפיצול תומך בכמה סעיפים.** `SPL[cfId][abs]` תמיד היה מערך, אבל העורך ידע
רק שניים. עכשיו מוסיפים סעיפים אחד־אחד, לכל אחד סכום משלו, **והסעיף שממנו
נפתחנו סופג את השארית** — ולכן הסכום תמיד מסתדר ואין מצב שגיאה שצריך להסביר.
הקלט חסום לתקרת מה שנשאר, כך שאי אפשר לחרוג מהסכום.

**העוזר AI בתמונת תזרים** קיבל נוכחות: ראש כהה (`--band`) כמו לכל פאנל אחר,
שלוש התובנות ככרטיסים לבנים על משטח אפור — הקו הצבעוני שכבר היה להן נשאר
אבל יש לו על מה לשבת — ושורת השאלה על משטח קורל משלה, כדי שתיקרא כפעולה.

**הפס הכחול בדשבורד** נשבר לשתי שורות גם כשהיה מקום: הסף היה `1399px` בעוד
המדידה מראה שהתוכן נכנס בשורה אחת מ-`1280px`. הסף תוקן לפי המדידה, הגובה
עלה ל-60px (כמו הרצועה), ו**מונה המשימות קיבל משטח משלו** — הוא העוגן של
הפס, ובלי משטח הוא נקרא כטקסט בין טקסטים.

**סדר מקטע התזרים בסרגל:** מעקב ופערים · תכנון פיננסי · תמונת תזרים — קודם
מגדירים ומתכננים, ואז מסתכלים על מה שקרה. תזרים עתידי ותשלומי ספקים הוצמדו
בסוף המקטע; קודם הם ישבו מפוזרים למרות ששניהם של מנהל התזרים בלבד.

## היום של היועץ — הקצב של יומן מנהל התזרים

המסך היה עמוס לא בגלל הפריסה אלא בגלל **מה שכל שורה נשאה**: שעה בגודל 22px,
שלוש שורות טקסט, כפתור בשורה נפרדת, ופאנל כהה פתוח בגובה 250px על הפגישה
הראשונה. היומן של מנהל התזרים נקרא נקי כי השורה שלו היא שעה · כותרת · שורת
מטא אחת — ותו לא.

הפרופורציות הועתקו משם (`.mc-item` ב-`css/portfolio.css`): `--axis` מ-96px
ל-52px, השעה מ-22px/500 ל-13px/700, `עד HH:MM` ירד (המנהל מציג רק התחלה),
שורת המטא ל-11.5px, והכפתור "הכנה לפגישה" הפך לצ׳יפ **בתוך** שורת המטא.
נוסף פס מוביל בן 3px לכל שורה — תכלת, קורל לפגישה הבאה, אפור למה שהסתיים —
וזה מה שגורם לרשימה להיקרא כיומן ולא כרשימת כרטיסים.

**הפאנל "מה נולד מהפגישה" התקפל לצ׳יפ** עם מונה (`4`). מלכודת: `hidden` לבדו
לא סגר אותו, כי `.born{display:grid}` גובר על כלל ברירת המחדל של הדפדפן —
נדרש `.born[hidden]{display:none}` מפורש.

**המשימות עברו לצד המוביל** (`grid-template-columns:430px 1fr` + `order:-1`),
ותשומת לב מתחתן. קודם הן היו משמאל; אצל המנהל הן מימין, וזה מה שיצר את
ההרגשה ש"אצלו יותר נקי".

**וביומן של המנהל** — שם החברה קיבל שדה משלו (`client`) וירד מהכותרת
ומהתת-שורה: `תפעול — אנרגי אינטרנשיונל` הפך ל-`תפעול` עם השם כשדה נפרד.

**סבב המשך — שלוש עמודות.** המסך התחלק ל**משימות · היומן · תשומת לב**: הצד
המוביל הוא מה שעלי לעשות, היומן נקרא מימינו, ותשומת לב — שהיא רקע ולא
פעולה — יושבת בקצה השני. פאנל המשימות אימץ את הקצב של הפאנל אצל מנהל
התזרים: כותרת בהירה במקום ראש כהה, רמז הגרירה מתחתיה, שורות דקות עם שם
הלקוח מתחת לכותרת, וההוספה ירדה לתחתית במקום לחסום את ראש הרשימה.

מלכודת גריד: `grid-column` לבדו לא הספיק. הפריסה האוטומטית זזה קדימה בלבד,
ומשהיומן תפס את עמודה 2 היא כבר לא חזרה לעמודה 1 — שתי העמודות נפלו לשורה
שנייה. נדרש `grid-row:1` מפורש על שלושתן.

**"פגישות היום" הפך ל"היומן שלי"** — העמודה האמצעית אינה רשימת פגישות אלא
היום כולו, והקישור עודכן ל"היומן המלא" בהתאם.


## היום של היועץ — כרטיס אחד, כמו אצל מנהל התזרים

הניסיון הראשון היה שלושה כרטיסים נפרדים זה לצד זה, וזה לא מה שקורה אצל
מנהל התזרים: שם יש **כרטיס אחד** שבתוכו שתי עמודות — `aside.mcal-side`
(המשימות) ו-`.mcal-main` (הציר) — חולקות מסגרת אחת וכותרת אחת.

המבנה הועתק: `section.panel.cal` מחזיק את הכותרת ואת `.calwrap`, שבתוכו
`aside.calside` (משימות, רקע `#FAFCFE` וקו מפריד) ו-`.calmain` (הציר).
תשומת לב נשארה כרטיס נפרד בקצה השני. `.wrap` הוא שתי עמודות, לא שלוש.

## הדשבורד של בעל העסק — פס אחד ופיננסים

שורת ה-KPI, כרטיסי השירות (פגישה · דוחות · מסמכים · משימות · תפעול), שורת
"מלווה על ידי" וכפתור הרענון — כולם ירדו. במקומם **פס כהה אחד** עם הפגישה
הבאה (`#cliMeetBar`), ומתחתיו רק הווידג'טים הפיננסיים.

- `renderClientRow` יוצא מוקדם לתפקידי לקוח ומצייר במקומו את הפס.
- `renderBoard` הרחיב את התנאי שהיה למתפעל בלבד (`_noKpi`) — גם בעל העסק
  מקבל רק את הווידג'טים הגדולים.
- מלכודת: הסתרת `#wboardTop` ב-`applyDashView` לא הספיקה, כי `renderBoard`
  רץ אחריה ומחזיר לו `display`. התיקון חייב לשבת בתוך `renderBoard`.

**העוזר הפיננסי** קיבל שאלות על המספרים במקום על המערכת: מגמת ההכנסות ·
מתי צפויה חריגה · היתרה בעוד שבועיים · כמה נקנה מסונול. אותו טיפול בעוזר
של "תמונת תזרים" ב-`past-cashflow.html`, שקיבל גם את המראה של מסכי היועץ.

## היום של היועץ — המבנה של מנהל התזרים, לא התאמה שלו

ארבעה סבבים בוזבזו על התאמת המסך הקיים. מה שעבד בסוף: **להעתיק את המבנה**.
`.mc-item` בפלקס — `שעה · צ׳קבוקס · כותרת + שורת מטא`, עם `border-inline-start`
בן 3px לפי סוג — וה-CSS הועתק כלשונו מ-`css/portfolio.css`. השורות ירדו
מ-123px ל-62px, ו-27 שורות CSS של `.mt` נמחקו.

**המסכים נמתחים עד התחתית.** שתי מלכודות שונות:
1. מסך מוטמע מדווח את גובהו למעטפת, ולכן `min-height` בתוכו מנפח את עצמו
   בלולאה (נמדד: 1203 → 1323 ועולה). הסיבה הספציפית: `header.strip` יושב
   **מחוץ** ל-`.wrap`, ולכן הגובה המדווח יצא גדול ב-60px בכל סבב. הפתרון:
   `body` פלקס-עמודה, `.wrap{flex:1}` — הסכום שווה בדיוק לגובה המסגרת,
   וזו נקודת שבת. המעטפת קובעת `max(מדווח, מה שנשאר על המסך)`.
2. `#viewCal{height:calc(100vh - 150px)}` — מספר קסם שלא תואם את המיקום
   בפועל. `fitTall()` גוזר את הגובה מ-`getBoundingClientRect().top`.

## בעל העסק — אותם מסכים, בלי מה ששייך ליועץ

`useCli` נפתח לבעל העסק ל-`meetings` ול-`past` בלבד: **אותו מסך של היועץ**,
לא כפילות. הדשבורד נשאר שלו. המסגרת נטענת עם `&r=cli`, ומכאן:

- `docs/cli/cli.js` מוסיף `html.r-cli`.
- **פגישות:** רק "פגישות שהתקיימו", ובזירה רק "סיכום הפגישה" ו"משימות".
  זיכרון, משוב, תמלול, ההכנה החיה והגדרת ההכנה — כלים של היועץ, ויורדים.
- **תמונת תזרים:** אותו מסך, עם העוזר שכבר עוצב.

## העוזר של בעל העסק — מסך, לא בועה

הבועה הצפה ("העוזר הפיננסי") ירדה. במקומה בעל העסק מקבל את **מסך העוזר של
היועץ** (`docs/cli/ai.html`) — עם היסטוריית שיחות, שיחה קולית ו"שיחה חדשה".
בועה צפה לצד מסך שלם היא שתי דלתות לאותו דבר.

אותו מסך, אותה מכונה — **רק השאלות והפנייה משתנות** (`html.r-cli`):

| | היועץ | בעל העסק |
|---|---|---|
| כותרת | מה תרצה לדעת על אנרגי אינטרנשיונל? | מה תרצה לדעת על העסק שלך? |
| קבוצות | מספרים · החברה והאנשים | הכסף שלי · מה קורה מול HK |
| היסטוריה | `HIST_ADV` — חמש שיחות של יועץ | `HIST_CLI` — שלוש שיחות שלו |
| סרגל | שיחות קודמות · אנרגי | השיחות שלי |

התשובות (`T.*`) משותפות: אותם מספרים, שאלה אחרת מובילה אליהם.

**במסך הפגישות** (`r-cli`): רק פגישות שהתקיימו, רק "סיכום" ו"משימות", בלי
נגן ההקלטה ובלי צינור העיבוד (הוקלטה ← תמלול ← זיכרון עודכן) — הוא פנימי
ל-HK. "ממתין לאישורך" הוא ניסוח של היועץ; אצל הלקוח זה **"סיכום מוכן"**
ו**"הסיכום נשלח אליך"**, ובמקום כפתורי האישור — הורדת הסיכום. הפגישה
האחרונה נבחרת בכניסה, כי מסך ריק אינו מצב פתיחה.

## הוואטסאפ של בעל העסק — שיחה אחת, ערוץ מזוהה

בעל העסק מדבר עם העוזר גם בוואטסאפ, ו**זו תמיד אותה שיחה** — לא שרשור לכל
נושא. היא יושבת ראשונה ברשימת השיחות, מסומנת בסמל וואטסאפ ירוק, ונפתחת
כשרשור אמיתי: ראש ירוק-כהה, רקע בז׳ מנוקד, בועות ירוקות יוצאות עם ✓✓ מול
בועות לבנות נכנסות, ומפרידי יום. הערוץ צריך להיות מזוהה במבט, לא בטקסט.

**מלכודת:** המחלקות `.ai` ו-`.me` כבר תפוסות במסך (`.ai` נושאת
`min-height:calc(100vh - …)`), ולכן בועות עם `class="wa-b ai"` ניפחו את עצמן
ל-467px כל אחת. שונו ל-`.in`/`.out`.

## הלקוח לא מכיר את HK

המוצר מול בעל העסק הוא **לבן־תווית**: מולו עומדים היועץ ומשרד הייעוץ, לא HK.
לכן בכל מה שהלקוח רואה — "מה קורה מול HK" הפך ל"הליווי שלי", "מה HK עשתה
עבורי" ל"מה נעשה עבורי", ו"יועץ HK" בפס הפגישה לשם המשרד (`c.firm`).
זה כלל רוחבי, לא תיקון נקודתי: כל מחרוזת חדשה בממשק הלקוח צריכה לעבור בו.

## קליטת מסמכים — השער בין הנהלת החשבונות לתזרים

מסך חדש ליועץ (`docs/cli/intake.html`), שנולד מ"תשלומי ספקים ולקוחות" שירד
מהסרגל. הוא לא טבלת הזנה — הוא **תור קליטה ואישור**: כל חשבונית שנפתחת
בהנהלת החשבונות נקלטת אליו אוטומטית, ורק אחרי אישור אנושי היא נכנסת לתזרים.

**המסך עונה על שלוש שאלות, בסדר הזה:**

1. **מה נכנס ומאיפה** — מקור האוטומציה הוא **עמודה, לא הערה**: שם המערכת
   ושעת הקליטה על כל שורה. המקורות הם אלה שכבר מוגדרים לחברה במערכת —
   **חשבשבת** (הנה״ח של אנרגי, רו״ח מיכל שגב), **Bizibox** (בנקים וכרטיסים)
   ו**קבוצת הוואטסאפ** — ולא ספקים שהומצאו.
2. **מי עבר על זה** — טל מלקר יושבת בראש המסך ("עוברת על כל שורה"), וכל
   שורה מאושרת נושאת את שמה ואת השעה. פס הזרימה בראש הוא הסיפור בשלושה
   מספרים: **נקלטו היום → ממתינים לאישור → אושרו ונכנסו לתזרים**.
3. **מתי זה ייפול על התזרים** — הציר משמאל נשאר, אבל הוא לא מודד יותר פערי
   גבייה: הוא מודד את המרחק שבין "נקלט" ל"בתזרים". כל צומת הוא תאריך תשלום,
   והבר שלו מפוצל — **כחול מלא = אושר, קורל מקווקו = ממתין**. מה שלא אושר
   מצויר מקווקו כי הוא באמת לא בתזרים.

**שני מצבי "נעצר לבדיקה"** — הם הסיבה שהאישור אנושי ולא כפתור:
- **חשבונית עסקה** אינה אירוע מס. היא נכנסת לתזרים רק כשמופקת ממנה חשבונית
  מס או קבלה — ולכן היא נעצרת, גם כשיש לה סכום ותאריך.
- **חשבונית זיכוי** מול חיוב שכבר בתזרים — צריך לוודא שהחיוב לא יורד פעמיים.

שניהם יושבים בטבלה עם ההסבר, ו**לא על הציר** — כי הם לא אמורים ליפול על
התזרים בשום תאריך עד שיוכרעו.

**מיקום בסרגל:** ראשון במקטע התזרים. אין טעם להסתכל על מעקב, תכנון או תמונה
לפני שיודעים מה עוד ממתין ולא נכנס. **מנהלת התזרים לא הועברה** — היא נשארת
עם "ניהול חומר" שלה (`js/ops.js`), שהוא מסך עבודה ולא מסך בקרה.

**מלכודת חוזרת:** `.bar` הוא הפס הכחול של הדשבורד ב-`cli.css`. בר בציר
שנקרא `.bar` ירש ממנו `min-height` ורקע כהה. כל מסך חדש שנשען על `cli.css`
צריך לבדוק את שמות המחלקות הגנריות מולו — `bar`, `ok`, `st`, `lb`, `d`.

## המקורות: מה נכנס, ומה הוא היעד

שני תיקונים שקיבלנו על המסך, ושניהם עקרוניים:

- **Bizibox אינו מקור נתונים — הוא היעד.** מכניסים אליו. במסך הוא מופיע רק
  בשורת "אחרי האישור — נכתב ל-Bizibox ונכנס לתזרים".
- **קבוצת הוואטסאפ אינה אוטומציה.** מסמך שנשלח שם מוזן **ידנית**. לכן מקור
  השורה הוא אחד משניים: מערכת מחוברת, או `הזנה ידנית`.

**הציר אנכי משמאל ומתחיל בראש העמודה.** ניסיון לפרוס אותו אופקית במקום הפס
נפסל: כל צומת נושא ארבעה נתונים, וזה קריא רק אנכית.

**הפס העליון** נבנה מחדש בשפה של שאר המסכים — `.strip` על `--band`, גובה 60,
פינות 14 — עם השם והמספרים בשורה אחת: נקלטו היום · ממתינים לאישור · נעצרו
לבדיקה · אושרו ונכנסו, ובקצה "אחרי האישור — נכתב ל-Bizibox". הגרסה הראשונה
שלו הייתה שלוש עמודות בגובה 60+ והרגישה כמו כרטיס, לא כמו רצועה.

**כפתור "אוטומציה חדשה"** פותח קטלוג של המערכות שאפשר להתחבר אליהן, עם
הלוגו האמיתי של כל אחת (הורדו מאתרי החברות ל-`docs/cli/logos/`):
חשבונית ירוקה (morning) · חשבשבת (H-ERP) · קארדקום · מאנדיי · פיימנט · רווחית.
ארבע הראשונות מסומנות "מחוברת"; לשתיים האחרות יש כפתור "חיבור" שמדגים את
המעבר. השורה בתחתית אומרת את הכלל: **מה שלא מחובר, מוזן ידנית**.

## הפס הלבן בתחתית — הפעם השורש האמיתי

הבעיה חזרה שוב ושוב כי לא היא שהייתה שבורה אלא **המדידה לא רצה**. שלוש
תגליות, בסדר:

1. `.cliframe{min-height:calc(100vh - 165px)}` — מספר קסם שלא תואם את
   המיקום בפועל של המסגרת.
2. השמירה שכתבתי בעצמי חסמה את ההשמה הראשונה:
   `parseFloat('')` הוא `NaN`, וכל השוואה איתו מחזירה `false`.
3. **והשורש:** הענף של מסכי `docs/cli` ב-`showTab` עושה `return` מוקדם
   ולעולם לא מגיע לשורת המדידה שבסוף הפונקציה. הקריאה הוזזה לתוך הענף.

מסקנה לפעם הבאה: כשתיקון "לא תופס", לבדוק קודם **שהקוד בכלל רץ** (מונה או
לוג) לפני ששוברים את הראש על הלוגיקה שלו.

**הפס העליון ירד בסוף.** נבנה, נשקל, והוסר: הוא הציג את אותם מספרים שהסינון
בטבלה כבר מציג. הסיכום ("כמה ממתין · כמה עוד לא בתזרים · כמה נעצר") נשאר
בכותרת הציר, ובמקום הפס **ראש הטבלה נצבע כהה** — אותה שפה של ראש הטבלה
בתכנון פיננסי, עם צ׳יפי סינון לבנים על הכהה.

## הסרגל בתוך חברה — ארבע קבוצות במקום אחת

היו שלוש-עשרה שורות וכותרת קבוצה אחת ("תזרים"), כך ש"תזרים" נראה כחריג ולא
כחלוקה. הקו שלפיו חולק: **מה שקורה עכשיו ודורש תגובה · מה שנצבר ואפשר לשאול ·
המספרים · איך החברה מוגדרת אצלנו**.

- **דשבורד** — לבדו בראש. הוא הבית, לא חבר בקבוצה.
- **שוטף** — קבוצת הוואטסאפ · פגישות
- **ידע על החברה** — זיכרון החברה · עוזר AI · מדדים
- **תזרים** — קליטת מסמכים · מעקב ופערים · תכנון פיננסי · תמונת תזרים
  (ולמנהלת התזרים גם תזרים עתידי ותשלומי ספקים)
- **ניהול** — התהליך שלי · הגדרות חברה

לבעל העסק אין כותרות בכלל — שלושה יעדים לא צריכים חלוקה.

## תנאי תשלום לפי ספק — מה שהופך קליטה לתזרים

עד עכשיו המועד בכל שורה היה נתון קבוע, ואיש לא ידע מאיפה הוא בא. עכשיו הוא
**נגזר**: לכל ספק ולקוח תנאי תשלום משלו, ו-`dueOf(d)` מחשב מתאריך החשבונית
ומהתנאי — `שוטף+N` נספר מסוף חודש החשבונית, `מיידי` מהתאריך עצמו. מתחת לכל
תאריך יושב הצ׳יפ שאומר לפי איזה כלל הוא נקבע, והוא לחיץ.

תנאי תשלום שייכים ל**חשבונית מס בלבד**: קבלה היא כסף שכבר זז, חשבונית עסקה
אינה אירוע מס, וזיכוי הולך למועד של החשבונית שהוא מבטל.

**המצב `noterm` נגזר ולא מוזן.** מסמך של ספק שאין לו תנאי אינו "ממתין
לאישור" — אין לו בכלל מועד לאשר, ולכן הוא נעצר, והפעולה בשורה היא *הגדרת
תנאי תשלום* ולא *אישור*. סונול נשארת בכוונה בלי תנאי כדי שיראו את זה קורה:
בוחרים שוטף+30, וברגע אחד השורה מקבלת תאריך, נכנסת לתור האישור, ומטפסת על
הציר. זה הרגע שמסביר למה בן אדם צריך לעבור על התור.

**הפופאפ** מחלק ללקוחות (מתי הכסף נכנס) וספקים (מתי הכסף יוצא), התנאים הם
צ׳יפים ולא `select`, ומי שנפתח מתוך שורה מודגש וגולל לאמצע. הכפתור בסרגל
העליון נושא ספירה קורל של הצדדים בלי תנאי.

**נוקו 65 שורות CSS כפולות** — קטלוג האוטומציות והציר הופיעו פעמיים מניתוחים
קודמים. הבלוק השני היה חסר `.ax-w` ומיותר לחלוטין.

## פירוק סעיף חשבונאי לסעיפי משנה

הלקוחות ביקשו לפרק קטגוריה בדוח החשבונאי לכמה סעיפי משנה, כדי לתת לכל אחד
יעד בנפרד. הדבר היחיד שאסור היה להישבר: **הסה״כ של הסעיף**.

**הפתרון: לא להמציא רמה, אלא לקבץ מחדש את מה שכבר קיים.** כל סעיף חשבונאי
מורכב מתיאורים (רמה 3, שהייתה בנויה במנוע אך מוסתרת). הפירוק רק מחליט איך
לקבץ אותם. כל תיאור שייך לסעיף משנה אחד בדיוק, ולכן הסכום זהה לשקל —
נבדק בבדיקה אוטומטית: ערכי הסעיף והקבוצה לפני ואחרי הפירוק זהים.

**שני עצים מאותם עלים.** `n1.kids` הם סעיפי המשנה ו-`n1.feeders` הן קטגוריות
התזרים; `kidsFor(node,vw)` בוחר ביניהם. התצוגה החשבונאית פותחת לסעיפי משנה,
התזרימית פותחת לקטגוריות תזרים — אותם עלים, אותו סכום, שתי שפות. החישוב
עצמו רץ תמיד על `kids`, כך שיש מסלול מתמטי אחד.

**המספרים בדיאלוג הם המספרים בדוח.** הם חשבונאיים (`×BOOKF`) ולפי החודש
הסגור האחרון, ולכן סכום סעיפי המשנה בדיאלוג שווה בדיוק לשורה בדוח.

`splitDesc` הפך ל-`splitList(cfId,accId,ds,abs)` שעובד על רשימת תיאורים ולא
על צומת — מאז שיש סעיפי משנה, תיאורים של קטגוריה אחת יכולים לשבת תחת הורים
שונים. `baseAbs`/`booksBase` מזהים עלה לפי `node.d` ולא לפי `lvl===3`.

בשמירה נמחק יעד שנקבע פעם על הסעיף עצמו — הוא היה מבטל את סכום סעיפי המשנה.

## תכנון תזרימי, והפס הקבוע של בעל העסק

**"תזרים עתידי" נעשה "תכנון תזרימי"** ונכנס לסרגל של בעל העסק. השם החדש
מזווג אותו ל"תכנון פיננסי", ולכן שניהם יושבים צמודים — ותמונת תזרים
אחריהם: קודם מתכננים, אחר כך מסתכלים על מה שקרה. כותרת המסך עצמו שונתה
גם היא, אחרת פריט בשם "תכנון תזרימי" היה מוביל למסך ששמו "דו״ח תזרים
חודשי". ליועץ הוא עדיין לא בסרגל — הוא צורך את התוצאה ולא מזין אותה.
בדרך אגב הכותרת **תזרים** חלה עכשיו גם על בעל העסק: יש לו שם שני פריטים,
ולפני כן היה לו פריט בודד בלי כותרת.

## הפס הקבוע בדשבורד בעל העסק

ארבעה דברים שהוא צריך בלי ללחוץ, בפס אחד: **יתרה נוכחית · חריגה צפויה**
על רקע נייבי מימין, **המשימות שלי · העלאת מסמכים** על לבן משמאל. החלוקה
היא בין *מה המצב* לבין *מה אני עושה*.

המספרים אינם חדשים — היתרה מ-`BAL` לפי חברה והחריגה מהתחזית המאוחדת,
אותם מספרים שהיו בווידג'טים שירדו. המשימות והמסמכים מחוברים ל-`CLIENT_TASKS`
ול-`CLIENT_DOCS` הקיימים: משימה חדשה נכנסת לרשימה עם סטטוס "נשלחה" ועוברת
ל"בטיפול", וקובץ שמועלה עובר "בקיטלוג AI" → "אצל המתפעל". `refreshHero()`
מרענן את שתי הרשימות בלבד, כדי לא לצייר מחדש את הפס באמצע גרירה.

**המלכודת:** `renderCliHero`/`renderCliMeetBar` בודקים את `CUR_TAB`, אבל הם
נקראו מ-`renderClientRow` שרץ ב-`setScope` ולא ב-`showTab` — ולכן הפסים
נעלמו בטאב אחר ולא חזרו. הקריאה עברה לראש `showTab`, מיד אחרי `CUR_TAB=t`
ולפני הענפים שיוצאים ב-return.

**תכנון תזרימי אצל בעל העסק — קריאה בלבד.** המסגרת נטענת עם `&r=cli`
(ונטענת מחדש בהחלפת תפקיד, כמו מסכי docs/cli), והמסך מוסיף `html.r-cli`.
מה שיורד: עורך התא, "טען מחדש", הרמז "✎ ניתן לעריכה", יתרת הפתיחה
(`readOnly`), כפתורי ההסתרה מהחישוב, סיווג התיאורים ופס הסימולציה.
מה שנשאר: הטווח, מתגי היעד-מול-בפועל וההורדה — אלה תצוגה, לא עריכה.
העיקרון: לא מושבת ואפור אלא לא קיים. התכנון נעשה מולו, לא על ידו.

## תקשורת — הקבוצה, הטלפון והפגישות

הכותרת "שוטף" בסרגל נעשתה **תקשורת**, ותחתיה שלושה ערוצים: קבוצת הוואטסאפ,
**שיחות טלפון** (מסך חדש) ופגישות. הטלפונים ניסו קודם כטאב בתוך מסך
הוואטסאפ — זה היה לא נכון: הם ערוץ בפני עצמו, ומקומם בסרגל.

**שיחה מקבלת את אותו תוצר כמו פגישה.** רשימה מימין (מסונן נכנסות/יוצאות/
לא נענו, מקובץ לפי יום), והשיחה שנבחרה משמאל: מי מול מי, כיוון, נגן הקלטה,
ושלושה טאבים — סיכום · תמלול · משימות. התמלול מוסתר מבעל העסק, אותו כלל
שכבר חל בפגישות.

**שיחה שלא נענתה אינה שיחה.** אין לה הקלטה, אין תמלול ואין ממה לסכם — ולכן
היא לא מקבלת את הטאבים בכלל. מה שיש לה: מי חיפש, כמה ניסיונות, והאם הוחזרה.
אם הוחזרה — קישור לשיחה שיצאה ממנה; אם לא — כמה ימים עברו, וכפתורי חיוג
והודעה. הסיכום למעלה סופר רק את מי שלא הוחזרה.

**שיוך מספר לנציג.** גלגל השיניים בראש הרשימה פותח את מספרי הטלפון של
החברה: לכל מספר — כמה שיחות, האחרונה מתי, ולמי הוא משויך. השיוך הוא צ׳יפ
מהנציגים הקיימים, או **＋ נציג חדש** שמוסיף שם ותפקיד ומשייך באותה פעולה.
מספר שאינו משויך נשאר "לא מזוהה", מסומן בכתום ברשימה, ומופיע כמונה על
הכפתור. `054-771-2280` נשאר בכוונה לא משויך — זה אותו מספר ששולח הצעת
מחיר בקבוצת הוואטסאפ ואיש לא יודע מי הוא; מי ששויך כאן מזוהה בשני
הערוצים. השיוך רטרואקטיבי: כל השיחות מהמספר נרשמות מיד על שם הנציג.

**שלוש מלכודות בסבב אחד:**
1. `hkChrome()` לא נקרא — כל מסך docs/cli חייב לקרוא לו בעצמו. בלעדיו אין
   `embed`, אין `r-cli`, ואין דיווח גובה למעטפת.
2. `margin:0 auto` על פריט בתוך `flex-column` **מצמצם אותו לרוחב התוכן**
   במקום למתוח — המסך יצא צר ב-300px. הפתרון: `width:100%`.
3. `hidden` על אלמנט עם `display:grid` אינו מסתיר. אותה מלכודת כמו ב-`.born`.

## מספרי טלפון — שתי רמות, שתי שאלות שונות

**באיזור הניהול של HK** (`adminScreens/phones.html`): מספר טלפון · נציג ·
חברת ייעוץ. זו הרמה הפלטפורמית — של מי המספר ובאיזו חברת ייעוץ הוא יושב.
בחירת נציג **גוררת את חברת הייעוץ שלו**, ובחירת חברה **מנקה נציג שאינו
שייך לה** — אחרת נוצרות שורות סותרות. הדרופדאונים הם רכיב משלנו ולא
`select` של הדפדפן.

**במסך היועץ** (`פגישות ושיחות` ← טאב `שיחות טלפון לא מזוהות`): לא של מי
המספר, אלא **על איזו חברה דיברו**. שני מצבים שבהם המערכת לא מכריעה לבדה:
המספר אינו מוכר לאף לקוח, או שהוא מוכר אבל הנציג יושב בשתי חברות — אותו
בעלים, שני עסקים. עד שהשיחה משויכת היא לא נכנסת לזיכרון של אף חברה.

מעל הרשימה יושב **סיכום השבוע**: כמה שיחות נקלטו, כמה שעות, כמה ממתינות,
ומי הכי דיבר — עם מד שמראה מה זוהה לבד מול מה שנשאר. הרשימה למטה היא
החריגים, והסיכום הוא מה שהם חריגים ממנו.

## קליטת הכרטסת — הארכיטקטורה שהוחלטה

**ה-AI ממפה, הקוד סופר.** כל רו״ח מוציא אקסל אחר, ולכן אין פרסינג קשיח:
המודל מקבל כותרות ושורות לדוגמה ומחזיר מפה (איזו עמודה היא מה, אילו שורות
לדלג); הסיכום של אלפי התנועות רץ בקוד לפי המפה — כסף לא סופרים במודל.
סיווג הכרטיסים (תוצאתי/מאזני, לאיזה סעיף) — קריאת מודל נוספת, וכל החלטה
שלו ניתנת לתיקון בסדנת המיפוי. עלות: אגורות ל-Excel, ~1 ₪ ל-PDF דיגיטלי.

**החלטת מוצר: סריקות לא נקלטות.** ב-PDF סרוק המודל מחלץ ספרות מתמונה
וטעות אפשרית. ההבחנה דטרמיניסטית — יש שכבת טקסט או אין — ונבדקת בקוד
לפני שהקובץ מגיע למודל. סריקה נדחית בשער עם הנחיה לבקש מרו״ח Excel או
PDF מהמערכת. ב-PDF דיגיטלי נשארת בדיקת ביקורת: חובה=זכות, והסכומים מול
שורות הסה״כ המודפסות; מה שלא מתאזן מסומן "לא אומת" וממתין לעין אנושית.

**כפתור ↺ (החזרה לסיווג של רו״ח) פותח אזהרה** — הכרטיס, מאיפה לאן, ומה
המשמעות; אם המקור מאזני, אזהרה כתומה שהכרטיס ייצא מהרו״ה. "טעינת כרטסת"
בדמו מציג את שלבי הצינור אחד-אחד.

## זיכרון החברה — עיצוב שקט והגדרות פר-יוזר

**הכחול הכהה ירד מכותרות הקטגוריות.** זיכרון נקרא כמו פנקס, לא כמו לוח
בקרה: כותרת בטקסט נייבי על לבן, ונקודת צבע קטנה שאומרת של מי הידיעה —
כחול לעסק, סגול לאדם. הכהה במסך הזה שמור לפריט הפעיל בסרגל בלבד.

**הגדרות פר-יוזר חוברו למסך.** גלגל הגדרות על צ׳יפ היוזר הפעיל פותח את
המסך שכבר היה קיים בהגדרות (`openUserSet`): שם, תפקיד, סוג, ורשימת
קטגוריות עם צ׳קבוקס — כיבוי קטגוריה מעלים אותה מהזיכרון של אותו יוזר.
הבאג שתוקן: `renderCoMem` בכלל לא סינן לפי `u.off` — הכיבוי עבד רק
בכרטיס הישן. עכשיו הוא חל, ומוצגת שורת "N קטגוריות כבויות אצל X —
להגדרות". הסגירה חוזרת למסך שממנו נפתחה (viewMem או הכרטיס).

## פאנל התפעול — "תזרים" נעשה "כספים", עם שלושה טאבים

בתפעול צריך את שלושת הזמנים: **פעולות אחרונות** — תנועות הבנק שנקלטו,
מקובצות לפי יום עם יתרה בסוף כל יום, לאימות מול המסמכים שבתפעול;
**תזרים צפוי** — הווידג'ט הקיים (ברירת המחדל, לא שיניתי התנהגות);
**אשראי צפוי** — החיובים שהצטברו בכל כרטיס, מתי הם יורדים וכמה, עם
פירוט — כי הם יורדים מהעו״ש בבת אחת וכבר משוקללים בתזרים הצפוי.
המימוש: `opsFinHtml()` ב-ops.js, מצב `_opsFinTab`, סגנונות `fin-*`.

## תפעול יומי — מסך העל של התפעול (Super Admin)

`adminScreens/daily-ops.html`, באיזור הניהול, ומופיע **רק בתצוגת מנהל
התזרים** (`mgrOnly` ב-ADMIN_SCREENS — הסינון ב-renderAdmRail וגם
ב-admGo). בורר תקופה משלנו — יום / טווח תאריכים / חודש עם דפדוף, בלי
פקדי דפדפן — וכל הקוביות מגיבות לו. הנתונים דטרמיניסטיים לפי
(חברה, תקופה), כך שדפדוף מחליף מספרים.

שלוש הקוביות הראשונות, ולידן placeholder מקווקו כי המסך בנוי להתרחב:
1. **נמחקו בגלל חוסר עדכון** — לא צפויות שנמחקו כי החברה לא הזינה צפי;
   כמות, כמה ₪ יצאו מהתזרים בדיעבד, ואדום מעל סף.
2. **הכי הרבה נגררות** — כמה נגררות עמדו, וכמה ימים הוותיקה.
3. **הכי הרבה זמן תפעול** — מהזמן שנמדד אוטומטית במצב תפעול; ביום —
   דקות, בטווח/חודש — שעות עם ממוצע לתפעול.
4. **הכי הרבה חריגות בפועל** — ביום: מי מתחת למסגרת וכמה עמוק; בתקופה:
   ימי חריגה, עם העמוקה ביותר.
5. **יומן החריגות** — התיעוד: "הודעה ללקוח"/"התעלם" על חריגה בסיום
   התפעול נכתבים ל-`hkOvLog` (מ-ops.js) ומוצגים כאן — מי החליט, מה, מתי.
   יומן ולא רשימת מטלות: מה שנרשם כבר הוכרע.

## מסך סיום התפעול — התיעוד קודם קורא, ואז כותב

התיעוד עבר לעמודה השמאלית מעל שורת האזהרה, שם יש מקום; בתחתית העמודה
הימנית הוא נדחק אחרי רשימת חריגות ארוכה. הוא נושא **תאריך ולא רק שעה**
— בלי תאריך אי אפשר לקרוא אותו אחורה, וזו כל מטרתו.

**מבנה אחד של שתי עמודות, כל דבר מיושר לעמודה שלו.** האזור העליון
הנפרד בוטל; פס השלבים הממוספר ירד כי חזר על טבלת «שלבי הבדיקה».

- **ימין:** תיעוד השיחה (רוחב מלא) ומתחתיו רשימת החריגות — קוראים את
  ההיסטוריה, ואז מחליטים על כל חריגה.
- **שמאל:** קופסת סיכום עם שתי הטבלאות — שלבי העבודה ושלבי הבדיקה —
  ומתחתיה האזהרה וכפתורי השליחה. מה שנעשה ומה שנבדק, בדיוק מעל
  הכפתור ששולח את התוצאה.

**ההיסטוריה מוצגת לפני שדה ההזנה.** מנהלת התזרים צריכה לדעת מתי דיברו
ומה נאמר לפני שהיא מחליטה מה לתעד היום: שורת פתיחה "שיחה אחרונה לפני
12 ימים · 20.08.2026" (כתומה מעל שבועיים), ומתחתיה התיעודים הקודמים
מהחדש לישן. מה שנרשם עכשיו מסומן בכחול וניתן למחיקה; היסטוריה אינה
נמחקת. `FIN_CALL_HIST` מחזיק את התיעודים מתפעולים קודמים.

**"סיום תפעול" נוחת ישר על המסך** — חלון רענון ה-raw data חדל להיות שער
חובה; הוא נשאר זמין מהכפתור שלו.

## שיחה היא פגישה קצרה — אותה ישות, סימון שונה

לשיחת טלפון יש בדיוק אותם תוצרים כמו לפגישה: סיכום · משימות · זיכרון ·
משוב · תמלול. לכן היא לא מסך נפרד אלא **אותה ישות ברשימה ובזירה**, עם
`kind:'call'` ב-ARENA.

ההבחנה ויזואלית בלבד ומשולשת: **אייקון** (טלפון סגול מול יומן אפור),
**פס צד סגול** על השורה, ו**שם הזירה** — "זירת השיחה" ולא "זירת הפגישה",
והטאב הראשון "סיכום השיחה". שאר חמשת הטאבים זהים לחלוטין.

הפגישות קיבלו אייקון יומן משלהן — הבחנה עובדת רק כששני הצדדים מסומנים.


---

## תקשורת — מסך אחד לשלושה סוגי אינטראקציה

`docs/adv3/meetings.html` — היה "פגישות ושיחות", עכשיו **תקשורת**. באותה
רשימה ובאותה זירה חיים שלושה סוגים:

| ערוץ | `kind` | תוצרים | מזהה במבט |
|---|---|---|---|
| פגישה | `meet` (ברירת מחדל) | סיכום · משימות · זיכרון · משוב · תמלול | צ׳יפ אפור |
| שיחת טלפון | `call` | אותם חמישה | צ׳יפ סגול |
| קבוצת וואטסאפ | `wa` | סיכום לילי · משימות · זיכרון · **ההודעות** | צ׳יפ טורקיז |
| עוזר AI | `ai` | סיכום ההתכתבות · משימות · זיכרון · **ההתכתבות** | צ׳יפ נייבי |

**עוזר AI הוא ערוץ, לא לוג של המוצר.** זה המקום היחיד שבו הלקוח מדבר
עם המערכת ולא עם אדם, ומה שהוא שואל מגלה מה מטריד אותו — «מה זאת אומרת
חריגה?» שווה יותר מכל מדד שימוש. נייבי כי זה צבע המערכת ולא של אף אדם.
בקוד המחלקה היא `.ai2` ולא `.ai`, כי `.ai` תפוס ב-`cli.css`.

**הנתונים אמיתיים, לא מומצאים.** הקבוצה נשענת על `js/ops.js · MSGS_THREADS`
(אותו קאסט, אותן הודעות, המונח «סיכום לילי») וההתכתבות עם העוזר על
`docs/cli/ai.html · WA_CHAT`.

**למה קבוצה היא לא פגישה.** לפגישה ולשיחה יש התחלה וסוף; לקבוצה יש **יום**.
לכן הישות היא "סיכום היום", אין לה משוב על היועץ (אין מפגש לבקר), ויש בה
דבר שאין בשתי האחרות — **מי כתב**. זה מוצג כשורת כותבים בראש הסיכום, ובטאב
ההודעות כל הודעה נושאת שם ושעה.

**איפה נוגעים כדי להוסיף ערוץ רביעי.** `KINDS` בתוך `arenaShow` (תווית · שם
טאב הסיכום · אייקון), `KCHIP` שבונה את עמודת הערוץ, `KSEG` שבונה את הבורר,
ושלוש שורות CSS (`.kchip.X` · `tr.m.X td:first-child` · `.ksegs .on[data-k=X]`).
שום מקום אחר.

**המילה היא «ערוץ», לא «סוג».** זה המונח שכבר חי במסכי הלקוח
(`docs/cli/ai.html`, `docs/cli/calls.html`) — ואין סיבה להמציא שני שמות
לאותו דבר בשני צדדים של אותה מערכת.

**הרצועה העליונה נושאת את מה שתקוע, לא את מה שהבא.** הספירה לאירוע הבא
חיה במסך «היום» וכפילות שלה כאן היא כפילות. מה שייחודי למסך הזה הוא
`stripQ()`: כמה שיחות ממתינות לשיוך, ולמה זה חוסם — «בלי שיוך לחברה
התמלול לא מתחיל». המספר ירד מהטאב כדי שלא יופיע פעמיים.

**הבורר סופר מהשורות עצמן.** `ksegRender()` נגזר מ-`data-kind` שנצרב על
כל שורה בזמן בניית העמודה. אין רשימה שנייה לתחזק, ולכן המספרים לא יכולים
להתנתק מהתוכן.

**עמודת הסוג נבנית ב-JS.** השורות כתובות ב-HTML; לולאה אחת אחרי הטעינה
מסירה את המדליון מתא הלקוח ומזריקה `<td class="kd">` עם צ׳יפ. עדיף על
ארבעים תאים ידניים שיישברו בעריכה הבאה.

**בורר הלקוח הוא הבקרה הראשית.** כשמסננים לקוח רואים עליו *הכל* — פגישות,
שיחות והקבוצה — ולכן הבורר יושב בשורת החיפוש, ולא בפינה: כפתור נייבי מלא
עם "לקוח: ‹שם› ✕" כשיש בחירה. שם הלקוח מופיע רק שם; שורת המצב מציגה מספר
בלבד, לא חוזרת על השם.

**אין `<select>` נייטיב במסך.** הבורר הוא רכיב שלנו עם חיפוש, ✓ על הנבחר,
סגירה ב-Escape ובלחיצה בחוץ. אותו כלל בתור הלא־מזוהות: כשהמספר מוכר והלקוח
מחזיק כמה חברות כל שורה היא «שם הלקוח — חברה»; כשהמספר לא מוכר הרשימה ארוכה
ולכן יש בה חיפוש.

### באג פונטים שהיה בכל מסכי היועץ

חמשת הקבצים ב-`docs/adv3/` **טענו Heebo** מגוגל, אבל ה-CSS שלהם מבקש
`Rubik` לגוף ו-`Frank Ruhl Libre` לכותרת. אף אחת מהשתיים לא נטענה, ולכן
העברית נפלה לפונט ברירת המחדל של המערכת — וזה מה שנראה מטושטש בתוך
ה-iframe, בזמן שהמעטפת (`index.html`, שכן טוענת Rubik) נראתה חדה.

**חשוב:** פונטים לא עוברים מהמסמך העוטף ל-iframe. כל מסך מוטמע חייב
לטעון בעצמו את מה שה-CSS שלו מבקש. `docs/cli/` תקין — טוען Heebo ומשתמש
ב-Heebo. אחרי תיקון כזה צריך רענון קשיח, כי ה-HTML נשמר במטמון.

### מה אין בקבוצת וואטסאפ

אין «אישור ושליחה»: הלקוח כתב את ההודעות בעצמו, ואין לו מה לאשר. הסיכום
הוא כלי פנימי של היועץ, ולכן `kind==='wa'` לא מקבל פס פעולות בכלל,
והסטטוס שלו — כמו של שיחת טלפון — הוא תמיד **«סיכום נוצר»**.

### «סיכום נוצר» — הסטטוס של הערוצים האוטומטיים

לשיחה ולקבוצה אין שלב אנושי: ההקלטה או ההודעות נכנסות, והסיכום נוצר.
לכן אין להם «אישור ושליחה» ואין להם «מה חסר» — אין מה לחסר. הסטטוס נבחר
כ«סיכום נוצר» ולא «עובד בהצלחה», כי הראשון מדבר על התוצר שהיועץ צריך
והשני מדבר על הצינור שהריץ אותו. עמודת «מה חסר» ממשיכה לעבוד רק לפגישות,
ששם השלבים החסרים הם אנושיים: הוקלטה · אושרה · נשלחה.

### מה אין בשיחה לא מזוהה

**אין נושא.** הנושא נולד מהתמלול, והתמלול מתחיל רק אחרי השיוך לחברה.
לפני זה ידועים רק המספר, מי מחזיק אותו, ואילו חברות יש לו — והחברות
מוצגות בשורה עצמה, כך שהבורר הוא הבחירה ולא הגילוי.

### הקבוצה מסוכמת ליום, ולכן היום כתוב

לקבוצה אין שעה — יש לה יום. בשורה, התת־שורה אומרת «סיכום יומי» (התאריך
כבר בשורה הראשית, ואין טעם לכתוב אותו פעמיים), ובזירה שדה `day` נושא את
התאריך המלא ומופיע בכל שלוש הכותרות: מי כתב · מה עלה בקבוצה · ההודעות.
`day` הוא שדה מפורש ולא חיתוך של `meta` לפי מיקום, כדי ששינוי בניסוח
לא ישבור כותרת.

### תקשורת = מה שכבר קרה

פגישה עתידית אינה ברשימה. אין לה סיכום, תמלול או משימות, והיא חיה
ב«היום». המסך הזה הוא יומן של מה שהתקיים.

### תכנון תזרימי — יישור לשפת המערכת (עיצוב בלבד)

`monthly-goal-index.html`: פס נייבי (Frank Ruhl Libre) בראש העמוד עם כלי
הטווח/רענון/ייצוא בסגנון שקוף־בהיר; קוביות היתרות ירדו לשפה השקטה —
מסגרת דקה, מספר גדול, אדום עמוק #C43D30 רק ליתרה בחריגה (בלי פסים
סגולים/כחולים); המקרא הפך ללוח אחד. בטבלה: קווי עמודות עדינים; כותרת
סעיף היא כותרת — נייבי על לבן עם אוויר מעליה וקו מתחתיה, בלי פס צבוע
ובלי ריבועים; ולכל שורות הסה״כ/רווח **רקע אחיד אחד** (EEF2F6#) —
הסמנטיקה חיה בצבע הפונט (ירוק להכנסות, אדום לעלויות), כי רקעים
צבעוניים מתחרים זה בזה ופונט צבוע על רקע אחיד נקרא כמו דוח של רו״ח.
התוכן והלוגיקה לא נגעו.

### היום — כל כותרת מעל העמודה שלה

`docs/adv3/today.html`: «היומן שלי» ישב בראש הפאנל כולו ונראה כאילו הוא
של עמודת המשימות. הועבר לתוך `calmain`, מעל היומן עצמו; «המשימות שלי»
כבר הייתה בתוך `calside`.

---

## עזרה בכל מסך

**אייקון ? אחד בפס העליון** (`js/help.js`), לא אחד פר מסך — כדי שהמשתמש
ילמד פעם אחת איפה העזרה, וכדי שהוספת מסך תדרוש נגיעה בקובץ תוכן אחד
ולא בשלושים קבצי HTML. הכפתור פותח את ההסבר של **המסך הפעיל**.

**זיהוי המסך** נשען על אותם משתני מצב שהניווט עצמו משתמש בהם —
`adminShell.show` · `OPSMODE` · `SCOPE`/`viewCli` · `CUR_TAB` · `ROLE` ·
`ADV_PVIEW` · `MGR_VIEW` · `ADM_CUR` — כדי שלא ייווצר מקור אמת שני
שיכול להתנתק מהמסך שבאמת מוצג. שים לב: «יומן» ו«הגדרות» נפתחים דרך
`showTab` גם מחוץ לחברה, ולכן הם נבדקים לפני ענף התיק.

**התוכן** ב-`js/help-content.js` — 33 רשומות המכסות 42 מסכים.
מבנה רשומה: `t` · `shot` · `lede` · `secs[{h,b,shot?,cap?,note?,kv?}]`
ואופציונלית `cli_lede`/`cli_secs`/`cli_shot` — הגרסה של בעל העסק, שהיא
white-label מלא. בתצוגת לקוח היא גוברת, ומסך `cli.X` בלי רשומה משלו
נופל ל-`co.X` עם גרסת הלקוח שלה.

**העיצוב** (`css/help.css`) — מודאל מרכזי דו־טורי ולא מגירת צד, מנימוק
אחד: **הצילום הוא התוכן, ולכן הוא קובע את הרוחב.** גיליון של 520px היה
מציג מסך HK מוקטן פי שלושה. מספור הסעיפים נגזר ב-CSS counters ולכן
מסונכרן בלי לגעת ברינדור; לניווט יש סימון «איפה אני»; ולצילום הגדלה
בלחיצה. `Escape` סוגר קודם את ההגדלה ורק אז את העזרה.

## צינור צילומי המסך

`./qa/shots.sh docs/shots [שמות]` — מזריק זמנית את `qa/shot-boot.js`
לסוף `js/main.js`, מריץ Chrome headless על `index.html?shot=KEY`
ומחזיר את `main.js` למקומו גם בכשל. **הרישום ב-`shot-boot.js` הוא
המקור היחיד לשמות הצילומים** — מסמכי השיווק, המצגת והעזרה כולם מפנים
אליהם. גישה לפונקציות בתוך iframe נחסמת ב-`file://`, ולכן ניווט בתוך
מסך מוטמע נעשה דרך חוזה הכתובת שלו (`?m=` · `?v=` · `?tab=`).

## חומרי המכירה

| קובץ | מה זה |
|---|---|
| `docs/ADVISORS_FEATURES.md` | קטלוג הפיצ׳רים המלא, מסודר לפי ארבעת עמודי הערך |
| `docs/LANDING_PAGE_BRIEF.md` | מסמך שיווק לצ׳אט שבונה את דף הנחיתה — כותרות, כאב, מתמטיקה, התנגדויות, הוכחות |
| `docs/HK-presentation.html` | המצגת — 12 שקפים, מצב הצגה מלא (חצים · F · N להערות · P להדפסה · ?slide=N) |
| `docs/HK-presentation.pptx` | אותה מצגת ל-PowerPoint, עם הערות דובר |

**המצגת היא הצגת מוצר, לא קמפיין.** כל שקף עונה על שלוש שאלות — מה זה,
מה זה עושה, ומה היתרון ליועץ. שני כללים שנלמדו בדרך: **צילום מסך מלא
מוצג ב-48% ואי אפשר לקרוא אותו** — ולכן כל שקף מציג `z-*`, חיתוך מוגדל
של האזור שמדובר עליו (נוצר ב-PIL מתוך `docs/shots/`); ו**שבעה שקפים
באותה תבנית נקראים כמסמך** — ולכן יש שישה ארכיטיפים של פריסה:
`cover` (ערימת צילומים) · `map` · `wide` (זום על כל הרוחב + שלוש קוביות) ·
`side`/`narrow` (זום + טור יתרונות) · `flowsl` (דיאגרמה + זום) ·
`tall` (זום לאורך, פריסה הפוכה) · `pair` · `artifact` · `tiles` · `sum` · `close`.
